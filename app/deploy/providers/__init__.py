"""Deploy-target drop-ins. Add a cloud by dropping ONE file here exposing a
module-level ``PROVIDER`` (a ``BaseDeployProvider`` instance) + a ``FEATURE``
header — the registry auto-discovers it. See app/deploy/registry.py."""
