# `ui/chat/js/` — chat and session management modules

Split from the original monolithic `chat.js` (1637 lines) and `sessions.js` (2468 lines) into focused modules, each under ~520 lines. Everything flows one direction — no circular imports.

## Chat modules (bubble rendering, streaming, sending, UI)

| Module | Exports | Purpose |
|--------|---------|---------|
| `chat-bubble.js` | `addChatBubble`, `updateLastBubble`, `_fillAgentBubble`, `linkifyText`, `escapeHtml`, `_renderMarkdownBody`, `_formatRelativeTime`, `_refreshAllBubbleTimes`, `_parseCreatedAt` | Bubble DOM creation, markdown rendering (marked + DOMPurify), linkification, relative timestamps. Sets `app.addChatBubble`, `app._linkifyText`, `app._renderMarkdownBody` at module scope. |
| `chat-bubble-actions.js` | `_addBubbleActions`, `_getBubbleText`, `_getBubbleCopyText`, `_bubbleAnchorId`, `_resetBubbleDeleteBtn`, `_resetAllBubbleDeleteBtns`, `_handleBubbleDeleteClick`, `_deleteTurn`, `_makeBubbleDeleteBtn`, `_setDeleteIcon`, `_setActionIcon`, `_renderActionIcons`, `_setBubbleModel`, `_setBubbleModelFromMeta`, `_modelLabelFromMeta` | Per-bubble action row (footer): **per-turn model tag** (`model EFFORT`, e.g. `deepseek-v4-flash HIGH`, left-truncated so the model's end stays visible), collapse/expand, read-aloud (SpeechSynthesis), copy, and per-turn delete (two-click trash → ⚠️ → confirm, resolved server-side by walking the parent chain). The footer renders on **streaming** bubbles too — a live reply, or one being recovered from the DB by the reconcile loop, gets the row as soon as it has real text (placeholder `…` is skipped); only the destructive **delete** button is held back until the turn finalizes (the streaming class drops → `_setBubbleText` re-calls `_addBubbleActions`, which adds delete via the reuse branch). The model + reasoning-effort come from the assistant interaction's `metadata` (persisted by the loop's `_build_meta`) on history/reconcile, and from `app._activeTurnModel`/`app._activeTurnEffort` (set in `chat-activity.js` from the live `llm_call_start` event before the first token) on a fresh turn. Sets `app._addBubbleActions` at module scope. |
| `chat-stream.js` | `_stripToolCalls`, `appendStreamToActiveBubble`, `finalizeAgentResponse`, `ensureStreamingBubbleForActiveTurn`, `finalizeAgentStep`, `markAgentInterrupted`, `seedStreamingBubble`, `attachToolCallsToLastBubble`, `_setBubbleText`, `_findAgentBubbleForTurn`, `_wsTurnBuffers` | Live streaming from WebSocket: accumulates chunks per turn (`_wsTurnBuffers`), finalizes/interrupts agent steps, attaches tool-call accordion panels to agent bubbles. **Lazy tool-call bodies:** a panel built from light history data keeps its call list on the container; the first time the "N tool calls" head is opened it fetches the bodies from `/session-turn-detail` and rebuilds its rows in place (preserving which were expanded). Live calls carry their bodies already, so they never trigger a fetch. Sets all streaming `app.*` methods at module scope. |
| `chat-send.js` | `sendMessage`, `sendStopMessage`, `abortChatStream`, `applyChatGate`, `_canChat`, `_autoResizePill`, `_updateScrollIndicator`, `_updateInputRowState`, `_saveDraft`, `_clearDraft`, `_restoreDraft`, `_renderPendingBubbles`, `_startOutboxPoll`, `_outboxHasPending`, `_persistLastSessionSeq`, `_loadLastSessionSeq` | Message sending pipeline: outbox retry queue (survives refresh), draft persistence, chat gate (sign-in check), session_seq persistence, self-healing watchdog, stop/interrupt, pill textarea auto-resize + scroll indicator dot. Sets `app.abortChatStream` and `app.refreshChat` at module scope. `app.refreshChat` (the 60s self-healing backstop) is guarded by the same remote-change check as `refreshTranscript`: unchanged → no DB resync/re-render (still clears a stuck "processing" state so the composer recovers); changed → full clean reload. |
| `chat-ui.js` | `initChat` | Wires everything together: sets remaining `app.*` methods (`app.updateLastBubble`, `app.autoResizeChatInput`, `app.sendChatMessage` — the exported `sendMessage`, so shared modules like the footer compaction panel's "Compact now" can stage `/compact` + send, etc.), binds chat input/keyboard/send events, execution mode toggle, continue/stop buttons with 500ms poll, scroll-to-bottom chevron with auto-scroll lock, input area padding (ResizeObserver), mobile focus-on-first-tap, initial gate + draft restore. Also calls `initTaskFrames()`. |
| `chat-terminal-engine.js` | `initTerminalChatEngine`, `activateTerminalChat`, `deactivateTerminalChat`, `sendTerminalInput`, `focusTerminalChat` | **Terminal Chat** agents (engine `terminal_chat`): replaces the whole bubble area with a live xterm.js terminal bound to a PTY (shared registry in `app/api/terminal.py`). On session load `activateTerminalChat` POSTs `/api/v1/chat/terminal-chat/activate` to get/create the PTY id, mounts xterm, and opens the terminal WS for output (string frames are JSON control — ping is answered with pong, never painted). `term.onData`/`onResize` forward keystrokes **and mouse events** (when the program enables mouse reporting) + viewport size to the PTY over the WS — the same mechanism as the admin terminal (`ui/shared/js/terminal.js`); Shift+Tab is intercepted to CSI Z. The chat pill sends single lines over HTTP `/write` (carriage-return Enter) via `chat-send.js`. On mount it **lazily imports the plugin-owned on-screen keys** (`/plugins/engines/terminal_chat/ui/terminal-keys.js`) and hands them `sendTerminalInput`/`focusTerminalChat`; on unmount it calls their `unmountTerminalKeys()`. Sets `app.terminalChatKey`/`app.terminalChatFocus`. |
| `chat-components.js` | `initChatComponents` | Renders server-validated declarative agent components. Sticky panels live outside the virtualized transcript; inline components attach to the next finalized agent bubble. The initial interactive component is a persistent `todo_list`. |
| `chat-task-frames.js` | `initTaskFrames` | Draws a **border around every run of bubbles in the same "task"** (one request + the agent's work + any approval/feedback turns). **Grouping comes from the backend** `GET /admin/settings/tasks/session/{id}` (the authoritative source — it runs the keyword rule **plus a fast LLM tie-breaker** that folds on-subject follow-ups onto the current task, which a client-only rule can't do). The fetch builds a `userMsgId → task index` map; the draw assigns every bubble to a task by that map. Until the fetch lands (first paint / endpoint down) it falls back to a **local text-only rule** so a border still shows instantly. **Both paths ignore synthetic `[ORCHESTRATION EVENT]` / event/automation "user" rows** (mirror of `_is_synthetic_user` in `app/admin/tasks.py`) so a background wake-up never splits a session. The frames are a **non-reparenting overlay** — absolutely-positioned boxes appended behind the bubbles in `#chat-messages-inner` — so streaming, reconcile, per-bubble actions, and the virtual scroller are untouched. A `MutationObserver` (+ scroll/resize) redraws them; placeholders preserve height so a task's extent stays correct when virtualised. Re-pulls the grouping on session change + when a new genuine user turn appears. Sets `app.redrawTaskFrames` + `app.refreshTaskGrouping`. |

| `chat-session-changes.js` | `initSessionChanges`, `refreshSessionChanges` | Admin-only working-tree drawer in the chat header. It snapshots the dirty path list when a session is first opened in the browser, shows only paths that become dirty afterwards with +/- counts, and calls the path-scoped Git API to undo or commit only those paths. |

## Data layer (shared between chat and session modules)

| Module | Exports | Purpose |
|--------|---------|---------|
| `chat-message-cache.js` | `_messageCache`, `_CACHE_TTL_MS`, `_sessionFocus`, `_FOCUS_LS_KEY`, `_FOCUS_MAX`, `_WINDOW_RADIUS`, `_loadingSessions`, `_loadSessionFocus`, `_persistSessionFocus`, `_captureSessionFocus`, `_cacheAppendMessage`, `_rememberSessionManifest`, `_transcriptChangedRemotely` | In-memory message cache (Map keyed by sessionId, 60s TTL), per-session scroll-position persistence (localStorage, up to 50 sessions), windowed-render constants. `_cacheAppendMessage` is used by both the WS streaming path and the session loader to keep the cache in sync. **Transcript manifests (remote-change detection):** keeps each session's last-known server manifest (`authority_revision` + `content_hash`, persisted to localStorage) and `_transcriptChangedRemotely` asks the server via `/session-messages?manifest_only=1&known_revision=&known_hash=` whether the transcript changed. Every refresh path consults this FIRST and must do NOTHING to the screen when the server says `not_modified: true`. |
| `chat-virtual-scroll.js` | `_bubbleHeights`, `_placeholderIds`, `_virtualScrollHandler`, `_VS_BUFFER`, `_storeBubbleHeight`, `_getBubbleHeight`, `_makePlaceholder`, `_recycleVisible`, `_recyclePlaceholderToBubble`, `_hookAddChatBubble`, `_installVirtualScroll`, `_teardownVirtualScroll`, `_loadingMoreMessages` | Bidirectional virtual scrolling: replaces finalized off-window bubbles with height-preserving placeholders and materialises only the buffered viewport. Streaming/focused/expanded bubbles stay live. Hooks `app.addChatBubble` to auto-store heights. Used by session-load for efficient session switching. |

## Session modules (dropdown, lifecycle, loading, agent selection)

| Module | Exports | Purpose |
|--------|---------|---------|
| `session-list.js` | `_sessionsCache`, `populateSessionSelect`, `_renderSessionRows`, `_setTriggerLabel`, `_clearSessionHeader`, `_fetchRelatedSessions`, `applySessionTitle`, `_formatRelativeTime`, `_truncate` | **Compatibility re-export shim** — the session-list logic now lives centralized in `ui/chat/elements/session-dropdown/list.js` (see the elements table below); this file re-exports it so existing importers keep working unchanged. Session dropdown list: fetches sessions from `/api/v1/db/sessions`, renders rows with drag handles, pin icons, status spinners (running/unread), spawn badges, relative timestamps. **Reliability policy:** the fetch validates `res.ok` + a real `sessions` array, NEVER clobbers a warm cache with an empty/invalid result (the server can answer 200 with `{"sessions": []}` when a mid-query exception is swallowed), and retries with bounded backoff — a cold cache keeps the skeleton phantom rows visible while retrying, so the dropdown always converges to the real list. Manages the header label and related-sessions chips — a persistent spawn-family tab bar (a "Main" tab for the orchestrator plus one tab per spawned helper — and per **delegated saved agent** (a `delegate_task_to_agent` target, carried as a `role: 'delegate'` child with a `share-2` icon and the agent's name as its label) — identical whether viewing the orchestrator or any member, with the currently-open session highlighted), plus browser-session tabs. The family + active highlight come from `/related`'s `orchestrator`/`spawns` fields. `applySessionTitle` handles the live titler from the WebSocket. `_clearSessionHeader` blanks the header synchronously when a switch begins (label/icon/status/agent-name/mode/target/chips + the dropdown's `data-loaded` "not loaded" CSS state) so the old session's identity never lingers while the new one loads. |
| `session-core.js` | `interruptSession`, `switchToSession`, `deleteSession`, `patchSession`, `togglePin`, `startRename`, `_headerRenameSession`, `autoRenameSession` | Session lifecycle actions: switching sessions (teardown, loading spinner, load chat, drain replayed events, send WS resume), deleting (interrupt run + DELETE API + switch to next or create fresh), pinning (PATCH), inline rename (dropdown row + header long-press, both use the same inline-input pattern), and **auto rename** (`autoRenameSession` — POSTs to `/api/v1/db/sessions/{id}/auto-title` so the Session Namer app function re-titles the session on demand, forced past any lock or special prefix; falls back to a first-message title in browser-storage mode). |
| `session-agent.js` | `_agentsCache`, `_lastSessionPerAgent`, `_loadLastSessionMap`, `_saveLastSessionMap`, `findWebagentAgent`, `createWebagentAgent`, `ensureWebagentAgent`, `populateAgentSelect`, `_refreshAgentAbilities`, `_agentIconFor`, `_setAgentTriggerLabel`, `_renderAgentRows`, `_fetchAgentRunningStatuses` | Agent selection: loads agents from API, manages the agent cache (`_agentsCache`), the last-session-per-agent map (localStorage), and the WebAgent singleton (find-or-create from the `default` template). `populateAgentSelect` sets `app.currentAgentId`. Abilities cache (`window.__currentAgentAbilities`) gates the "Send to agent" button. |
| `session-load.js` | `loadSessionChat`, `refreshTranscript`, `_maybeLoadMoreOnScrollTop`, `_showDebugBubble` | Core session loader: fetches messages from `/api/v1/db/session-messages`, handles `restricted` responses (token refresh + retry, auto-create fresh session), renders via windowed virtual scroll, attaches tool-call panels from persisted data, sets up **bidirectional** infinite scroll. **Fast open:** the opening fetch is small and, when a saved scroll position exists for the session, CENTRED on it (`around_id`) instead of always hauling the newest 100 — so a long session reopens right where the user left off. Older rows page in on scroll up (`before_id`); newer rows on scroll down (`after_id`, `_maybeLoadMoreOnScrollBottom`) when opened mid-history. **Light open:** every fetch is `light=1` — tool-call BODIES (arguments, results, LLM input/output) are NOT downloaded; only headings (name + duration) render, and `chat-stream.js` lazy-fetches a panel's bodies from `/session-turn-detail` the first time it's expanded. The reconcile cursor is seeded at the session's true latest seq (`max_session_seq`) so opening mid-history never backfills the tail. The ctx indicator is set from the server's whole-session `context_tokens` (the small window would under-report). Exposes `_maybeLoadMoreOnScrollTop`/`_maybeLoadMoreOnScrollBottom` for the virtual-scroll handler. While a load runs it sets `app._sessionLoadInFlight` and calls `app.abortNeighborWarm()` (cancelling any background neighbour pre-load — the on-screen session always wins); once the load settles it calls `app.warmNeighborSessions()` to warm the next/prev pinned neighbours when idle (see `session-swipe.js`). `refreshTranscript` is a **hard** reset of just the transcript area for the current session (drops the message cache, clears the bubbles, re-fetches + re-renders from the DB; never reloads the page) — wired to the header refresh button beside the agent name; differs from `app.reloadCurrentSession`, which reuses the cache. **Remote-change guard:** before doing any of that it asks the server (manifest_only validation via `_transcriptChangedRemotely`) whether the transcript actually changed — if the server says NOT modified it returns immediately and the screen is untouched (rubber-band pull animation still plays; only the actual re-fetch/re-render is skipped). Sets `app.loadSessionChat` + `app.refreshTranscript` at module scope. |
| `session-swipe.js` | `_initPinSwipeNavigation` | Touch/pointer swipe between pinned sessions: horizontal drag slides the current transcript away and reveals the next pinned session's bubbles (preloaded from cache). Includes desktop prev/next buttons. **Background neighbour warming:** pre-loads ONLY the next + previous pinned sessions (the two the arrows/swipe can reach next) so navigation feels instant, and ONLY when idle — every warm fetch is `requestIdleCallback`-scheduled (timer fallback), runs one-at-a-time at `priority: 'low'`, is `light=1`, mirrors what a real open would download (anchored `around_id` window when that neighbour has a saved scroll position, else newest), and is skipped while a turn is running / the active session is loading / the tab is hidden. Exposes `app.warmNeighborSessions` (the active-session loader calls it once a load settles) and `app.abortNeighborWarm` (the loader calls it the instant a new load begins, so a warm can never compete with the on-screen session). |
| `session-notification.js` | `initSessionNotification`, `checkSessionCompletions` | Sliding toast panel that watches `_sessionsCache` for `running → done/interrupted/error` transitions on non-current sessions. Toasts persist (until manually dismissed or an enabled timer fires), re-appear after refresh and on every device — they're stored in the caller's per-user DB (`session_notifications` table in `data/user_data/{user_id}/{user_id}.db`, API `/api/v1/db/session-notifications`, hybrid-synced cross-device). A "more" (⋮) sub-menu offers "Close when session visited" and an optional timed auto-dismiss. Deduplicates per page session. Hooked into the 15s poll in `session-init.js`. |
| `session-init.js` | `registerSessionApi`, `initSessions` | Entry point: `registerSessionApi` sets `app.populateSessionSelect`, `app.populateAgentSelect`, `app.loadSessionChat`, `app.applySessionTitle`, `app.reloadCurrentSession` and wires focus persistence. `initSessions` orchestrates the chat header: **mounts the session-dropdown controller** (logic centralized in `ui/chat/elements/session-dropdown/` — controller.js + list.js; mounted here for the static chat-side-panel.html markup, since the control loader reuses existing DOM without calling the element's `init()`), wires the agent-name dropdown + agent picker popup (+ button), header delete button, **header refresh button** (resets the transcript via `refreshTranscript`), TUI bridge status poll, session list poll, **completion notification init**, and swipe navigation. Boot sequence: `populateUserSelect` → `populateSessionSelect` → `loadSessionChat`. |

## Session dropdown element (centralized home)

| Module | Exports | Purpose |
|--------|---------|---------|
| `elements/session-dropdown/index.js` | `html`, `init`, `destroy`, `style` | Element contract for the `session_dropdown` control (manifest: `ui/chat/elements/_manifest.json`). `init()` mounts the controller (idempotent); the static `chat-side-panel.html` markup is mounted at boot by `session-init.js` because the control loader reuses existing DOM without calling `init()`. |
| `elements/session-dropdown/controller.js` | `mountSessionDropdown`, `unmountSessionDropdown` | The dropdown menu controller: open/close/position/flip-up animation, keyboard dismissal, the loading skeleton, trigger wiring (click / Enter / space / long-press rename / kebab / delete), the per-row "more" popup, menu click delegation (switch / delete / bin-restore / visibility / checkbox / expand / search / manage footer), pinned-row drag reorder and long-press rename. `mountSessionDropdown(root)` returns `{ open, close, isOpen, destroy }` and stores it on `root._sessionDropdownController`. |
| `elements/session-dropdown/list.js` | `_sessionsCache`, `populateSessionSelect`, `_renderSessionRows`, `_setTriggerLabel`, `_clearSessionHeader`, `_fetchRelatedSessions`, `applySessionTitle`, `_formatRelativeTime`, `_truncate`, `toggleSessionGroup`, search/bin state helpers | Session data + rendering (the former `ui/chat/js/session-list.js` body, moved). Fetch validates `res.ok` + a real `sessions` array, never clobbers a warm cache with an empty/invalid result, and retries with bounded backoff so a transient server blip can't leave a stub-only dropdown. |

## Import chain

```
main.js
├── chat-ui.js (initChat)
│   ├── chat-bubble.js         (addChatBubble, linkifyText, ...)
│   ├── chat-bubble-actions.js (_addBubbleActions, ...)
│   ├── chat-stream.js         (appendStreamToActiveBubble, ...)
│   │   └── chat-message-cache.js (_cacheAppendMessage)
│   └── chat-send.js           (sendMessage, applyChatGate, ...)
│
└── session-init.js (registerSessionApi, initSessions)
    ├── chat-message-cache.js  (_loadSessionFocus, ...)
    ├── chat-virtual-scroll.js (_installVirtualScroll, ...)
    ├── session-load.js        (loadSessionChat, ...)
    │   └── session-list.js    (shim → elements/session-dropdown/list.js)
    ├── session-core.js        (switchToSession, ...)
    ├── session-agent.js       (populateAgentSelect, ...)
    ├── session-swipe.js       (_initPinSwipeNavigation)
    └── elements/session-dropdown/controller.js (mountSessionDropdown)
        ├── elements/session-dropdown/list.js   (cache, fetch+retry, render)
        └── session-core.js    (switch/delete/pin/rename actions)
```

## Boot order

1. **Module evaluation** (ES import time) — all `app.*` assignments at module scope run immediately in dependency order
2. **`initChat()`** — sets remaining chat methods, wires input/send/stop/continue, execution mode, scroll chevron, gate, draft restore
3. **`registerSessionApi()`** — sets session API methods on `app`, loads saved focus positions
4. **`initSessions()`** — builds dropdown UI, agent picker, header buttons, starts polling (TUI bridge, sessions, related sessions), runs boot sequence (user-panel → sessions → load current chat)
