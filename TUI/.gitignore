# build artifacts
build/
dist/
*.spec
webagent.exe

# python
__pycache__/
*.pyc
.venv/
*.egg-info/

# generated at exe-build time (stamped into the bundle, then removed)
tui_app/_build.py

# local runtime data — lives in tui-runtime/ when run from source
# (the SQLite store, config, watchdog config/alarms, logs, attachments, PIDs)
tui-runtime/
