# build artifacts
build/
dist/
*.spec
webagent-tui
webagent-tui.exe

# python
__pycache__/
*.pyc
.venv/
*.egg-info/

# generated at exe-build time (stamped into the bundle, then removed)
webagent_tui/_build.py

# local data (the external DB lives in the per-user data dir, not here — but
# guard against accidental local copies)
*.db
*.db-journal
*.db-wal
config.json
