{
  "_note": "Human-readable tool list for the Server Manager. The code (tools/registry.py) binds each name to a handler + parameter schema + mutating/needs_project flags; the PROSE the model reads (description) and whether a tool is exposed (enabled) live HERE. Edit a description to change how the agent understands a tool; set enabled=false to hide one. Unknown names are ignored; a tool missing from this file keeps its Python default description and stays enabled.",
  "tools": {
    "link_project": {
      "category": "manager_state",
      "enabled": true,
      "description": "Link the manager to an EXISTING webAgent checkout (a folder with run.py + app/). Switches to managed mode and adopts that repo's AI key. Use when the user already has a copy."
    },
    "setup_launch_shortcut": {
      "category": "manager_state",
      "enabled": true,
      "description": "Android/Termux only: write a Termux:Widget home-screen shortcut that launches the manager (~/.shortcuts/webagent.sh). Use as the FINAL onboarding step on Termux, then tell the user to install the Termux:Widget add-on and add the widget. Mutating."
    },
    "check_install_readiness": {
      "category": "install",
      "enabled": true,
      "description": "Read-only preflight for a fresh install: OS, Python 3.11-3.12, git, internet, browser capability, and (if given) the target folder's space/emptiness. Run this first."
    },
    "clone_repo": {
      "category": "install",
      "enabled": true,
      "description": "Clone the public webAgent repo into an empty/new target folder. Mutating. Recommended target: C:/webagent (Windows) or ~/webagent."
    },
    "setup_environment": {
      "category": "install",
      "enabled": true,
      "description": "Build the install's virtual environment and install dependencies (and the headless browser unless unsupported). Slow (minutes). Mutating."
    },
    "seed_config": {
      "category": "install",
      "enabled": true,
      "description": "Write the install's .env + provider.json (seeding the app's AI key) + db_connection.json (local SQLite). Mutating."
    },
    "verify_install": {
      "category": "install",
      "enabled": true,
      "description": "Verify a fresh install imports cleanly and its local DB initialises. Mutating (creates the local database)."
    },
    "server_status": {
      "category": "server",
      "enabled": true,
      "description": "Is the local webAgent server up? Read-only."
    },
    "server_start": {
      "category": "server",
      "enabled": true,
      "description": "Start the local webAgent server (detached) and verify health. Mutating."
    },
    "server_stop": {
      "category": "server",
      "enabled": true,
      "description": "Stop the local webAgent server the manager started. Mutating."
    },
    "server_restart": {
      "category": "server",
      "enabled": true,
      "description": "Restart the local webAgent server. Mutating."
    },
    "server_logs": {
      "category": "server",
      "enabled": true,
      "description": "Tail the captured server log. Read-only."
    },
    "check_updates": {
      "category": "server",
      "enabled": true,
      "description": "Check whether the linked checkout is behind the public repo (read-only). Offer to pull if so."
    },
    "reset_app": {
      "category": "server",
      "enabled": true,
      "description": "Reset the linked webAgent install to a clean state (like reset_webagent.bat). NON-DESTRUCTIVE by default: ALWAYS wipes only the userbase (ACTIVE database backend + visuals/users/) — credentials are kept so the app still works. Credentials now live outside the user-data DB: the LLM key/provider config (provider.json), local logins (users.json), and integration OAuth tokens + secrets (the dedicated app/db/vault.db) all survive a default reset. Backend-aware via app/db_connection.json: active Postgres (postgres/neon/gcp_cloud_sql) has its schema dropped & recreated (irreversible, NOT backed up, stray SQLite files left alone); active SQLite has local.db + sidecars removed (backed up). Opt-in flags: clear_logs (diagnostics logs.db + recordings.db — kept by default), clear_secrets (AI key/provider config + vault.db OAuth/integration creds), clear_users (local accounts + tokens), delete_env (.env), delete_agents (agent template JSONs — zero agents after). backup also covers visuals/users + opt-in file groups; stops the server first. The DB, default admin/admin user, and agents regenerate on next start (if agent JSONs were kept). Mutating — confirm first."
    },
    "app_login": {
      "category": "app_control",
      "enabled": true,
      "description": "Log into the RUNNING webAgent app over its HTTP API (the same API the web UI uses) and cache the session. Defaults to the local admin (admin/admin). Read-only side effect — just authentication. app_chat logs in on its own, so use this only to confirm credentials or surface the user_id."
    },
    "app_list_agents": {
      "category": "app_control",
      "enabled": true,
      "description": "List the chat agents available to the logged-in app user (id + name) so you can target one with app_chat(agent_id=...). Read-only."
    },
    "app_chat": {
      "category": "app_control",
      "enabled": true,
      "description": "Talk to the RUNNING app's agent AS the logged-in user (default admin/admin) and return its reply — a one-shot synchronous chat. For an ongoing live conversation in the connected panel, use webapp_send instead. The app auto-creates the session and uses the user's default agent unless you pass agent_id. Mutating: the app's agent may run tools and take real actions, so confirm intent first. Needs the server running."
    },
    "webapp_send": {
      "category": "app_control",
      "enabled": true,
      "description": "Send a message to the CONNECTED web-app session (the active target set in the Connect panel) and return the app agent's reply. Use this when the WebAgent is unmuted and you're bridged to a session — it holds a real back-and-forth and the exchange streams into the shared transcript. Mutating; the app agent may run tools and take real actions."
    },
    "webapp_status": {
      "category": "app_control",
      "enabled": true,
      "description": "Report the connected web-app target (agent + session) and live-stream health. Read-only."
    },
    "app_list_sessions": {
      "category": "app_control",
      "enabled": true,
      "description": "List the logged-in app user's chat sessions (id, title, agent). Optionally filter by agent_id. Read-only."
    },
    "app_get_settings": {
      "category": "app_control",
      "enabled": true,
      "description": "Read the app's settings (app-settings.json): access mode, presentation mode, run-watchdog tuning, etc. Read-only."
    },
    "app_set_settings": {
      "category": "app_control",
      "enabled": true,
      "description": "Update one or more app settings (app-settings.json). Pass an object of the keys to change; the rest are preserved. Mutating."
    },
    "app_get_auth_keys": {
      "category": "app_control",
      "enabled": true,
      "description": "Read the app's LLM provider config (the auth-key entry in the DB): provider, model, base URL; the API key is masked. Read-only."
    },
    "app_set_auth_keys": {
      "category": "app_control",
      "enabled": true,
      "description": "Set the app's LLM auth key / provider / base URL / model (writes the DB auth_elements row + provider.json). Only the fields you pass change; the key is never echoed. Mutating — confirm with the user first."
    },
    "read_diagnostics": {
      "category": "diagnostics",
      "enabled": true,
      "description": "Read the webAgent app's diagnostics — its flight-recorder of warnings/errors (with tracebacks), agent-loop problems, run outcomes, tool errors, every HTTP request (access) and WebSocket lifecycle (ws) — straight from the checkout's dedicated logs database (app/db/logs.db; falls back to local.db on older builds), so it works even when the server is DOWN. Filter by level (error/warning) or category (server/http/access/ws/loop/run/recovery/tool). Use it to diagnose a broken server. For BROWSER/UI-side problems (wrong render, lag, a JS error, a failed fetch in the page) use read_recordings instead — that's the browser flight-recorder."
    },
    "read_recordings": {
      "category": "diagnostics",
      "enabled": true,
      "description": "Collect the webAgent app's RENDER RECORDER yourself — the browser flight-recorder of HTML snapshots, DOM-mutation deltas, lag/long-task metrics, JS errors, console warnings, and failed/slow network calls — read STRAIGHT from the checkout's recordings DB (data/db/recordings.db), so it works even when the server is DOWN. This is the browser-side counterpart to read_diagnostics (which is server-side). The recorder is OFF by default; YOU can turn it on with app_set_settings({\"render_recording_enabled\": true}), reproduce the UI behaviour in the browser (or via the connected app session), read it back here, then turn it off. Filter by kind (snapshot/mutation/lag/js_error/console/network/nav/meta), level, session_id, since_minutes, or search; pass rec_id or include_html=true to also pull the captured HTML (large — keep limit small). Read-only. Use this whenever a UI/rendering change needs verifying or a browser-side bug needs evidence."
    },
    "monitor_status": {
      "category": "monitoring",
      "enabled": true,
      "description": "Report the background watchdog's state: whether it's on, its interval, autonomy level, notify channels, thresholds, the current server health, and what it has reacted to recently. Read-only."
    },
    "list_alarms": {
      "category": "monitoring",
      "enabled": true,
      "description": "List the current alarm rules the watchdog evaluates — each rule's match (text/category/level), action, loudness, and channels. Read-only."
    },
    "add_alarm": {
      "category": "monitoring",
      "enabled": true,
      "description": "Add a standing alarm rule the watchdog evaluates every heartbeat. Match by 'contains' text and/or level/category; action is 'notify' or 'auto_restart'; loudness is 'every' / 'once' / 'digest'. Use this when the user says 'tell me every time X happens'. Mutating (edits alarms.json, picked up live)."
    },
    "remove_alarm": {
      "category": "monitoring",
      "enabled": true,
      "description": "Remove an alarm rule by its id (from list_alarms). Use when the user says 'stop watching that'. Mutating."
    },
    "set_monitor_config": {
      "category": "monitoring",
      "enabled": true,
      "description": "Change the watchdog configuration: enable/disable, interval_seconds, autonomy (notify|auto_restart|self_heal), channels, auto_restart, thresholds (error_rate_threshold, disk_percent_threshold, mem_percent_threshold, cpu_percent_threshold — a percent of 0 disables that resource check), and the Playbook's self-healing policy: remediation_mode (document|safe_auto|autonomous), remediation_confidence_threshold (0–1), program_trigger_after (occurrences before a recurring issue gets an auto-programmed alarm). Mutating (edits monitor.json, picked up live)."
    },
    "server_resources": {
      "category": "monitoring",
      "enabled": true,
      "description": "Report server + host resources right now: host CPU, memory, disk free, and the webAgent server process's memory. Read-only. Use when the user asks how much CPU/RAM/disk things are using or suspects a memory leak / full disk."
    },
    "notify_test": {
      "category": "monitoring",
      "enabled": true,
      "description": "Send a test notification on the configured channel(s) so the user can confirm alerts reach them. Read-only side effect (just a notification)."
    },
    "playbook_list": {
      "category": "monitoring",
      "enabled": true,
      "description": "List every issue the self-healing Playbook has learned: how often each fired, its status, and its best-known remedy + confidence. Read-only. Check this before guessing at a recurring problem."
    },
    "playbook_show": {
      "category": "monitoring",
      "enabled": true,
      "description": "Show one Playbook issue in full (key from playbook_list): every remedy with its helped/didn't-help record, and the recent incidents (how it was triggered + how each was resolved). Read-only."
    },
    "playbook_record_remedy": {
      "category": "monitoring",
      "enabled": true,
      "description": "After you diagnose a recurring issue, record the fix so the watchdog can reuse it next time. kind ∈ restart_server|clear_port|escalate_to_agent|notify_only|command|note ('command' = a shell command in payload, 'note' = a written instruction). Custom commands are stored as 'suggested' and won't auto-run until approved (approved=true, or the user approves it on the Playbook screen). This is how the system gets smarter over time."
    },
    "playbook_set_remedy": {
      "category": "monitoring",
      "enabled": true,
      "description": "Approve, disable, or re-prioritise a Playbook remedy (status ∈ approved|suggested|disabled; higher priority breaks confidence ties). Mutating (manager state)."
    },
    "playbook_forget": {
      "category": "monitoring",
      "enabled": true,
      "description": "Delete a whole Playbook issue (key=...) and its history, or a single remedy (remedy_id=...). Use when something was learned wrong. Mutating (manager state)."
    },
    "web_search": {
      "category": "web",
      "enabled": true,
      "description": "Search the web via DuckDuckGo. Returns titles, URLs, and snippets (no API key required). Use to research errors, find current docs, check dependency versions, or look up solutions. Read-only."
    },
    "browser_navigate": {
      "category": "web",
      "enabled": true,
      "description": "Navigate the shared headless Chromium browser to a URL and return a plain-text snapshot of the page (title, visible text up to 6000 chars). ALWAYS start with this before inspecting or interacting with a page. Read-only."
    },
    "browser_snapshot": {
      "category": "web",
      "enabled": true,
      "description": "Capture an accessibility snapshot of the CURRENT page: a numbered list of interactive elements (buttons, links, inputs, selects) with their CSS selectors, ARIA roles, labels, and visible text. Use this after browser_navigate to find what to click, type into, or read. Read-only."
    },
    "browser_screenshot": {
      "category": "web",
      "enabled": true,
      "description": "Take a full-page PNG screenshot of the current page (or a single element matching an optional CSS selector) and save it to a temp file. Returns the absolute file path. Use when the user wants to SEE the page, verify a visual change, or debug layout. Read-only."
    },
    "browser_click": {
      "category": "web",
      "enabled": true,
      "description": "Click an element on the current page by CSS selector. ALWAYS use browser_snapshot first to find the element's selector. If multiple elements match, use index (0=first). Waits for any navigation triggered by the click and reports the new URL + title. Mutating (changes page state)."
    },
    "browser_type": {
      "category": "web",
      "enabled": true,
      "description": "Type text into an input/textarea element. Clears the field first, then types with a small human-like delay. If submit=True, presses Enter and waits for navigation. Use browser_snapshot first to find the right selector. Mutating (changes page state)."
    },
    "browser_evaluate": {
      "category": "web",
      "enabled": true,
      "description": "Run an arbitrary JavaScript expression in the current page and return the result (JSON-serialised). Use for extracting data from the DOM, counting elements, checking state — anything the snapshot doesn't cover. Read-only."
    },
    "browser_close": {
      "category": "web",
      "enabled": true,
      "description": "Close the shared browser and free its memory. Call this when you're done browsing. The browser will restart automatically on the next browser_* call. Mutating (frees resources)."
    },
    "read_source": {
      "category": "files",
      "enabled": true,
      "description": "Read a file region with line numbers."
    },
    "write_source": {
      "category": "files",
      "enabled": true,
      "description": "Create or overwrite a file (auto-backup). Mutating."
    },
    "edit_source": {
      "category": "files",
      "enabled": true,
      "description": "Exact unique find-and-replace in a file. Mutating."
    },
    "patch_source": {
      "category": "files",
      "enabled": true,
      "description": "Fuzzy find-and-replace (whitespace tolerant). Mutating."
    },
    "delete_source": {
      "category": "files",
      "enabled": true,
      "description": "Delete a file or (recursive) directory; backed up. Mutating."
    },
    "search_source": {
      "category": "files",
      "enabled": true,
      "description": "Case-insensitive text search across the tree."
    },
    "read_directory": {
      "category": "files",
      "enabled": true,
      "description": "List a directory tree to a given depth."
    },
    "run_command": {
      "category": "execution",
      "enabled": true,
      "description": "Run a shell command in the project root. Mutating."
    },
    "run_python": {
      "category": "execution",
      "enabled": true,
      "description": "Run a Python snippet with the project on sys.path. Mutating."
    },
    "terminal_open": {
      "category": "execution",
      "enabled": true,
      "description": "Open a NEW local terminal session and optionally run a command (e.g. command='claude'). Returns a session id + first screen. Launch any interactive program you then drive. Mutating."
    },
    "terminal_read": {
      "category": "execution",
      "enabled": true,
      "description": "Read what's on a terminal session's screen (recent output, ANSI-cleaned). Read-only."
    },
    "terminal_send": {
      "category": "execution",
      "enabled": true,
      "description": "Type into a terminal session; `keys` sends named special keys (enter, y, ctrl+c, arrows) to answer prompts. Waits for the program to react, returns the screen. Mutating."
    },
    "terminal_wait": {
      "category": "execution",
      "enabled": true,
      "description": "Wait until the program reacts and settles (done, awaiting input), then return the screen. Read-only."
    },
    "terminal_list": {
      "category": "execution",
      "enabled": true,
      "description": "List your open local terminal sessions. Read-only."
    },
    "terminal_close": {
      "category": "execution",
      "enabled": true,
      "description": "Kill a terminal session and the program in it. Mutating."
    },
    "git_tool": {
      "category": "vcs",
      "enabled": true,
      "description": "Structured git. Read-only: status, log, diff, show, branch, remote, ls-files, rev-parse, reflog, tag. Mutating: add, commit, push, pull, fetch, reset, checkout, restore, switch, merge, rebase, cherry-pick, revert, stash apply/pop/drop. Never force-push."
    },
    "resolve_conflict": {
      "category": "vcs",
      "enabled": true,
      "description": "Resolve merge conflicts in a file by keeping one side. Mutating."
    },
    "self_status": {
      "category": "self_update",
      "enabled": true,
      "description": "Report how THIS manager is running (source checkout vs frozen exe), its version/build, where its code lives, and whether a newer version is available upstream. Read-only."
    },
    "self_update": {
      "category": "self_update",
      "enabled": true,
      "description": "Update the manager's OWN code. Always backs up first (timestamped): source mode pulls the repo (fast-forward only); a frozen exe rebuilds itself from fresh source and stages the new exe. Set restart=true to apply immediately (closes + relaunches the manager). Mutating."
    },
    "self_restart": {
      "category": "self_update",
      "enabled": true,
      "description": "Close and relaunch the manager — applies a staged exe swap (frozen) or reloads just-pulled source. Mutating."
    }
  }
}
