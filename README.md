# webAgent

A **FastAPI** service with a **tool-calling** LLM agent (OpenRouter), optional **Supabase** or **local SQLite** persistence, and a **vanilla JS** UI at **`/index.html`** (static assets under `/ui/`).

> **📖 Full feature docs live in the in-app Wiki** (`data/wiki.db`). The list below is a
> brief index - each line links to its Wiki article. Open a link with the app running
> and it deep-links straight to the article (`?tab=wiki&article=<slug>`). When you change
> user-facing behaviour, update the Wiki article rather than this README - see
> `docs/claude/repo-conventions.md`. Developer/architecture, setup and deployment detail
> stays below.

## Features

Every feature has a Wiki article; this is the map. Browse them all in the **Wiki** tab,
or jump straight to one at `http://localhost:8080/?tab=wiki&article=<slug>`.

### Get started

- **[Welcome to webAgent](http://localhost:8080/?tab=wiki&article=welcome-to-webagent)** - What webAgent is and what you can do with it.
- **[What webAgent Is](http://localhost:8080/?tab=wiki&article=what-webagent-is)** - A self-hostable app that puts a tool-using AI agent behind a clean chat UI, with workspaces around it.
- **[Quick Tour: Your First Conversation](http://localhost:8080/?tab=wiki&article=quick-tour-your-first-conversation)** - A walkthrough of the first few minutes.
- **[The Interface: Tabs at a Glance](http://localhost:8080/?tab=wiki&article=the-interface-tabs-at-a-glance)** - What each top tab is for.
- **[Key Concepts & Glossary](http://localhost:8080/?tab=wiki&article=key-concepts-glossary)** - The handful of ideas the whole app is built from.

### Using webAgent

- **[Chatting with an Agent](http://localhost:8080/?tab=wiki&article=chatting-with-an-agent)** - How chat works - connection-independent runs, live streaming, interrupts.
- **[Sessions & Conversation History](http://localhost:8080/?tab=wiki&article=sessions-conversation-history)** - Conversation threads, history, pinning and hiding.
- **[Attachments & Voice Input](http://localhost:8080/?tab=wiki&article=attachments-voice-input)** - Sending images, audio, video and files, and dictating by voice.
- **[Suggested Replies & Message Actions](http://localhost:8080/?tab=wiki&article=suggested-replies-message-actions)** - Suggestion chips, copy / read-aloud, and deleting a turn.
- **[Pages (Your Dashboard Workspace)](http://localhost:8080/?tab=wiki&article=pages-your-dashboard-workspace)** - Personal visual dashboards an agent builds and updates for you.
- **[Using the Wiki](http://localhost:8080/?tab=wiki&article=using-the-wiki)** - The shared knowledge base - including the shareable article links this README uses.

### How agents work

- **[How Agents Work: The Mental Model](http://localhost:8080/?tab=wiki&article=how-agents-work-the-mental-model)** - The four building blocks behind everything an agent can do.
- **[The Agent Loop](http://localhost:8080/?tab=wiki&article=the-agent-loop)** - The repeating cycle that produces every answer.
- **[Prompts & Context](http://localhost:8080/?tab=wiki&article=prompts-context)** - An agent's standing instructions and background.
- **[Memory](http://localhost:8080/?tab=wiki&article=memory)** - How an agent remembers things across conversations.
- **[Abilities](http://localhost:8080/?tab=wiki&article=abilities)** - A single switch that unlocks a bundle of related tools.
- **[Tools & How They're Exposed](http://localhost:8080/?tab=wiki&article=tools-how-theyre-exposed)** - Individual actions, and how each is presented to the agent.
- **[Skills (On-Demand Knowledge)](http://localhost:8080/?tab=wiki&article=skills-on-demand-knowledge)** - Written playbooks loaded only when a task needs them.
- **[Choosing & Switching Models](http://localhost:8080/?tab=wiki&article=choosing-switching-models)** - How the model for each turn is chosen and changed.

### Agents working together

- **[Built-in Agents](http://localhost:8080/?tab=wiki&article=built-in-agents)** - The ready-made agent templates the app ships with.
- **[Building Your Own Agent](http://localhost:8080/?tab=wiki&article=building-your-own-agent)** - Creating and shaping your own agents.
- **[Delegation & Hand-off](http://localhost:8080/?tab=wiki&article=delegation-hand-off)** - Handing one conversation between agents mid-thread.
- **[Spawning Helper Agents](http://localhost:8080/?tab=wiki&article=spawning-helper-agents)** - One agent spawning purpose-built helpers.
- **[The Prompt Optimizer](http://localhost:8080/?tab=wiki&article=the-prompt-optimizer)** - Automatically improving an agent's instructions over time.

### Capabilities & integrations

- **[Web Access (Search, Weather, Maps)](http://localhost:8080/?tab=wiki&article=web-access-search-weather-maps)** - No-account web search, weather and maps.
- **[Browser Control & the Live Browser](http://localhost:8080/?tab=wiki&article=browser-control-the-live-browser)** - An agent driving a real web browser you can watch.
- **[Shared Browser Architecture](http://localhost:8080/?tab=wiki&article=shared-browser-architecture)** - How you and the agent share one live browser page.
- **[Connected Accounts (Integrations)](http://localhost:8080/?tab=wiki&article=connected-accounts-integrations)** - Letting an agent act in your email, calendar, files and social accounts.
- **[External Data Sources](http://localhost:8080/?tab=wiki&article=external-data-sources)** - Attaching an agent to your own databases and documents.
- **[Automation, Events & Scheduling](http://localhost:8080/?tab=wiki&article=automation-events-scheduling)** - Agents that run on a schedule or react to events.
- **[App Control (Agent Drives Your Screen)](http://localhost:8080/?tab=wiki&article=app-control-agent-drives-your-screen)** - Two-way: an agent rearranging your own screen to match its work, and you right-clicking (or long-pressing, for touch/mobile) anywhere to point at a UI element and hand it to the chat (the point-and-share panel — `ui/shared/js/app-control-point.js`). The handoff carries a full fingerprint of the clicked element — its label, a CSS locator path, the live computed style (colour/size/font), a markup slice, the page region, and x/y — so the agent can pinpoint the exact control and style rule with no extra context. **Only the words you type go into the chat pill** (a clean, editable message); the fingerprint rides along separately as the request's `app_control` field, and the backend (`app/api/chat.py` `_maybe_emit_app_control`) renders it as its own **foldable `app_control` tool chip** in the transcript (live + on reload, the same machinery as the image-describe chip) while folding the fingerprint to the agent for that turn — so the message bubble stays just your words while the agent still receives exactly what you clicked. The chip wording is the backend-applied `ui_handoffs.app_control_point.template` in `app-prompts.json`. The point-and-share panel is always available (it is not gated on whichever agent you're currently chatting with). By default it drops only your words into the chat box for you to review and press Send (targeting whatever agent is active); set `ui_handoffs.app_control_point.auto_send` to `true` in `app-prompts.json` to make it send immediately instead, falling back to the default **WebAgent** (`app/defaults/agents/default.json`) only when no chat is open.
- **[Image Generation](http://localhost:8080/?tab=wiki&article=image-generation)** - Creating images from a description.
- **[Image Vision (Reading Images)](http://localhost:8080/?tab=wiki&article=image-vision-reading-images)** - Understanding images you attach, even on text-only models.

### Terminal & desktop

- **[The In-App Terminal](http://localhost:8080/?tab=wiki&article=the-in-app-terminal)** - A real terminal, in your browser.
- **[Agents Driving Terminals](http://localhost:8080/?tab=wiki&article=agents-driving-terminals)** - An agent opening and operating interactive terminal programs.
- **[Terminal Tunnel (You Drive a Program)](http://localhost:8080/?tab=wiki&article=terminal-tunnel-you-drive-a-program)** - You driving a program through the app.
- **[The Server-Manager TUI & Launchers](http://localhost:8080/?tab=wiki&article=the-server-manager-tui-launchers)** - Terminal desktop apps for running and managing the server.

### Admin & configuration

- **[App Config Overview](http://localhost:8080/?tab=wiki&article=app-config-overview)** - The single admin control panel for the platform.
- **[Administrator Tools (Privileged)](http://localhost:8080/?tab=wiki&article=administrator-tools-privileged)** - Privileged read / write / run capabilities for self-administration.
- **[Diagnostics & the Flight Recorder](http://localhost:8080/?tab=wiki&article=diagnostics-the-flight-recorder)** - The built-in running record of what the app is doing.
- **[Agent Orchestration Configuration](http://localhost:8080/?tab=wiki&article=agent-orchestration-config)** - Configuring the spawn-helpers ability.
- **[App Control Configuration](http://localhost:8080/?tab=wiki&article=app-control-config)** - Configuring the screen-control ability.

### Self-hosting & operations

- **[Installing & Running webAgent](http://localhost:8080/?tab=wiki&article=installing-running-webagent)** - The recommended install / run path (uv) - see also Installation below.
- **[Supported Devices & Platforms](http://localhost:8080/?tab=wiki&article=supported-devices-platforms)** - Where webAgent can run.
- **[Models & Providers](http://localhost:8080/?tab=wiki&article=models-providers)** - How webAgent talks to AI models.
- **[Storage Backends](http://localhost:8080/?tab=wiki&article=storage-backends)** - Choosing where your data is stored.
- **[Secrets & Encryption](http://localhost:8080/?tab=wiki&article=secrets-encryption)** - How sensitive values are kept in a vault.
- **[Remote Access](http://localhost:8080/?tab=wiki&article=remote-access-reach-your-pc-from-anywhere)** - Reaching a home / office PC from your phone or another network.
- **[Deploying to the Cloud](http://localhost:8080/?tab=wiki&article=deploying-to-the-cloud)** - Running webAgent as an always-on service.
- **[Editions and the Production Path](http://localhost:8080/?tab=wiki&article=editions-and-the-production-path)** - Shipping curated production builds from one codebase.
- **[Billing & Usage](http://localhost:8080/?tab=wiki&article=billing-usage)** - Metering or billing usage if you want to.

### Developer reference

- **[Architecture Overview](http://localhost:8080/?tab=wiki&article=architecture-overview)** - A high-level map for anyone working on the code.
- **[Core vs Plugins: Structure Philosophy](http://localhost:8080/?tab=wiki&article=core-vs-plugins-structure-philosophy)** - The small-core / many-plugins split.
- **[Extending with Drop-in Plugins](http://localhost:8080/?tab=wiki&article=extending-with-drop-in-plugins)** - The defining rule: new capabilities are drop-in files, never core edits.
- **[Adding a Plugin](http://localhost:8080/?tab=wiki&article=adding-a-plugin)** - How to add a new capability.
- **[Agent Abilities (drop-in)](http://localhost:8080/?tab=wiki&article=agent-abilities-drop-in)** - How an ability is structured as a drop-in folder.
- **[Ability-Bundled Skills](http://localhost:8080/?tab=wiki&article=ability-bundled-skills)** - Skills shipped alongside an ability's tools.
- **[The FEATURE Header](http://localhost:8080/?tab=wiki&article=the-feature-header)** - The self-describing FEATURE dict every plugin can carry.
- **[The Feature Catalog and Discovery](http://localhost:8080/?tab=wiki&article=the-feature-catalog-and-discovery)** - How the app discovers what code is available.
- **[Edition Gating and Packaging](http://localhost:8080/?tab=wiki&article=edition-gating-and-packaging)** - How the active edition decides what ships.

## Architecture and module map

> Prefer the plain-English version? See the Wiki's **Developer Reference** — [Architecture Overview](http://localhost:8080/?tab=wiki&article=architecture-overview), [Core vs Plugins](http://localhost:8080/?tab=wiki&article=core-vs-plugins-structure-philosophy), and [Extending with Drop-in Plugins](http://localhost:8080/?tab=wiki&article=extending-with-drop-in-plugins). The tables below are the exact module/route reference.

### HTTP + WebSocket Architecture

The agent loop (`app/agent/loop.py`) runs as a **supervised background task owned by the Run Manager** (`app/agent/run_manager.py`), independent of any client connection. All user messages enter via **HTTP POST** (no WebSocket sends): `POST /api/v1/chat/send` saves the message + starts the run and returns immediately. The WebSocket is a **receive-only subscriber** — connected once per user, it receives events for all of that user's sessions. The DB is the source of truth (assistant answers stream into `interactions`; `session_runs` tracks the live turn); the WebSocket is a live accelerant.

```text
       [CLIENT]
          |
          |--- (HTTP POST) ---> [ app/api/chat.py ]   send message; run starts server-side
          |                           |
          |                    +-- POST /api/v1/chat/send    (fire-and-forget; primary)
          |                    +-- POST /api/v1/chat/stream  (SSE fallback; tails the run)
          |                    +-- POST /api/v1/chat         (buffered, inline)
          |                    +-- POST /api/v1/chat/interrupt  (graceful stop)
          |                           |
          |                    [ app/api/uploads.py ]        POST /api/v1/upload
          |                    store_file()  → bytes saved + attachment row
          |                           |
          |                           v
          |              +--------------------------+
          |              | SUPERVISED BACKGROUND RUN|
          |              | run_manager -> loop.py   |
          |              | (survives disconnects)   |
          |              +--------------------------+
          |                           |
          |              events emitted to:
          |              ├── SSE response body     (primary chat bubble display)
          |              └── Listener registries:
          |                    ├── _visualizer_listeners[session_id]   (per-session WS)
          |                    └── _user_listeners[user_id]            (per-user WS)
          |                           |
          |                           v
          |--- (WS receive-only) -> [ app/api/agent.py ]   /api/v1/agent/ws
                                      mode: "user_subscriber"
                                      optional resume: { session_id: last_session_seq }
                                      receives: stream, response, tool_call,
                                                tool_result, pipeline, db events
                                      for ALL sessions belonging to user
                                      On (re)connect, replays any buffered
                                      events with session_seq > last seen
                                      from app/agent/run_buffer.py.

Events are routed on the frontend:
  - "stream" / "response" events for the CURRENT session → chat bubble
       (SSE drives the bubble for the in-flight POST; WS takes over for
        replays after refresh / session-switch via
        `appendStreamToActiveBubble` and `finalizeAgentResponse`)
  - "tool_call" / "tool_result" / "pipeline" / "db" events → stream/loop/flow debug panels
  - Every event carries `session_seq`; the client tracks the highest seq
    per session in `app.lastSessionSeq` so the next WS handshake can
    request replay of only-newer events.
```

### Backend (`app/`)

| Module | Role |
|--------|------|
| **`main.py`** | FastAPI app: routers, CORS, no-cache for `/`, `/ui/`, `/web-terminal`, and `/index.html`, **`StaticFiles`** for `/ui/`, `/web-terminal` (the Terminal-tab page, `html=True`), and `/screenshots`, **`GET /`** serves the main UI (`index.html`), **`GET /index.html`** (alias for legacy bookmarks), **`GET /test`**, **`GET /privacy`** (serves `ui/privacy.html`), **`GET /tos`** (serves `ui/tos.html`), **`GET /health`**, **`GET /favicon.ico`** + **`GET /favicon.svg`**, **`GET /sw.js`** (PWA service worker at root scope), **`GET /ui/manifest.json`** (PWA manifest with correct `application/manifest+json` MIME type), **`POST /api/v1/restart`**, shutdown (browser + terminal + watchdog). **Startup self-healing:** `mark_orphans_for_resume()` flags runs the previous process left mid-flight as `server_restart`, then — after the scheduler/event runtimes are up — `resume_orphans_at_boot()` re-ignites the eligible ones, and `start_watchdog()` launches the liveness sweep. |
| **`api/chat.py`** | **`POST /api/v1/chat/send`** (fire-and-forget; primary), **`POST /api/v1/chat/stream`** (SSE fallback that tails the run), **`POST /api/v1/chat`** (buffered, inline), **`POST /api/v1/chat/interrupt`** (graceful stop via the Run Manager — tags `user_stop`), **`POST /api/v1/chat/resume`** (one-click re-ignite of a run held by the auto-resume opt-out, or any stopped turn — backend-driven, works with no live WebSocket), **`GET /api/v1/chat/self-heal/status`** (watchdog counters + runs awaiting manual resume), **`POST /api/v1/chat/suggestions`** (silent Suggested-Replies generation — single-shot LLM call, never persisted/streamed; returns short candidate next-user-messages for the pill chips) + **`GET|PUT /api/v1/chat/suggestions/config`** (read/update the engine's mode `off`/`on`/`scheduler` + chip count; backed by `app/agent/suggestions.py`). `_prepare_send()` does the synchronous prep (auth, agent + access/billing gating, user-message persist, RunBuffer + `session_runs` start, user_message broadcast); `_run_turn_background()` is the connection-independent turn executor (memory search → prompt build → attachment/vision fallback → history → agent loop → emit/persist → run-state finalize) handed to the Run Manager via `RunManager.start_or_replace`, recording run-state with **`origin='web'` + a `relaunch_ctx` recipe** so a dead/frozen turn can be re-ignited headlessly; `_resume_web_turn()` is the registered `web` resume builder (re-streams a resumed turn into the chat via the same RunBuffer + WS path); `_sse_tail_run()` backs the SSE fallback. Also: **listener registries** — `register_user_listener()` / `register_visualizer_listener()` for per-user and per-session WebSocket broadcasting. |
| **`api/agent.py`** | **`WebSocket /api/v1/agent/ws`** — **receive-only per-user subscriber**. Client sends `{"mode": "user_subscriber", "user_id": "..."}` to register. Server streams all agent events (user_message, stream, response, tool_call, tool_result, pipeline, db) for all of that user's sessions. No message processing — all sends go through HTTP POST. Also carries **out-of-band UI-sync events** so open tabs/devices stay live without a refresh: `agent_created` / `agent_trashed` / `agent_deleted` / `agent_restored` (Agents-grid lifecycle, broadcast from the agent CRUD endpoints) and `agent_status` (`running`/`idle`, emitted from the `session_runs` run-state chokepoints so the per-agent status dot reflects live runs). Broadcast via `notify_user()` in `app/api/chat.py`; consumed by `ui/shared/js/agentWs.js` → `app.onAgentLifecycleEvent`. |
| **`api/browser_stream.py`** | Live browser panel for the **Web** page, addressing a **browser session** (`bs_id`, a first-class persistent tab) — the same Playwright page the agent's `browser_action` drives. **`GET /api/v1/browser/proxy?token=&bs_id=&url=`** — fetches the page through Playwright and returns its HTML in an `<iframe>` with an injected bridge script (the default in-app view). **`WebSocket /api/v1/browser/ws?token=&bs_id=`** — command channel: carries navigation (navigate/back/forward/reload) and pushes `nav` events when the agent navigates (URL polled every 1s). **`WebSocket /api/v1/browser/screencast?token=&bs_id=`** — the **live pixel mirror**: a JPEG **CDP screencast** of the page (headless OR the on-device window) out, with the user's mouse / keyboard / scroll forwarded back in; used when the session is on the on-device backend and for pages the iframe can't frame. Token auth (exempted in `app/auth/middleware.py`); page via `get_or_create_page()` in `app/tools/browser.py`. Headless-safe — works on the Cloud VM. **REST (tab management):** **`GET/POST /api/v1/browser/sessions`** (list / create), **`PATCH/DELETE /api/v1/browser/sessions/{bs_id}`** (rename, toggle `shared` + linked agent / close), **`POST /api/v1/browser/sessions/resolve`** (`{agent_id}` → the tab `resolve_agent_session` picks, auto-created if none), and the **backend switcher** **`GET/POST /api/v1/browser/sessions/{bs_id}/backend`** (`{mode:"local"|"headless"|"connector", profile?}` → `open_local_browser`/`use_remote_browser`/`use_connector_browser`; the `connector` mode points the session at the user's own browser via the extension — see `api/connector_ws.py`). |
| **`api/connector_ws.py`** | **Browser connector** — the server end of the **browser-extension** link (the third browser backend, beside headless + on-device). A user installs the **webAgent Browser Connector** extension (`clients/browser-extension/`); it opens **`WebSocket /api/v1/connector/ws?token=`** and runs the agent's `browser_action` commands in the user's **real, logged-in browser** — even across the internet from the server. Thin route over the engine in **`app/tools/browser_connector.py`** (per-user connection registry + RPC futures + `connector_execute`, which normalises replies into the `browser_action` contract and saves extension screenshots into the owner's home). `app/tools/browser.py` adds the **`"connector"` backend mode**: `browser_action` forwards the whole action via `_connector_action` when a session is on it (no server-side Playwright page — `_ensure_page` refuses, so the screencast/proxy never spin up a headless browser for a connector session). **`GET /api/v1/connector/status`** reports whether the caller has a live extension (drives the Browser page's switcher badge). **`GET /api/v1/connector/pair`** mints a **long-lived token** (≈1 year) for the signed-in caller, which the Browser page's **"Pair extension"** card (the plug icon → `openPairModal` in `ui/main-panel/browser/js/browser.js`) shows alongside the server URL for one-click copy into the extension — the no-developer-tools way for a user to authenticate the extension when the server isn't in open access mode (refused for anonymous callers). Auth mirrors the browser WS (JWT in `?token=`, open-mode tokenless fallback); a command is only ever routed to the authenticated owner's own connection. |
| **`api/uploads.py`** | **`POST /api/v1/upload`** — multipart file upload (images, audio, video, PDF, text). **`GET /api/v1/upload/{id}`** — metadata lookup. **`DELETE /api/v1/upload/{id}`** — delete. File bytes stored via `app/db/attachments/`. |
| **`api/terminal.py`** | **`WebSocket /api/v1/terminal/ws?session_id=<uuid>&token=<jwt>&name=<label>&claude_session=<id>`** + **`DELETE /api/v1/terminal/sessions/{session_id}`** + **`POST /api/v1/terminal/sessions/{session_id}/pause`** (take-over lock — `{"paused": bool}`) + **`POST /api/v1/terminal/sessions/{session_id}/rename`** (set a friendly per-user display name — `{"name": "..."}`; backs the **Your sessions** row 3-dot **Rename** action, reachable over HTTP so a session that isn't open in this browser can still be renamed — same field the WS `set_name` control writes) + **`GET /api/v1/terminal/sessions`** (caller's own live PTYs, used by the **Your sessions** sidebar so the user can reattach to shells started on another device; each row has a 3-dot menu = Rename / Delete; rows carry `agent_driven` / `launch_command` / `paused`) + **`GET /api/v1/terminal/quick-launches`** + **`GET /api/v1/terminal/claude-resume-target`** + **`GET /api/v1/terminal/tmux-sessions`** + **`POST /api/v1/terminal/tmux/send-keys`** (deliver one named key — currently **Shift+Tab** → tmux `BTab` — to a tmux session's active pane via `tmux send-keys`; body `{"session","key"}`, admin-only, session name validated. The browser keybar routes **Shift+Tab** here for **tmux tabs** because a TUI running inside tmux 3.x — e.g. **Claude Code** — negotiates extended keys with tmux and then ignores the legacy back-tab `\x1b[Z` the keybar would otherwise inject raw; letting **tmux** originate the key means it's encoded to match the pane's current keyboard mode. Non-tmux tabs and all other keys keep the raw-byte path. Front-end: `_parseTmuxSession` records a tab's tmux session name from its launch command and `_sendTmuxKey` in `ui/shared/js/files.js` calls this endpoint, falling back to the raw `\x1b[Z` on any failure) + **`POST /api/v1/terminal/paste-image`** (multipart `file=` — saves a pasted/dropped image under `data/uploads/terminal-paste/` and returns its **absolute server path**, which the frontend then types (double-quoted) into the terminal so **Claude Code** ingests it via its "image file path in the message" route; needed because the web terminal is a **text-only pipe** an image can't pass through, and Claude's own Ctrl+V reads the *server's* OS clipboard not the browser's — useless over the tunnel. Admin-only; prunes saved files older than `TERMINAL_PASTE_RETENTION_HOURS`, default 24h. Front-end: the keybar **Paste** chip reads an image off the clipboard via `navigator.clipboard.read()` and an image dropped onto the terminal is handled the same way — both in `ui/shared/js/files.js` `uploadTerminalPasteImage`). **Quick-launches** are sidebar shortcuts that open a new terminal tab. A short **tap** types the plain `command` (a one-off process in a normal terminal window); an optional `tmux_command` is run instead on a **long-press** (≥500ms) — the same thing wrapped in a named tmux session so it survives tab close/refresh and is reattachable from any device (a small "in tmux" confirmation flashes). Rows with no distinct `tmux_command` (e.g. `tmux attach`/`tmux ls`/plain shell) behave the same either way. One entry instead carries `action:"claude-resume"` — the **"Resume Claude"** launcher, resolved server-side at click time by `claude-resume-target` (which returns both a plain `command` and a `tmux_command`, tap vs long-press), which scans `~/.claude/projects/*/*.jsonl` (newest first), drops any conversation **already open** — two unioned signals: (1) a live **web-terminal tab** is running it (each "Resume Claude" tab connects with `?claude_session=<id>`, stashed on its `TerminalSession`; **cross-platform**, so a second click correctly skips to the next-newest on Windows too, and self-clears when the tab is closed), and (2) on Linux/Termux only, a `/proc` open-fd + `--resume <id>` command-line scan (also catches `claude` started by hand outside webAgent) — reads the winner's `cwd` **and name** (its `ai-title`, falling back to a summary / first user message) out of its transcript, and returns a **two-line** `cd '<cwd>'` + `claude --resume <id>` command (newline-separated, **not** `&&` — typed line-by-line with a real `\r` after the prompt settles, so it submits correctly in Windows PowerShell as well as bash) plus that conversation name as the tab label (or a fresh `claude` when nothing's resumable). Multi-session browser shell (PTY / **`pywinpty`** on Windows). One `TerminalSession` per client-supplied `session_id`, kept alive across WS reconnects so refresh / browser-close doesn't kill a running shell. Each session runs a **background pump** (`start_pump`) that owns the PTY's sole reader and drains output into a 64 KB scrollback ring (replayed on every attach) **and** fans it out to all subscriber queues — so output is captured (and the agent can read it) even with **no WebSocket attached**, and multiple watchers share one PTY. Friendly `name` via `?name=` or a `{"type":"set_name"}` control. The WS handler decodes the JWT and requires `user_profiles.is_admin = 1`; unauthorized connects close with code 4401. Dead PTYs are reaped lazily on each new session lookup. **Agent-facing API** (used by the Terminal Control tools): `open_agent_session` / `get_owned_session` / `snapshot_sessions` / `set_session_paused`, plus `TerminalSession.send_text` / `send_keys` / `read_text` (ANSI-stripped) / `wait_idle` (waits for the program to react *then* settle, so it doesn't false-settle on cold start). |
| **`api/webhooks.py`** | **`POST /api/v1/webhooks/{plugin_name}`** — communication channel webhooks (Telegram, WhatsApp, etc.). Delegates to plugins for auth and parsing. |
| **`api/webhooks_generic.py`** | **`POST /api/v1/webhooks/generic/{webhook_id}`** — generic inbound webhooks. Receives any external payload, routes to agent loop with custom instructions, returns agent reply. Logs all events for review. |
| **`api/events.py`** | **`POST /api/v1/events/{source}`** — push intake for event sources (Gmail Pub/Sub, Graph subscriptions, Drive/Calendar channels, Dropbox/Shopify webhooks). Source plugin owns verification + normalization; the router fans out to matching subscriptions. **`GET /api/v1/events/sources`** returns the source manifest (used by the automation parser). **`GET /api/v1/events/subscriptions`** lists subscriptions. |
| **`api/automations_dashboard.py`** | Automations dashboard API. **`GET /api/v1/automations/dashboard`** — read-only aggregation per agent (scheduled tasks, event subscriptions, spawns) + user-level webhooks + clone runners. **`GET /api/v1/automations/{id}/runs`** — per-run history from `automation_runs`. **`PATCH /api/v1/automations/{id}`** — edit / pause / resume. **`DELETE /api/v1/automations/{id}`** — delete (reaps a dedicated clone runner). **`POST /api/v1/automations/{id}/run-now`** — fire a scheduled task immediately. All scoped to the caller's own rows. |
| **`ui/automations/server.py`** | **`GET /api/v1/automations/dashboard`** (+ row CRUD / run-now) — aggregation endpoint unifying `agent_automations`, `agent_event_subscriptions`, `agent_spawns`, and `webhook_registrations` into a single per-agent view. Accepts optional `?agent_id=` filter and a **`?view=`** of `active` (default — only in-process/future automations: scheduled tasks with a next run, live spawns, event triggers, webhooks, clones), **`completed`** (a history of automations that have finished for good — one-off tasks that fired, expired/exhausted recurring tasks, terminal-status worker spawns — so the main list stays clean), or `bin` (the recycling bin). `active`/`completed` read the same non-deleted rows and differ only in which partition they keep; both responses carry **`completed_count`** for the toolbar's Completed badge. **Per-row deletes (used by the dashboard table's trash button), all scoped to the caller:** **`DELETE /api/v1/automations/{id}`** (scheduled task or event subscription — also reaps a dedicated clone runner), **`DELETE /api/v1/automations/spawn/{id}`** (a worker-spawn ledger row in `agent_spawns`), **`DELETE /api/v1/automations/clone/{id}`** (a standalone clone agent), **`DELETE /api/v1/automations/webhook/{id}`** (a user-level generic webhook). **Drop-in page backend**: lives inside the Automations page folder and is auto-mounted by `app/ui_pages.discover_routers()` (page.json `"router": "server.py"`) — removing the folder removes the endpoint, no `main.py` edit. |
| **`events/`** | Generalized event-trigger subsystem. **`base.py`** = `EventSource` abstract; **`manager.py`** = auto-discovers sources under `sources/`; **`router.py`** routes a `NormalizedEvent` to all matching `agent_event_subscriptions` rows; **`executor.py`** creates a session + runs the agent loop with the event payload injected and a `[DELIVERY CHANNELS]` overlay so the agent can ask the user where to send the result; **`poller.py`** (15s tick) drives poll-only sources; **`renewer.py`** (hourly) refreshes push subs before TTL; **`providers/google_pubsub.py`** + **`providers/graph_subscription.py`** are shared verification/subscription helpers. **`sources/`** has one file per integration: `gmail_source.py` (push via Pub/Sub when `EVENTS_GMAIL_PUBSUB_TOPIC` is set, else automatic 60s poll fallback via `users.history.list`), `outlook_mail_source.py`, `google_calendar_source.py`, `outlook_calendar_source.py`, `google_drive_source.py`, `onedrive_source.py`, `dropbox_source.py`, `shopify_source.py`, `twitter_source.py`, `reddit_source.py`, plus comms-bridge shims `telegram_source.py` / `slack_source.py` / `discord_source.py` / `twilio_sms_source.py` / `twilio_whatsapp_source.py`. |
| **`api/db_viewer.py`** | **`/api/v1/db/*`** — SQLite introspection; DB files under **`app/db/`** (default filename **`local.db`** for the UI query param `db=`). **`GET /api/v1/db/session-stats`** — aggregated per-session usage stats (tokens, duration, cost, turn count), plus **`has_canvas`/`has_browser`** flags marking sessions that built/edited a canvas or drove the live web browser (derived from the `tool_executions` log; surfaced as the Sessions table's **Links** column). **`GET /api/v1/db/session-messages`** — a session's messages for the chat viewer, each with its lifecycle `status`, plus a `run` object describing any in-flight turn (status + `latest_session_seq`) so a cold/second device can show the live indicator. **Three open modes by cursor:** no cursor → the **most recent** `limit` rows; **`around_id`** → a small window CENTRED on that message (`limit` rows each side — the "reopen exactly where I left off" path, fed by the saved scroll position so a long session doesn't haul its whole tail); **`before_id`/`after_id`** → the batch immediately older/newer (infinite scroll up/down). Responds with `has_more` (older remain) + `has_newer` (newer remain), `max_session_seq` (the session's true latest seq, so the live reconcile poll doesn't backfill the gap when opened mid-history), `context_tokens` (a whole-session estimate for the ctx indicator, since the small window alone would under-report it), and **`execution_mode`** (the session's recorded Ask/Plan/Auto mode, or `null` if never set — the chat panel applies it on load so the pill matches what the server will actually do even on a cold device / second browser; the server stamps this onto `sessions.metadata.execution_mode` on **every** turn, making the **session** the source of truth rather than the browser's localStorage, which closes the "pill says Ask but the run went Auto" desync). Ordering and paging use the composite **`(created_at, rowid)`** — `interactions.created_at` is only second-resolution, so a whole turn's rows often share a timestamp and `rowid` (insertion order) breaks the tie. **`&light=1`** (the chat's default) blanks the heavy tool-call bodies — per-turn LLM input, tool results, tool-call arguments — keeping the wire shape so only headings (tool names + durations) render; the rarely-viewed bodies aren't downloaded at all on open. **`GET /api/v1/db/session-turn-detail?session_id=&ids=`** serves those bodies on demand: the chat calls it with just the assistant ids in a tool-call panel the moment the user expands it, getting back each turn's full `input`/`output`/`metadata` + child tool rows. **`PATCH /api/v1/db/sessions/{id}`** — update a session's `title` and/or `pinned` flag. A manual `title` change also sets `metadata.auto_title_locked` so the background **Session Namer** ability (`plugins/abilities/Core/session_titler/`) stops overwriting it. **`POST /api/v1/db/sessions/reorder`** — persist the manual drag order (writes `sessions.sort_order` by index, scoped to the requesting user). **`DELETE /api/v1/db/sessions/{id}`** — recycle a session (soft delete → `status='recycled'`); add **`&permanent=true`** to erase it and its interactions. Either way it takes its whole **run-family** with it (`resolve_child_sessions`): the optimizer **Planner** (`optimizer-*`, `metadata.target_session`) + **Closer** (`closer-*`) it spawned, and any orchestrated **spawn** clones — recycled together (soft) or erased together (permanent). `restore` reverses the soft cascade. Phantom-safe: if the id has no `sessions` row (the Sessions page derived it straight from the `interactions` log because the table held no rows for the user, or only rows under another `user_id`), a soft delete would be a silent no-op, so it falls through to hard-deleting the orphan transcript — it actually disappears instead of reappearing on reload. **`DELETE /api/v1/db/turn?session_id=&interaction_id=&user_id=`** — delete one whole conversation turn (the root user message + its entire `parent_id` descendant closure: assistant steps, tool calls, memory writes) from `interactions`, resolved from any interaction id in the turn; strips that turn from the agent's rebuilt context. This backs the trash button on every chat bubble. Participant check accepts the optional **`user_id`** as a fallback identity when there's no matching JWT (tokenless local users, and TUI-/launcher-created sessions owned by `admin`) — exactly like the session list — so the chat-bubble delete works for local users; a claimed `user_id` still only authorizes when it matches the session's owner/participants. |
| **`agent/loop.py`** | Unified multi-turn loop (streaming + buffered): tool validation, parallel tool runs, pipeline events. **Streams the assistant answer into the DB**: the assistant row is inserted `status='streaming'` on first token, throttle-updated as text arrives (`AGENT_STREAM_PERSIST_INTERVAL`, default 0.6s), and finalized `complete` / `interrupted` / `error`; mid-stream interrupt is polled at the persist cadence. Emits per-step assistant events (`stream`/`response`/`agent_step_end` carry the `asst_id` of that step so the UI shows every step as its own bubble) and `attachment` events for frontend file rendering. Reads `LoopConfig` from the agent record to gate optional steps at runtime. Builds the effective destructive-tool set from `DESTRUCTIVE_TOOLS` baseline ∪ `agents.safety_policy.destructive_tools` ∪ per-tool `requires_confirmation` flags; supports `auto_confirm` and `max_concurrent_tools` from safety policy. **`run_command` per-arg exemption:** read-only inspect commands matching `SAFE_RUN_COMMAND_PREFIXES` (e.g. `git status`, `git log`, `git diff`, `ls`, `cat`, `grep`, version probes) bypass the guardrail confirmation gate; commands containing `;`, `&&`, `||`, `>`, `<`, `` ` ``, or `$(` are always treated as mutating. Emits `guardrail_skip` pipeline event when exempted. **Liveness heartbeat:** advances `session_runs.heartbeat_at` (best-effort, throttled to `AGENT_RUN_HEARTBEAT_INTERVAL`, default 5s) at each turn boundary and during token streams, so the watchdog can tell a genuinely frozen run from one that is simply between turns. **Self-bounded LLM call:** both phases of every model call are time-bounded so a hung provider can't freeze a turn indefinitely — the stream **open** (`AGENT_STREAM_OPEN_SECONDS`, default 120s) and each subsequent **token read** (`AGENT_STREAM_STALL_SECONDS`, default 45s); a breach of either aborts the turn as a resumable `crash` rather than relying on the client's own (sometimes silent) request timeout. |
| **`agent/loop_executor.py`** | **`LoopConfig`** — parses the `loop_logic` JSON column from an agent record and exposes `is_enabled(node_id)`. Supports two formats: flat string array (legacy, all nodes enabled) and object array (`[{"node": "skill_track", "enabled": false}, ...]`). Defines `LOCKED_NODES` (steps that can never be disabled: `user_input`, `load_tools`, `llm_call`, `execute_tools`, `check_continue`, `final_response`) and `GATED_NODES` (11 steps with runtime gating: `interrupt_chk`, `permission_chk`, `guardrails`, `delegation_chk`, `skill_track`, `memory_search`, `memory_save`, `fire_optimizer`, `copy_defaults`, `data_src_load`, `attachment_describe`). Pre-loop nodes (`memory_search`, `memory_save`, `copy_defaults`, `attachment_describe`) are gated in `chat.py`; in-loop nodes in `loop.py`. |
| **`agent/session_history.py`** | Maps **`interactions`** rows → OpenAI-style **`messages`** for the active session (excludes internal memory tools). **Context-strategy-aware:** when called with an `agent_id` it resolves the agent's enabled **context-management strategy** (`context_strategy_for_agent` in `app/abilities/`; default **Context Control**) and runs its write-side reshape — the default folds older turns via `agent/compaction.py` — then assembles the **compaction train** `[summary car 1] … [summary car N] + [verbatim hot tail]` from the `session_summary_segments` rows (each car's `end_index` marks where the verbatim tail begins) instead of replaying the whole transcript. `load_segments` folds a legacy single `session_summaries` row into one car so old sessions keep working. Raw rows are never deleted — they stay searchable and are retrievable verbatim by range. **`trim_history_for_resume`** further bounds a *resumed* turn's context to the last `RESUME_TAIL_MESSAGES` (default 12) plus the first-user-message task anchor and any leading summary, advancing past orphan tool results so tool-call pairing stays valid — a resume continues recent steps, it does not re-feed the full transcript (which grew the working set every attempt and drove runaway memory on the hot resume path). |
| **`agent/compaction.py`** | **Context compaction — the "compaction train"** (Part 2 of the Context Control ability). `maybe_compact` measures the assembled context and, when it exceeds `compact_threshold × token_limit`, peels the oldest turns that have aged past the verbatim hot tail (`tail_fraction × token_limit`) and turns them into **frozen summary "cars"** — each made from ~`segment_source_tokens` of raw turns compressed once to ~`segment_target_tokens`, stored as a row in **`session_summary_segments`** with the `[start_index, end_index)` range it covers, and **never re-summarised**. This replaces the old single-rolling-summary (which re-compressed already-summarised text every pass — lossy drift, repeated cost, cache-breaking). The one exception is the **far-back merge**: past `max_cars`, the oldest cars fold into one coarser `tier=1` block so the train stays bounded. Cuts only on user-turn boundaries; failure-safe (any LLM/DB error is a no-op). **`force=True`** is the agent-initiated path behind the **`compact_context`** tool: it peels *now*, skipping the fill-threshold gate and running even when background auto-compaction is off. Compacted detail is fully recoverable: the ability also ships **`recall_compacted`** (read the exact turns behind a car verbatim, by PART number or message range) and **`search_this_session`** (keyword-find a detail across the current session's full transcript). Summariser uses the same provider config as chat (`COMPACT_MODEL`/`LLM_MODEL`); settings resolved by `agent/context_control.py` from the **Context Control** ability's `agent_connections` row. **`token_limit` is model-aware:** when `auto_context_limit` is on (default), `get_context_settings(db, agent_id, session_id, user_id)` sizes it to the ACTIVE model's real context window (`resolve_active_model` → `model_catalog.lookup`), falling back to the stored number for an unknown model — so the gauge + compaction stay correct across per-session model switches; the car sizes are **proportional fractions** of that limit (`segment_source_fraction`/`segment_target_fraction`), so they scale from a 32k to a 1M window. Context Control is an **always-on safety device** (`locked_on`, auto-compaction forced on) invoked through the generic context-strategy seam (`context_strategy_for_agent`), so it is the **default** strategy, not a hard-wired call. |
| **`plugins/abilities/Core/session_titler/`** (**Session Namer** ability) | **Auto session-namer.** A `TURN_HOOK` drop-in ability (not core) dispatched after each chat turn from `chat.py` (fired fire-and-forget, like the memory save); summarizes the session's **first up-to-3 user messages** into a short name (1–10 words, biased short) with one lightweight LLM call (same shared client as `suggestions.py`/`compaction.py`). **Refines progressively** over user turns 1→2→3 — so greetings/early scope settle — then **locks** it (`sessions.metadata.auto_title_locked`); a late out-of-order hook never clobbers a locked name, and a manual rename also locks it. Pushes a `session_title` WS event (`generating`→`done`) over the per-user socket so the chat-header shows the load spinner then swaps in the name live. Best-effort: any failure keeps the existing name. Skips `optimizer-`/`closer-`/`slash-` sessions. Disable it / swap in a different naming strategy with no core edits. |
| **`agent/run_manager.py`** | **Run Manager** — server-side owner of every agent turn. Holds a strong, supervised reference to each run (keyed by `session_id`) so it survives client disconnects and cannot be garbage-collected mid-run; `start_run`, `start_or_replace` (a new user message interrupts the active run — graceful then hard-cancel — and starts the replacement once the prior run has finalized), `is_running`, `interrupt` (sets the DB flag the loop polls **and tags `stop_cause` — `user_stop` for the Stop button, `replaced` for a superseding message — so the self-healing layer never auto-resumes a voluntary stop**), and `cancel` (hard-cancel used by the watchdog on a frozen run). |
| **`agent/runner.py`** | **Self-healing run engine.** `run_supervised_turn` is the single funnel every launch path uses so EVERY origin (web, automation, event, inbound, webhook) is **recorded** in `session_runs` (with `origin` + a `relaunch_ctx` recipe), **supervised** by the Run Manager, and **recoverable**. `resume_one` re-ignites an involuntary stop (`server_restart` / `zombie` / `frozen` / `crash`) at the turn boundary from a **bounded checkpoint** of durable history (`trim_history_for_resume`, not the whole transcript) — never a `user_stop` / `replaced` / throwaway / opted-out run — enforcing the retry budget, exponential backoff, and an atomic lease. `resume_orphans_at_boot` runs at startup; `manual_resume` backs the one-click resume for a run held by the opt-out. Per-origin **resume builders** rebuild a turn headlessly (the `web` builder, registered by `chat.py`, streams to the UI; a generic builder handles the background origins and re-delivers inbound/webhook replies). Delegation is covered for free (an in-session agent swap inside one supervised run; resume reads the session's *current* `agent_id`). |
| **`agent/watchdog.py`** | **Liveness watchdog** — a single asyncio sweep (mirrors `scheduler/local.py`) that heals runs which stop *while the process is alive*. Each tick runs **two sweeps**. **Sweep 1 — `running` rows:** classifies each via `RunManager.is_running` + the loop's `heartbeat_at` into **frozen** (live task, no heartbeat past `run_frozen_threshold_seconds` → cancel + resume), **zombie** (running row, no live task past `run_zombie_grace_seconds` → resume), or healthy (leave). **Sweep 2 — terminal `run_state_list_resumable` rows:** re-ignites runs that already ended with a resumable cause (`crash` / `server_restart` / `frozen` / `zombie`) but are sitting terminal — the same set `resume_orphans_at_boot` handles, now also caught at **runtime** so a recoverable stop comes back within a poll interval instead of waiting for the next reboot (the durable `next_resume_at` backoff gate rate-limits it; the retry budget retires it). Delegates all resume decisions to `runner.resume_one`. `get_watchdog()` / `start_watchdog()` / `stop_watchdog()` lifecycle from `main.py`. |
| **`agent/run_buffer.py`** | In-memory per-session `RunBuffer` registry for live WS replay — a low-latency cache in front of the DB (the source of truth). Stamps every emitted event with monotonic `session_seq` / `turn_id` / `turn_seq` / `emit_time`. Buffers persist `stream_buffer_retention_seconds` after a turn completes so a refresh/session-switch right after the turn ends still replays from RAM. Background sweeper drops stale buffers every 30s. Used by `app/api/chat.py` (`_emit_to_visualizers` stamps; `chat` / `chat_stream` call `start_turn` and `end_turn`) and `app/api/agent.py` (WS handshake + mid-session `resume`). |
| **`agent/diagnostics.py`** | **Diagnostic flight-recorder.** `DiagnosticRecorder` — two-tier (in-memory ring + durable `diagnostics` table in the **dedicated `logs.db`**, see `db/logs_store.py`) capture of server logs (via `DiagnosticLogHandler` on the root logger, **INFO by default** with a per-logger policy — our `app.*` at INFO+, noisy libs at WARNING+), agent-loop pipeline problems + tool calls (via `tap_loop_event`), run outcomes (`record_run_lifecycle`), **every HTTP request** (`record_access`, the `access` category) and **WebSocket lifecycle** (`record_ws_event`, the `ws` category). The loop tap also **pairs each `tool_call` with its `tool_result`** into the structured `tool_executions` table (duration/success). Every record carries correlation keys (`interaction_id` / `session_seq` / `turn_seq`) + `instance_id` so a log line joins to the exact `interactions` row by key. `record()` is non-blocking (RAM append + write queue); a background task batch-writes INFO+ rows + prunes. `query()` merges durable rows with the live ring. Started in `main.py` via `start_recorder()` + `install_log_handler()`. |
| **`agent/diag_investigate.py`** | **Opt-in auto-investigation.** `maybe_auto_investigate()` (called from `runner._finish_run`) hands an *involuntary* run failure (status `error` / `crash` / `zombie` / `frozen`) to the Diagnostic Agent — a fresh background session + failure brief launched via `run_supervised_turn`. **OFF by default** (`diagnostics_auto_investigate` in `app-settings.json`); guarded against recursion (never investigates a diagnostic run), storms (per-hour cap), voluntary stops, and non-admins. |
| **`api/diagnostics.py`** | **`GET /api/v1/diagnostics`** (admin) — filtered recent flight-recorder records (`levels`, `categories`, `session_id`, `agent_id`, `search`, `since_minutes`, `limit`, `include_ring`); **`GET /api/v1/diagnostics/stats`** — recorder health (enabled flag, ring/queue sizes, counts by level); **`DELETE /api/v1/diagnostics`** — clear records by scope (`older_than_minutes` / `levels` / `categories` / `search`, or no scope = all; `clear_files=true` also truncates the `logs/*.log` files on a full clear). Same admin gate as the DB viewer. Backs the Admin Tools → **Diagnostics** sub-page. |
| **`api/http_diag.py`** | **HTTP-error capture** for the flight-recorder. Registers exception handlers for `HTTPException` (all 4xx/5xx, including a deliberate `HTTPException(400, …)` FastAPI otherwise never logs) and `RequestValidationError` (422) that record `{status, method, path, cause, user}` into the recorder's **`http`** category (+ `logs/http.log`) then delegate to FastAPI's default handlers (responses unchanged). 500s are recorded from `main.py`'s global `Exception` handler via `record_server_error()`. Static-asset 404s are skipped as noise; toggle with `diagnostics_http_capture`. (Separately, an **access-log middleware** in `main.py` records *every* non-static request — method/path/status/duration — into the recorder's **`access`** category.) |
| **`plugins/abilities/Administrator/diagnostics.py`** | **`read_diagnostics`** agent tool — a **self-contained drop-in ability** (handler, helpers, and schema all live in this plugin file; no core factory). Reads the flight-recorder (server errors/loop problems/failed runs/tool errors, plus **`access`** = every HTTP request and **`ws`** = WebSocket lifecycle) as structured JSON, newest-first, with tracebacks + correlation keys (`interaction_id` / `session_seq` / `turn_id` / `instance_id`). **`view='tools'`** instead returns aggregated `tool_executions` metrics (per-tool call count, failure rate, avg/max duration, recent failures). Gated by the **diagnostics** ability; `tools/loader.py` discovers it generically (its `build_tools` injects the handler, heavy imports stay lazy). |
| **`agent/render_recorder.py`** | **Client render recorder** — the *browser-side* flight recorder (the blind spot `diagnostics` can't see). `RenderRecorder.ingest()` validates + queues batches shipped by `ui/chat-side-panel/js/recorder.js` (HTML snapshots, DOM-mutation summaries, lag/long-task metrics, JS errors, console warnings, failed/slow network calls); a background task batch-writes them to the durable **`render_recordings`** table in the **dedicated, always-local `recordings.db`** (own WAL — see `db/logs_store.py`) and prunes by row/age cap. Every row carries **`session_seq`** so a render moment joins exactly to the `interactions` / `diagnostics` row beside it. **OFF by default** (`render_recording_enabled`); `enabled`/`client_config()` re-read `app-settings.json` live so the toggle needs no restart. Started in `main.py` via `start_render_recorder()`. The store is local SQLite regardless of the main DB backend (logs stay per-instance even when the main DB is remote Postgres). |
| **`api/recordings.py`** | **`GET /api/v1/recordings/config`** (light auth) — tells the browser whether to record + capture knobs; **`POST /api/v1/recordings`** (light auth — the browser ships batches; user_id derived server-side from the token, never spoofed; no-op when disabled); **`GET /api/v1/recordings`** (admin) — filtered recent records (html omitted); **`GET /api/v1/recordings/one?id=`** (admin) — one record *with* its html; **`GET/POST /api/v1/recordings/enabled`** (admin) — read/flip the on/off flag (backs the Admin Tools **Render recorder** toggle + the TUI App Config switch); **`GET /api/v1/recordings/stats`** + **`DELETE /api/v1/recordings`** (admin). |
| **`agent/prompts.py`** | Assembles the system prompt from the resolved per-caller slot list plus brain context and attachments. Slot resolution (admin base + user overrides, lock + replace/append merge mode) lives in `app/db/local.py`. Includes **`format_attachments_for_prompt()`**, **`build_user_message_content()`** (inlines images for vision models), and **`describe_image_attachment()`** (single-call vision fallback for non-multimodal models) helpers. |
| **`agent/error_classifier.py`** | Structured tool errors (**used on the WebSocket / streaming path**). |
| **`plugins/engines/`** | **Alternate agent runtimes** (drop-in plugin tree, sibling of `plugins/abilities/`). A normal agent runs `app/agent/loop.py`; an agent whose record carries **`metadata.engine = "<id>"`** hands its WHOLE turn to the matching engine adapter here, which yields the loop's own event vocabulary and persists the same `interactions` rows — so the chat UI, streaming, and reload are unchanged. ONE generic dispatch in `stream_agent_events` looks the engine up by id (`get_engine_stream`, auto-discovered from `plugins/engines/__init__.py` — no per-engine `if`, no central list). Each engine is a **folder** (`plugins/engines/<id>/`) whose package `__init__` re-exports `ENGINE_ID` + `stream`. Ships **`plugins/engines/claude_code/`** — the **Local Claude Code** engine: drives the locally-installed `claude` CLI in headless `stream-json` mode (prompt fed on **stdin**, output read on a **Windows-safe worker thread**), starting in a configured **Working folder** that **defaults to the app's own project directory** (`app/util/paths.py:project_root()`) — the config card pre-fills it and the engine falls back to it when blank, so a fresh Claude agent points at *this* project out of the box; the admin can set any other folder. Resumes the Claude conversation mapped to the chat session (`sessions.metadata.claude_session_id`). Built from the **`local-claude`** template; **admin-only at runtime**; cost shown **informational** (Claude spends its own login, not webAgent credits). It is a **distinct agent type in the UI** — created from the single **"New Agent" tile** by picking the **Claude** segment of the create card's WebAgent / Claude / Terminal type chooser (admin-only segments; the lower area becomes an inline sign-in + settings form, finalised by the card's "+") and edited on its **own stripped, tab-less card** (no normal Config/Prompts/Abilities tabs), all in `ui/main-panel/agents/js/claude-agent.js`. Admins **sign in to Claude from the card itself** — a button + paste-a-code field, no terminal: **`app/api/claude_auth.py`** + **`plugins/engines/claude_code/claude_code_login.py`** drive `claude setup-token` in a real PTY (WinPty), capture the **long-lived token**, store it via the secrets backend, and the adapter injects it as **`CLAUDE_CODE_OAUTH_TOKEN`** every turn (so the agent survives the keychain's daily-expiring OAuth). Routes: **`POST /api/v1/claude-code/login/start`** (returns the authorize link), **`/login/submit`** (the pasted code), **`/login/cancel`**, **`GET /api/v1/claude-code/auth/status`**, **`POST /api/v1/claude-code/auth/signout`** — all admin-gated. The card is no longer fully tab-less: it carries a slim **Settings / Skills** tab bar (`mountClaudeCardTabs` in `claude-agent.js`). The **Skills tab** (`ui/main-panel/agents/js/claude-skills.js`) mirrors the normal agent's Prompts tab — a squares-carousel of the **SKILL.md files the local `claude` finds** (personal `~/.claude/skills` + the agent's working-folder `.claude/skills`), each editable (name / description / body) with a live **Markdown preview**. Routes **`GET/PUT/DELETE /api/v1/claude-code/skills`** (admin-gated; `app/api/claude_skills.py` → `plugins/engines/claude_code/claude_code_skills.py`, which resolves the working folder **server-side** from the agent record and is slug/path-traversal-guarded). |
| **`remote_access/`** | **Remote Access** subsystem. **`store.py`** = per-machine config (`remote_access.json`) + the multi-tenant signpost directory (`remote_access_pointers.json`, push-token bound on first use); **`netinfo.py`** = LAN address + QR; **`tunnels.py`** = ngrok / cloudflared supervisors + Tailscale / port-forward status; **`signpost.py`** = pushes the current address to the directory; **`manager.py`** = orchestrator + address-change watcher (lifecycle hooks `start_remote_access` / `stop_remote_access`). |
| **`admin/remote_access.py`** | **`/admin/remote-access/*`** (admin-only) — `status`, `method`, `config`, `start`, `stop`, `test`, `auto-start`, `regenerate-key`, `ngrok-authtoken`. Drives the Remote Access card. |
| **`api/remote_access.py`** | Public signpost — **`POST /api/v1/remote-access/pointer`** (PC reports its address, token-authed), **`GET /api/v1/remote-access/pointer/{key}`** (lookup), **`GET /go/{key}`** (the phone's permanent bookmark → redirect to the live address). |
| **`deploy/`** | **Deploy** subsystem — live one-click deploy of this app onto a cloud target (App Settings → Deploy). Each cloud is a **drop-in** under **`providers/<id>.py`** (a `BaseDeployProvider` instance exposed as `PROVIDER` + a `FEATURE` header), auto-discovered by **`registry.py`** — add a cloud by dropping one file (mirrors `app/scheduler/providers/`). **`bootstrap.py`** = the one shared install recipe every fresh-VM target injects (Python+Caddy+systemd on Ubuntu 24.04). **`credentials.py`** = the cloud key in the encrypted vault (admin scope), **auto-discarded after a successful deploy** ("Forget keys after deploy", default on). **`store.py`** = non-secret per-target settings + last-deployment record (`data/config/deploy.json`). **`manager.py`** = builds the catalog + runs the streaming deploy/tear-down, **plus** the instance-management orchestration (`manageable_providers` / `list_instances` / `run_instance_action`) and the Cloud VMs page's **sign-in** (`save_connection` writes the id → `deploy.json` + the key → the vault; `disconnect` forgets the key, or with `forget_config` also clears the saved id to fully remove the account) behind the **Cloud VMs** admin page. Ships **`google_vm`** (Compute Engine VM — no new dependency; drives the REST API with `httpx` + the service-account JWT, like the Google scheduler). A provider opts into the Cloud VMs page by setting **`supports_instances`** and implementing **`list_instances`** (every VM in the project) + **`instance_action`** (start/stop/delete, streaming) — optional methods on `BaseDeployProvider` with safe "not supported" defaults — and declares **`connect_config_keys`** (the non-secret "id" fields, e.g. the project, the page's sign-in panel collects alongside the secret key). The page is self-sufficient: it reuses one cloud key for both deploy and full lifecycle management, and when none is stored it shows its own sign-in panel rather than depending on the Deploy card. |
| **`api/deploy.py`** | **`/admin/deploy/*`** (admin-only) — `catalog`, `select`, `config`, `credentials`(+`/clear`), `test`, and the NDJSON-streaming `deploy` / `destroy`. Drives the Deploy card; resolves the caller via `resolve_admin_uid` (same chokepoint as Remote Access). |
| **`tunnel_link/`** | **Tunnel** subsystem (webAgent side of tunnel.webagent.live). **`store.py`** = relay registration config (`data/config/tunnel_link.json`: relay base URL + the instance_id/secret the relay issued + the Cloudflare tunnel address). Frontend panel **`ui/shared/js/tunnel-link.js`** (App Settings → Tunnel); the hosted relay it talks to is the separate **`webagent-tunnel`** project. |
| **`admin/tunnel_link.py`** | **`/admin/tunnel-link/*`** (admin-only) — `status`, `save` (register/update with the relay), `pairing-code` (mint a single-use code + QR SVG), `reset`. Calls the relay via httpx; the instance_secret never reaches the browser. |
| **`app/defaults/agents/`** | **Agent template JSON files** (bundled seeds; a `data/agents/` folder overrides them if present — resolved by `app/util/paths.agents_seed_dir`) — seed the `agent_templates` table (model/temperature/etc.) and the template's admin-base prompt slot rows in `agent_prompts`. A JSON file may declare slots explicitly via a `slots` array, or use the legacy flat keys (`system_prompt`, `agent_prompt`, `user_prompt`, `skills_prompt`, `tasks_prompt`, `misc_prompt`, `bootstrap_tools`) which the seeder converts into slots automatically. Bundled: `default.json` (the canonical full-schema reference), `opt_planner.json` + `opt_closer.json` (the optimizer pipeline agents), `server-manager.json`, and `local-claude.json` (the **Local Claude Code** agent type — its `metadata.engine="claude_code"` routes runs to `plugins/engines/` instead of the normal LLM loop). (Other agents — diagnostic, visualizer, etc. — are seeded from their plugins, not this folder.) |
| **`context/context_templates/`** | **Context template .md files** — seed `context_templates` table per context_type (agent, user, skills, tools, tasks, memory, project, jobs). Copied to user context on first chat. |
| **`agent/embed.py`** | Embedding utility using same provider config as chat. Returns configurable-dimension vectors (`EMBED_DIM`, default 1536). |
| **`db/__init__.py`** | **`get_db()`** → **`PostgresBackend`** (when `db_connection.json` / env selects a Postgres-family provider), else **`SupabaseBackend`** or **`LocalBackend`** from persisted mode. Honors `WEBAGENT_CONFIG_SOURCE=env` for Cloud Run (reads `WEBAGENT_DB_*` env vars instead of the JSON files). Falls back to SQLite if a configured Postgres is unreachable. |
| **`db/schema/`** | Canonical Python schema definitions (`tables.py`) + dialect renderers (`ddl_renderer.py`) producing SQLite / Postgres / MySQL CREATE TABLE statements from one source of truth. Used by the Storage modal's "Show Schema SQL" and "Auto-Create Tables" buttons. |
| **`db/logs_store.py`** | **Dedicated, always-local logs store** (`get_log_store()`). Two separate SQLite files with their own WAL + write lock, independent of the main DB: **`logs.db`** (`diagnostics` + `tool_executions`) and **`recordings.db`** (`render_recordings`). Splitting the high-volume log firehose out of the file holding `interactions` means a burst of log writes never stalls a user-facing write (SQLite = one writer per *file*), and logs stay **per-instance local even when the main DB is remote Postgres**. Stamps a persisted per-box `instance_id` on every record. Mirrors the `insert/query/prune/clear_*` method names the recorders already called, so wiring was a one-line `_db()` swap. See `app/db/README.md`. |
| **`db/connection_config.py`** | `DBConnectionConfig` dataclass — provider, host/port/database/username, ssl_mode, schema, supabase_url, password_secret_key. Persists to **`app/db_connection.json`** (env-locked in Cloud Run). Builds SQLAlchemy-style URLs for asyncpg / aiomysql. |
| **`db/postgres_backend.py`** | **Live Postgres backend.** `test_connection()` / `bootstrap_schema()` (asyncpg) for the Storage modal, plus **`PostgresBackend`** — a subclass of `LocalBackend` that overrides only the connection + schema bootstrap. Every data method is inherited and runs through the **`PgPortableConnection`** translation layer, so one codebase serves both SQLite and Postgres. On init it renders the Postgres schema (FK-ordered, pgvector, GIN full-text), reconciles any columns from a reference SQLite schema (migration-proof parity), and seeds. Uses a `psycopg` connection pool. |
| **`db/pg_portable.py`** | **SQLite→Postgres dialect translation layer.** A `sqlite3.Connection`-compatible facade over a pooled `psycopg` connection: converts `?`→`%s` placeholders, `INSERT OR IGNORE/REPLACE`→`ON CONFLICT`, `datetime('now')`→text-timestamp, `IFNULL`→`COALESCE`, `json_each(col)`→`json_array_elements_text(...)`, and returns rows that support both index and key access (sqlite3.Row parity). |
| **`db/migrate_sqlite_to_pg.py`** | One-time, idempotent **data copy** SQLite→Postgres (FK-dependency order, FK checks disabled during load, BLOB embeddings → pgvector, NUL-byte stripping, NULL-PK backfill). Run via `python -m app.db.migrate_sqlite_to_pg [sqlite_path]` with `WEBAGENT_DB_*` env vars set. |
| **`db/migration.py`** | Data migration — streams the current backend's tables as a JSON document (export) and bulk-inserts a previously-exported document into the active backend (import). Used by **`POST /admin/storage/migrate/{export,import}`**. |
| **`secrets/`** | `SecretsBackend` interface + impls: **InlineDBSecrets** (default; stores in `auth_elements.secret_ref` — which lives in the dedicated **`vault.db`**, see below), **EnvSecrets** (read-only `WEBAGENT_SECRET_*` env vars), **OSKeyringSecrets** (`keyring` package — Windows Credential Manager / macOS Keychain / Linux Secret Service), **GCPSecretManager**, **AWSSecretsManager**. Factory at **`app.secrets.get_secrets()`** mirrors `app.db.get_db()` pattern. Provider choice persisted to **`app/secrets_mode.json`** (env-locked in Cloud Run). |
| **`db/supabase.py`** | Cloud: **`sessions`**, **`interactions`**, **`context`**, **`context_templates`**, **`attachments`**, memories / tools / skills per shared schema. |
| **`db/local.py`** | Local SQLite — schema init, FTS5 + vector hybrid search, embed-on-write, **`user_profiles`** (tracks `created_at`, `last_login_at`, and a per-user theme override in **`appearance`** — a sparse JSON patch layered on the global theme by `GET /api/v1/auth/ui-config` when the admin enables `allow_user_appearance`; edited from the account page's "My appearance" panel via `GET/PUT/DELETE /api/v1/auth/me/appearance`, migration 031), **`webhook_registrations`** and **`webhook_event_log`** tables. **Credentials vault:** `auth_elements` (integration tokens + inline secrets) is kept OUT of the user-data DB — `_get_conn()` `ATTACH`es a sibling **`vault.db`** (`VAULT_SCHEMA`) and the table is created only there, so a user-data reset never wipes credentials and unqualified `auth_elements` queries resolve to the vault transparently (one-time migration relocates a legacy copy). See `app/db/README.md → Credentials vault`. **`agents`** table includes **`user_mode`** (`anonymous` \| `register`) — controls whether channel users stay anonymous or are guided through registration/account-linking across channels. Prompt content lives in **three tables**: **`agent_templates`** (per-template config), **`agent_prompt_templates`** (canonical slot defaults, versioned + `source = 'json' \| 'admin'`), **`agent_prompts`** (per-agent runtime rows: admin-base cloned from templates + per-user overrides). See [Prompt slots and overrides](#prompt-slots-and-overrides). |
| **`db/attachments/`** | **`file_store.py`** — file byte storage abstraction. Dispatches to local filesystem (`uploads/`) or Supabase Storage based on `db_mode.json`. Exports `store_file()`, `read_file()`, `delete_file()`. See `app/db/SUPABASE_STORAGE.md` for cloud setup. |
| **`db/interface.py`** | **`StorageBackend`** protocol with session, interaction (incl. `update_interaction` + `status`), context, memory, skills, agent, attachment, interrupt, **run-state (`run_state_*` + self-healing: `run_state_set_cause` / `_heartbeat` / `_list_active_all` / `_list_resumable` / `_claim_for_resume` / `_mark_failed` / `_mark_manual` / `mark_orphans_for_resume`)**, **webhook (register/get/list/delete/log)** methods. |
| **`tools/`** | **`loader`** (dynamic tool loading + built-in injection: register_webhook, list_webhooks, delete_webhook, get_webhook_log, **`list_event_sources` / `list_delivery_channels` / `event_subscribe` / `list_event_subscriptions` / `event_unsubscribe`** (real-time event triggers — gated by the **`automation`** ability; defaults to channel `webchat`+current session_id so the reply lands in the user's open chat), render_visual, plus **`delegate_to_agent` / `list_delegatable_agents`** for non-pipeline agents). **Ability-gated injections:** `web_search` / `get_weather` / `maps_geocode` (web_access), `browser_action` / `http_request` (browser_control), `generate_image` (image_generation), `db_query` + source-edit suite (codebase_admin), `create_tool` (create_tools), `read_diagnostics` (diagnostics), `list_agent_templates` / `list_my_agents` / `get_agent` / `create_agent` / `update_agent` / `edit_agent_prompt` / `set_agent_ability` (agent_management — self-contained in `plugins/abilities/Core/agent_management.py`), **`terminal_open` / `terminal_read` / `terminal_send` / `terminal_wait` / `terminal_list` / `terminal_close`** (terminal_control — drive interactive terminal programs; self-contained in `plugins/abilities/Administrator/terminal_control.py`). All of these ability tools now live in their own files under `plugins/abilities/` (each ability carries its `build_tools` handlers, `TOOL_SCHEMAS` and `DESTRUCTIVE`); `app/tools/` holds only **host infrastructure**. Host modules: **`core_tools`** (the always-on host tools only — memory, session_search, get_time, get_date, calculate, list_skills, load_skill; `web_search` / `get_weather` moved to web_access, `db_query` to codebase_admin, `http_request` to browser_control), **`loader`** / **`registry`** (create_tool, safety scanner, rating utilities) / **`tracker`** (`track_execution` → writes structured per-call metrics to the `tool_executions` table in `logs.db`; auto-filled by the diagnostics loop tap), **`tool_modes`** / **`tool_defaults`** (per-tool exposure modes + app-wide defaults), **`browser`** (persistent cross-session Playwright Chromium *driver* — host infra used by `app/main.py`/streaming/db; the `browser_control` ability wraps its `browser_action`), **`read_attachment`** (read uploaded files via `app/db/attachments/`), **`optimizer_tools`**. Self-contained ability tools that used to be `app/tools/` factory files now live under `plugins/abilities/` — `image_generation`, `app_control`, `agent_management`, `automation`, `agent_orchestration` (delegate_to_agent / list_delegatable_agents) under `Core/`; `diagnostics`, `terminal_control` under `Administrator/`; `wiki_context` under `Memory/`; `web_access` (web_search / get_weather / maps_geocode) under `Web/`. |
| **`visualizer/`** | **Multi-canvas workspace tools** — `render_visual`, `list_canvases`, `get_canvas`, `create_canvas`, `delete_canvas`, `rename_canvas`, plus the **data tools** `get_canvas_data` / `set_canvas_data`. Canvases stored per-user as one folder per canvas at `data/user_data/<user_id>/canvas/<slug>/index.html`, each with an optional `page.json` descriptor (title, agent_context, order) — no central manifest. **A canvas's content is kept separate from its markup** in an optional sibling `data.json` (a JSON object); the `/html` endpoint bakes it into the served page as `window.__CANVAS_DATA` so the page reads it via `api.getData()` instead of hardcoding records — the agent updates content with `set_canvas_data` (whole-replace or `merge=true`) without rewriting the page. Stored on disk for filesystem/hybrid backends, in the `canvases.data` column for the database backend. `canvas.py` is the canvas-CRUD facade (over `app/canvas_store/`); `tool.py` implements `render_visual`. The agent design/page-building guide lives with the ability at `plugins/abilities/Core/visualizer/visualizer.skill.md` (resolved at runtime as the Visualizer ability's skill; also read by the Supabase template seed). Self-contained — delete to disable. |
| **`models/schemas.py`** | Pydantic models (`ChatRequest`, etc.). |
| **`util/config_io.py`** | Shared, dependency-free JSON-config I/O used by every config writer. **`read_json`** (never raises — returns the supplied default on a missing/corrupt file), **`safe_write_json`** (creates the parent dir on demand + atomic temp-file-then-`os.replace`, so a save into an absent `data/config/` self-heals and a crash mid-write can't truncate a config), **`set_config_key`** (read-modify-write **only** the named key/nested path, preserving siblings, creating the file if absent). This is what lets the app ship with **no `data/` folder** — config files materialize lazily on first change. |
| **`util/paths.py`** | Resolves the read-only **seed** data that must ship even with no `data/` folder. **`app_prompts_path()`** and **`agents_seed_dir()`** each prefer a `data/` copy if one exists (a deployment override) and otherwise fall back to the bundled **`app/defaults/`** copy. Centralizes the location so the ~7 modules that read the app-prompts catalog don't hard-code a `data/` path. |
| **`defaults/`** | Bundled, always-tracked **read-only seed data** (ships even when `data/` is absent): `app-prompts.json` (the system/UI prompt catalog) and `agents/*.json` (the default + system agent templates). These are *not* runtime state — `data/` stays purely runtime, written lazily. Resolved by `util/paths.py`. |
| **`admin/`** | **`review`** (`/admin/tools` — list/deprecate DB tools), **`db_mode`** (`/admin/db/` — legacy cloud/local switch), **`storage`** (`/admin/storage/*` — provider dropdown, test/bootstrap/activate, secrets vault, data migration export/import), **`settings`** (provider config, model list, metadata toggle), **`integrations`** (`/admin/integrations` — OAuth integration management: configure and revoke Google, Microsoft, Yahoo, Dropbox credentials; `GET /admin/integrations` returns status for all four providers), **`guardrails`** (path/command deny-list for source tools), **`communications`** (multi-channel plugin mgmt: Telegram, Twilio SMS, Twilio WhatsApp, Discord, Slack — `GET /admin/communications/plugins`, `POST /admin/communications/plugins/{name}/credentials`, `POST /admin/communications/plugins/{name}/enable|disable|token`), **`source`** + **`source_tools`** (optional privileged filesystem & shell access — delete to disable), **`users.py`** (`GET /admin/users`, `POST /admin/users/{user_id}/set-admin` — admin user management). See [Administrator Tools](#administrator-tools). |
| **`api/oauth.py`** | OAuth callbacks for all supported providers. **`GET /api/v1/oauth/callback/{provider}`** — providers: `google`, `microsoft`, `yahoo`, `dropbox`, `meta` (Facebook+Instagram), `twitter`, `linkedin`, `tiktok`, `pinterest`, `reddit`, `snapchat`, `twitch`, `ebay`, `etsy`, `shopify`, `amazon`. All callbacks share one driver (`_run_callback`) running the common flow — exchange the authorization code for tokens, store credentials in `auth_elements`, signal the opener popup, close the window — with each provider supplying only its token-exchange + profile closures (Shopify is bespoke for its per-shop host). Twitter, TikTok and Etsy use PKCE; Meta stores under `service="meta"` and aliases to `facebook` and `instagram`. |
| **`api/data_sources.py`** | **`GET/POST /api/v1/data-sources`**, **`GET/PUT/DELETE /api/v1/data-sources/{id}`**, **`POST /api/v1/data-sources/{id}/test`**, **`POST /api/v1/data-sources/{id}/introspect`**, **`POST /api/v1/data-sources/{id}/ingest`** (doc_store), **`GET /api/v1/data-sources/types`** — per-user external data source registry. **Attachments:** **`GET /api/v1/agents/{agent_id}/data-sources`**, **`POST /api/v1/agents/{agent_id}/data-sources`** (attach), **`PUT/DELETE /api/v1/agents/{agent_id}/data-sources/{ds_id}`** — per-agent attachment management. Each attached source contributes a synthetic tool at agent load time (`app/tools/loader.py`) and a snippet to the system prompt's **`[DATA SOURCES]`** block (`app/agent/prompts.py`). Connector implementations live in **`app/connectors/`** — v1: `sql_postgres`, `doc_store`, `web_search_domain`. |
| **`automation/`** | Agent automation file parsing + sync. **`parser.py`** calls the configured LLM to convert the free-form `automation` slot into a JSON **object** with two arrays: `tasks` (cron-style, validated via `croniter`) and `event_subscriptions` (push/poll triggers, validated against the live event-source manifest). **`sync.py`** hashes each parsed entry, upserts/deletes rows in **`agent_automations`** (cron) and **`agent_event_subscriptions`** (events) for the `(agent_id, owner_user_id)` pair, recomputes `next_run_at` for cron rows, and for newly-added event subs calls the source plugin's `register_subscription` to wire up the provider-side watch (and `unregister_subscription` for removed rows). Notifies the scheduler. Called from `update_my_prompts` whenever the `automation` slot is saved. The chat agent can also create event subs directly via the built-in **`event_subscribe`** tool (see `app/tools/loader.py`), which reuses the same `_event_sub_hash` + `_register_event_sub` helpers from `sync.py`. |
| **`scheduler/`** | Pluggable scheduler backends behind a `SchedulerBackend` ABC. **`local.py`** = in-process asyncio loop polling `agent_automations` every 30s. **`remote_base.py`** = `BaseRemoteScheduler` shared sync_tasks loop that generates per-row `fire_token`, builds the public webhook URL, and delegates `_push_job` / `_cancel_job` / `test_connection` to provider subclasses. **`providers/`** = registry + concrete providers: **`google_cloud.py`** (Cloud Scheduler REST + RS256 service-account JWT), **`cronjob_org.py`** (cron-job.org REST), **`generic_webhook.py`** (POSTs job specs to an admin-supplied URL). **`executor.py`** creates a fresh session and runs the task's prompt through **`runner.run_supervised_turn` (origin `automation`)** so the run is recorded + supervised + self-healing-recoverable, dispatches via the channel plugin or runs silently, then updates `last_run_at` / `last_status` / `next_run_at`. **`__init__.py`** exposes `get_scheduler()` / `start_scheduler()` / `stop_scheduler()` / `reset_scheduler()` (mirrors `app.db.get_db`). Provider chosen via `scheduler_config.json` (see **`app/admin/scheduler_config.py`** for the admin endpoints + dynamic-form field schemas). |
| **`connectors/`** | Per-type external data source implementations. Each module defines a `Connector` subclass implementing `test_connection`, `introspect`, `generated_tools`, `prompt_snippet`, `safety_validate`. Registry in **`app/connectors/__init__.py`**. SQL connectors enforce a statement-type allowlist and optional `allowed_tables` set, parse queries via `sqlglot` (when installed), and inject a default `LIMIT`. Document-store connector chunks + embeds files via `app/agent/embed.py` into the **`doc_chunks`** table. Web-search-domain connector wraps the existing web-search tool with hard server-side `site:<domain>` filtering. |
| **`api/agents.py`** | **`GET /api/v1/agents/templates`** — list all agent templates. **`POST /api/v1/agents/templates`** — create a template (admin only). **`PUT /api/v1/agents/templates/{id}`** — update a template (admin only). **`DELETE /api/v1/agents/templates/{id}`** — delete a template (admin only). **`POST /api/v1/agents/{agent_id}/save-as-template`** (admin only) — snapshot a custom agent's config + admin-base prompt slots into a new `agent_templates` row plus `agent_prompt_templates` rows (`source='admin'` so JSON re-seed will not overwrite). Body: `{user_id, template_id, name, description?, icon?, discoverable?, access_level?}`. Surfaced in the UI as the **Save as Template** button at the bottom of a custom agent's Config tab (admin-only). Config fields themselves **auto-save per field** (each flashes a small green check on success) — there is no manual "Save Changes" button. **`GET /api/v1/agents/my-agent`** — get the current user's active agent. **`POST /api/v1/agents/set-default`** — set the user's default agent template. **Agent connections** — `GET /api/v1/agents/{id}/connections` returns all connections + `user_role` (`"admin"` or `"member"`). `PUT` requires agent-admin (global admin or in `admin_users`). The agent OAuth connect flow runs through the `check_oauth_connection` tool, which builds a provider authorize URL (via `app/admin/integrations.build_<provider>_authorize_url`) that lands on `GET /api/v1/oauth/callback/{provider}`; token revocation is handled by the table-driven `app/admin/integrations.revoke_and_delete(provider, …)`. **Agent members** — `GET /api/v1/agents/{id}/members?user_id=...` (agent-admin only) returns the agent's `admins` and `members` lists joined with profile + per-agent session/interaction counts, plus the agent's current `user_mode` and per-member `is_authorized` flag. **Access policy** — `POST /api/v1/agents/{id}/user-mode` (agent-admin only; body `{user_id, user_mode}`) sets the policy to `anonymous` (default — anyone with the link can chat), `register` (must have a registered account), or `authorized` (registered + must be in `authorized_users`). `POST /api/v1/agents/{id}/members/{target}/authorize` and `/restrict` add/remove the target from `authorized_users`. The chat endpoints enforce the policy at the start of each request; `/anon-session` is refused for `register` and `authorized` agents. Powers the **Members** tab on the agent card (admin-only UI) — top-level policy radio + per-row Authorize/Restrict buttons. **Row ordering** — `POST /api/v1/agents/reorder` (body `{user_id, order: [agent_id, …]}`) writes `agents.sort_order` by index for the agents the caller administers; `list_agents_for_user` returns customs ordered by `sort_order` (NULLS LAST, then `created_at`), so the Agents page (drag-to-reorder) renders agents in that synced order. **Ability config (app-level / admin scope)** — `GET /api/v1/abilities/{id}/config-schema` returns the ability's settings schema (the descriptor's `config.settings`); `GET /api/v1/abilities/{id}/config` and `PUT /api/v1/abilities/{id}/config` (both admin-only) read/write the NON-secret app-level config knobs, persisted to `data/config/agent-abilities.json` under `ability_config.{id}` via `app/admin/ability_config.py` (`get`/`set_ability_config`); both call `ensure_bootstrapped` so the file auto-seeds/self-heals if missing. Secrets stay in the vault via `GET|POST|DELETE /api/v1/abilities/{id}/credentials`. (Per-*agent* ability config/visibility/access lives on the `/api/v1/agents/{agent_id}/abilities/{ability_id}*` routes.) **Per-ability caller access ("Available to")** — `PUT /api/v1/agents/{agent_id}/abilities/{ability_id}/access` (agent-admin only; body `{user_id, level}`) sets which caller may trigger that ability when chatting the agent: `everyone` (anyone incl. anonymous guests — the default), `registered` (signed-in accounts only), or `admin` (admins only). Stored per-agent in `agents.metadata.ability_access` (no migration; absent = everyone) via `db.set_agent_ability_access`. **Enforced server-side at tool-assembly time** — `app/tools/loader.load_tools(gate_caller_access=True)` (the chat/run paths) drops a gated ability through `app/agent/ability_access.filter_abilities_for_caller` so its tools are never materialized for a caller below the level, a real boundary rather than a prompt hint; the matching ability skill is also stripped from the prompt by `app/agent/prompts.append_skills_section`. Config/preview endpoints leave the gate off so they always show the agent's full configured set. This is the finer-grained, per-ability sibling of the agent-wide `user-mode` access policy. Surfaced as the **Available to** dropdown beside each enabled ability on the per-agent **Abilities** tab (`agent-ability-table.js`). **The app-level store is the app-wide DEFAULT** the agent runtime layers per-agent settings on top of: `ability_config.effective_ability_config(id, per_agent)` merges admin defaults UNDER the agent's own choices (mirrors the global tool-permission defaults), consumed by `app/agent/context_control.py get_context_settings` (compaction knobs) and `app/agent/ability_skills.py` (declarative `config.bias` posture). |
| **`openai_compat.py`** | OpenAI-compatible client wiring for OpenRouter. |

### Frontend (`ui/`)

Single-page app: **`index.html`** + a service worker (`sw.js`). The frontend is organised **by location, not file-type** — each page/view is a self-contained drop-in folder. Top-level areas:

- **`ui/shared/`** — app-wide CSS + JS. CSS in `shared/css/` (`design-system.css` = the palette/theme single source of truth, plus `app1.css`, `app3.css`, `index.css`). Shared JS in `shared/js/`: **`main.js`** (boot orchestrator), **`state.js`** (global `app` object), **`config.js`** (URL/WebSocket helpers), **`agentWs.js`** (per-user receive-only WebSocket subscriber; exports `registerSessionSubscriber(sessionId, handler)` so a chat widget can claim a session's live events), **`chat-activity.js`** (pill "thinking" ticker), **`tabs.js`**, **`toolLog.js`**, **`terminal.js`** (`createTerminalInstance()` xterm factory), **`files.js`** / **`files-git.js`** (Admin Tools file editor + Source Control), the **Database viewer** cluster (`index.js` + `query-render.js` / `tables.js` / `columns.js` / `edit.js` / `delete.js` / `pagination.js` / `modal.js`), the two sister **ability tables** (`admin-ability-table.js` / `agent-ability-table.js`), **`dom-utils.js`** (shared helpers), `attachments.js`, `diagnostics.js`, etc.
- **`ui/chat-side-panel/`** — the chat panel: `chat-side-panel.html` + `js/` split into `chat-*` (bubble / stream / send / ui / virtual-scroll) and `session-*` modules (lifecycle, list, loader, agent picker). See `ui/chat-side-panel/js/README.md`.
- **`ui/main-panel/<page>/`** — the tabbed main-panel pages, each a drop-in folder: `agents/` (management + the `agent-loop/` pipeline & runtime-loop visualizers), `canvas/` (`canvas.js` page builder + swappable `canvas-toolbar.js` footer), `browser/` (in-app AI browser), `wiki/`, `sessions/`, `automations/`, `admin-tools/` (the launcher).
- **`ui/admin-tools/<view>/`** — Admin Tools views, each a drop-in folder with `page.json`: `app-config/`, `database/`, `diagnostics/`, `git/`, `interactions/`, `runtime-loop/`, `terminal/`, `explorer/`, `update/`, `cloud-vms/`. The **`update/`** view (**Update / Selective Sync**) carries its own drop-in backend `server.py` (mounted via its `page.json` `"router"` field, no `app/main.py` edit) exposing **`/api/v1/update/{status,config,init,fetch,diff,apply}`** (admin-only, reusing `app/api/github.py` auth helpers). It pulls changes from a configured public **upstream** repo for only the folders the operator ticks, 3-way merging each file in place so local customizations survive; conflicts are written with standard `<<<<<<<` markers and can be handed to the web agent to resolve. Config persists to `data/config/update_sync.json`; pre-merge backups go to `.update-backups/` (both gitignored). The **`cloud-vms/`** view (**Cloud VMs**) is a full manager for every VM in the admin's cloud account — list / Open in browser / Start / Stop / Delete — the evolution of the Deploy card, which only remembers the single last server it created. It carries its own drop-in backend `server.py` exposing **`/admin/cloud-vms/{providers,instances,instance/action,connect,disconnect}`** (admin-only via `resolve_admin_uid`, same as `app/api/deploy.py`; `instance/action` streams NDJSON). It reuses the **deploy** subsystem entirely — the cloud key (vault) + saved project/zone (`deploy.json`) — so it lists nothing of its own; a cloud takes part by setting `supports_instances` on its deploy provider (today: `google_vm`, via Compute `aggregatedList` + per-instance start/stop/delete). The **sidebar is a multi-account manager**: a provider dropdown + a **＋** button add a cloud account (every provider that declares `supports_instances` shows up in the dropdown automatically), and each added account is a row that expands into its own **login sub-menu** — a "login"-style form collecting that account's **id** (the provider's `connect_config_keys`, e.g. the project) + secret **key**, with **Save** / **Sign out** (forget the key, keep the id) / **Remove** (clear both, drop the account). The **main pane combines every server across all added accounts in one view, grouped per account**; per-server Open / Start / Stop / Delete act on the cloud the row belongs to (the action carries its provider). `connect` saves the id to `deploy.json` and the key to the same vault entry the Deploy card uses (a blank key keeps the stored one); `disconnect` forgets the key (`forget_config` also clears the saved id). So even though the Deploy card auto-forgets the key after a deploy, the account stays listed and the operator just re-enters the key in its sub-menu.
- **`ui/background/<id>/`** (drop-in animated backgrounds), **`ui/splash/`** (splash overlay), **`ui/chat-widget/`** (floating mini chat — `createChatWidget()` runs a task-scoped session in its own card; see `ui/chat-widget/README.md`), **`ui/tutorials/`** (in-app tour).
- **Standalone pages served at the `ui/` root:** `login.html`, `setup.html`, `privacy.html`, `tos.html`, **`test_interface.html`** (minimal dependency-free chat, served at **`GET /test`**). A **Terminal** header tab (`#tab-terminal`, wired in `tabs.js`) embeds the self-contained **`app/web-terminal/`** page in an `<iframe>` — see the **Terminal** feature above. **`diagnostics.html`** (repo `ui/diagnostics.html`, served at **`/ui/diagnostics.html`**) is a **standalone** copy of the Diagnostics sub-page — one self-contained file (inlined design-system tokens + a classic script, no partial-loader, no admin-tools shell, no ES-module boot) that renders the same `/api/v1/diagnostics` feed. Because it depends on nothing else, it keeps working when the main app's front-end boot is broken (the API just has to be alive); it reuses the app's `auth_token` same-origin, and can be opened from disk (`file://`) against a remote server via its sidebar **Connection** panel or `?server=…&token=…`. It shares the app's `webagent_theme` key (dark/light) and falls back to a honest "server unreachable" state instead of hanging.

**Boot sequence.** A pre-paint inline script in `index.html` applies the saved theme + panel visibility before first paint, then a full-screen **boot curtain** (`#boot-curtain`) covers the whole app while it assembles — so the user never sees the bare shell, partials injecting, the theme settle, or the splash fade in; the curtain lifts only when the page is already in its intended view. It is the first child of `<body>` (critical CSS in `<head>`), so it paints on the first frame as an opaque, theme-coloured (`var(--bg-0)` — deep near-black in dark mode) overlay with a centered branded loading animation, at a z-index above the splash. A small header spinner (`body.is-booting`, removed by `ui/shared/js/tabs.js` once the saved tab mounts) still runs behind it. The curtain is lifted by `window.__boot` (defined inline near the top of `<body>`): `appReady()` fades it once the app shell + saved tab are mounted **when no splash is showing**; if the splash plugin claims it (`splashWillShow()`), the curtain instead stays opaque until the splash has fully faded in and then hands off (`splashReady()` → black → splash, no shell flash). `reveal()` is forced on the server-down path and by a 10s safety net so boot is never trapped on the curtain. Two settings under **App Config → App Settings**, persisted per browser in `localStorage`:
- `bootAnimation` (values: `chat-slide-right` (default), `chat-slide-left`, `no-animation`) — picks the reveal transition.
- `bootMobileMode` (values: `chat-first` (default), `memory`) — mobile only. `chat-first` always runs the chat-first boot then animates to the saved view; `memory` skips the boot animation entirely when the user last had a Page open (saved `chatPanelVisibleMobile=false`) and restores that page directly.
- `pagesCatalogCache` (localStorage, written by `header-build.js`) — a cached copy of the page catalog used **only** to pre-paint the header tab strip synchronously before first paint (no flash for returning users). It is NOT the source of truth — the order/names/icons/visibility of pages are an **admin preference stored server-side** (see **Pluggable pages** below). The cache is refreshed from `GET /api/v1/pages/catalog` on every load.

### Directory tree (abbreviated)

```
webAgent/
├── app/                    # Python package (see table above)
│   ├── visualizer/         # Canvas page tools (render_visual etc.; design skill lives in plugins/abilities/Core/visualizer/). Vault Capture (request_credential/list_vault_keys + canvas.py /vault/* proxy) is bundled here — see app/abilities/vault_store.py
│   ├── automation/         # Automation parser/sync + shared run engine (runner.py) & unified delivery resolver (delivery.py)
│   ├── scheduler/          # Scheduler backends: local (in-process) + google (stub) + executor
│   ├── events/             # Event-trigger subsystem: router, poller, renewer, sources/, providers/
│   ├── integrations/       # OAuth-backed integration tools (Gmail, Calendar, Drive, …); one subdir per provider — delete to remove
│   ├── web-terminal/       # Self-contained terminal-styled chat page (Terminal tab); React+Babel single-file app, served at /web-terminal/ (see its README.md)
│   └── db/                 # DB *code* (logic): local.py, logs_store.py, postgres_backend.py, schema/ — the DB *files* live in data/db/
│       ├── logs_store.py   # Dedicated, always-local logs store → data/db/logs.db + recordings.db (own WAL)
│       └── attachments/    # File storage abstraction (store_file / read_file / delete_file)
├── plugins/                # Root home for DROP-IN capability files (core stays in app/; only the swappable leaves live here)
│   └── abilities/          # One FEATURE file per host ability (tools it gates + UI metadata); read by app/abilities/, the loader map, app/api/agents.py, and both ability panels via GET /api/v1/abilities/catalog. Copy _TEMPLATE.py to add one
│                           # NOTE: UI PAGES use the same drop-in pattern but live under ui/ (not here): each ui/<page>/page.json
│                           #   descriptor is discovered by app/ui_pages/, served via GET /api/v1/pages/catalog, and admin
│                           #   order/name/icon/visibility overrides persist to data/config/{main,admin}-panel-pages.json.
│                           #   A page folder is FULLY self-contained: page.json `css` auto-injects its stylesheets (no
│                           #   index.html <link> edit) and an optional `server.py` (page.json `router`) is auto-mounted as
│                           #   its backend API (no main.py edit). Drop the folder in / delete it → the page + its API + its
│                           #   CSS appear / vanish with ZERO edits elsewhere. (.py under ui/ is never served to the browser.)
├── data/                   # All non-code data: seed templates, app config, and runtime artifacts (keeps the root tidy)
│   ├── agents/             # Agent template JSON — seeded into agent_templates (reverse-export: scripts/export_agent_templates.py)
│   ├── config/             # App config JSON: provider.json, app-settings.json, debug-config.json (consolidated debug knobs), scheduler_config.json, optimizer.json, remote_access.json, deploy.json (Deploy panel targets — cloud keys are in the vault, not here),
│   │                       #   agent-abilities.json (ability toggles/order), main-panel-pages.json + admin-panel-pages.json (page order/name/icon/visibility).
│   │                       #   Page visibility is 3-state — "all" (incl. anonymous) / "auth" (signed-in, the DEFAULT so a fresh deploy exposes no tab to anon) / "off"
│   │                       #   (admins only); the public splash defaults to "all". Enforced at the NAVIGATION layer for EVERY page — header-build.js hides the tab AND exposes __pageHiddenByVisibility(), which tabs.js _pageVisibilityBlocked() consults so a gated page can't be reached via ?tab=, middle-click, a restored lastActiveTab, the mobile dropdown, or an agent ui_command. Rather than redirect, the page area shows an inline sign-in form (ui/shared/js/page-access-gate.js) right where the visitor tried to go; the page is never mounted and the check re-runs on the post-sign-in reload (Admin Tools keeps its own redirect + Restricted treatment for the boot race). See app/ui_pages._default_visibility + app/auth/identity.user_may_access_page.
│   │                       #   Enforced SERVER-side (defence-in-depth beyond ownership) for: Canvas (runs agent-authored code), Browser (/proxy + ws, SSRF surface), and Automations (caller_may_access_page in ui/main-panel/automations/server.py).
│   │                       #   Per-user page endpoints (Sessions/Browser/Automations/Agents) also verify the REAL caller (app/auth/identity.assert_caller_is — accepts Authorization header OR ?token=, open-mode tokenless) and scope every read/write to rows the caller owns. Agents/Sessions reads stay un-page-gated server-side on purpose: they're shared with the always-on chat (agent picker, session list), so the page setting governs them at the nav layer while ownership remains the data boundary.
│   ├── db/                 # Runtime databases (gitignored): local.db (user data), logs.db, recordings.db, vault.db (credentials), optimizer scratch
│   ├── screenshots/        # Browser-tool screenshots — legacy/fallback pile only (mounted at /screenshots; gitignored)
│   └── user_data/          # Per-user home (gitignored), mounted at /user_data (auth-gated):
│                           #   data/user_data/<user_id>/{uploads,visuals,canvas,screenshots,files}
│                           #   — inbound attachments, generated images, Canvas bodies (canvas/<slug>/index.html),
│                           #   browser screenshots, and general agent outputs. Written via the
│                           #   attachment store, image-gen, pages store, and the User Files ability.
│                           #   (Old data/uploads & data/visuals are retired — no longer written.)
├── docs/                   # Operator docs (events-setup.md, remote-access-setup.md) + claude/ agent guides
├── tests/                  # e.g. test_session_history.py (unittest)
├── sw.js                   # PWA service worker (must be at root scope for coverage)
├── ui/                     # Static UI + test_interface.html
│   ├── manifest.json       # PWA web app manifest
│   ├── icons/              # PWA app icons (192×192, 512×512, maskable variants)
│   ├── background/         # DROP-IN animated backgrounds. _engine/ owns the <canvas> + swaps the chosen one; each
│   │                       #   ui/background/<id>/ (json+js+css) is one self-contained background (stargaze, bullet-grid,
│   │                       #   aurora, particles). Catalog scanned by app/ui_backgrounds/ → GET /admin/settings/backgrounds;
│   │                       #   admin picks one per theme (App Settings → Appearance), applied app-wide via the public
│   │                       #   GET /api/v1/auth/ui-config. Drop a folder in / delete it → it appears / vanishes everywhere.
│   └── generate_icons.py   # Script to re-generate PWA icons
├── scripts/
│   ├── start_webAgent.sh            # Unix: cd to repo root, background uvicorn (default :8080, PORT= overrides)
│   ├── backfill_embeddings.py       # One-off: embed existing memory pages
│   ├── reset_memory.py              # One-off: clear the memory engine only (memories + chunks); backend-aware
│   ├── seed_tools.py                # Optional tool DB seeding
│   └── export_agent_templates.py    # Reverse-seed: DB → app/defaults/agents/*.json
├── export_agent_templates.bat  # Double-click: runs export_agent_templates.py (--dry-run supported)
├── migrations/             # Ad-hoc SQL snapshots (includes 007_channel_identities, 008_linking_codes, 009_multi_agent_system, 010_agent_name_backfill, 011_add_login_tracking, 014_data_sources, 018_agent_prompt_templates); see migrations/README.md
├── supabase/migrations/    # e.g. 005_memory_system.sql (Supabase CLI / team workflow)
├── temp/                   # Scratch files incl. Markdown drafts (see agent.md); roadmap: temp/FUTURE_PLANS.md
├── kill_webagent.bat       # Windows: show + kill all webAgent server processes on port 8080
├── reset_webagent.bat      # Windows: guided clean-slate reset (DB, visual pages, optional auth/secrets/templates); see Maintenance
├── webAgent.bat            # Windows: uvicorn loop + restart support (uses run.py)
├── webagent.exe            # Built launcher + self-bootstrapping installer (gitignored). Produced at the project ROOT by launcher/scripts/build_exe.py.
├── launcher/               # Polished Textual TUI launcher + self-bootstrapping installer; builds to a single portable webagent.exe at the PROJECT ROOT (../webagent.exe). FIRST RUN can install webAgent from nothing — downloads the public repo (git if present, else ZIP), fetches the self-contained uv toolchain (which installs its own private Python), uv-syncs all deps, and downloads the Playwright Chromium browser (~150MB) for browser_action; the end user needs only the .exe (or it can point at an existing folder). Server controls (Launch / Restart / Stop / Browser / Clear DB / Reset Python / Full Reset / Update = re-pull code + re-sync deps) + watchdogs that relaunch the server if it crashes (auto_restart_server) OR goes unresponsive on a health probe (health_check_restart) — shared exponential backoff + 5-strike crash-loop guard; both default ON, toggle in launcher.json + animated procedural ASCII background, AND an embedded keyboard-driven chat client (default view): agent/session scrolling pickers, live token streaming, expandable tool-call blocks, per-turn token/cost stats, image drag-to-attach, talks to the local server's chat/stream + db APIs (can drive the admin coding agent — shell/grep/file-edit). Emoji when the terminal supports it (auto-relaunches into Windows Terminal). See launcher/README.md.
├── launcher_android/       # Termux + proot-distro Ubuntu TUI launcher for running webAgent on an Android phone. Launch / Restart / Kill / Browser + dependency Doctor with one-tap fixes for the common Android-ARM issues (Playwright Chromium, build-essential, missing .env). See launcher_android/README.md.
├── clients/                # Standalone client apps that talk to the server. `browser-extension/` = the Chrome MV3 **Browser Connector** — the connector backend's client half, letting the server drive the user's own browser (see clients/browser-extension/README.md + app/api/connector_ws.py).
├── TUI/                    # Standalone server-manager TUI (Textual; inner package `webagent`). `/termux` serves TUI/install-termux.sh. See TUI/README.md.
├── deploy/                 # On-device scripts the in-app Deploy panel hands off to. `termux-setup.sh` = the full-server install for the "Linux / Termux" deploy target: the target generates a one-line command that installs git (pkg/apt/dnf/pacman), clones the repo and runs this script, which DETECTS Termux vs a plain Linux box — on Termux it builds the venv inside an Ubuntu proot-distro (+ wake-lock, via start_server_termux.sh); on Linux it installs natively (system packages + venv + a systemd service `webagent` when systemd is available, else a nohup keep-alive loop via start_server_linux.sh). Both SURVIVE A REBOOT — systemd on Linux (or a `@reboot` cron entry as fallback); a Termux:Boot script (`~/.termux/boot/`) on the phone, which fires once the free Termux:Boot add-on is installed. Both install req_no_playwright.txt, port 8080, AND the standalone Server Manager TUI (the `webagent` command) alongside the server — Termux reuses TUI/install-termux.sh, Linux uses a dedicated TUI/.venv + a /usr/local/bin/webagent launcher; both carry WEBAGENT_TUI_NO_SUPERVISE=1 so the manager never spins up a second server. See docs/claude/deployment.md.
├── start_server_termux.sh  # Launch + keep-alive loop for webAgent inside a Termux proot-distro Ubuntu (nohup run.py + restart-if-it-dies). Called by deploy/termux-setup.sh.
├── deploy/start_server_linux.sh  # Native-Linux counterpart of start_server_termux.sh (nohup run.py + restart loop). Used on Linux boxes WITHOUT systemd; deploy/termux-setup.sh calls it for first launch + adds a @reboot cron entry pointing at it.
├── run.py                  # Single-instance launcher: kills any LIVE :8080 server (take-over) and waits until it's gone, then binds. If a force-killed predecessor left lingering sockets (a DEAD pid still owning the port — Windows WinError 10048), it confirms no live owner and reclaims the address with SO_REUSEADDR so startup never stalls on a stuck port; a plain bind otherwise keeps two live servers from stacking
├── Dockerfile
├── docker-compose.postgres.yml  # Local Postgres+pgvector for dev (port 5433)
├── requirements.txt
├── pyproject.toml
├── uv.lock
├── .env.example
├── .fallowrc.jsonc         # `npx fallow` config (dead-code/cycles/dupes): real JS entry points + ignore rules so runtime-loaded UI isn't false-flagged
├── agent.md                # Instructions for assistants working in this repo
└── README.md
```

## Environment variables

Copy **`.env.example`** to **`.env`**:

```bash
cp .env.example .env          # Windows (cmd): copy .env.example .env
```

| Variable | Purpose |
|----------|---------|
| `OPENROUTER_API_KEY` | OpenRouter API key |
| `OPENROUTER_MODEL` | Model id (default in `.env.example`: `deepseek/deepseek-v4-flash`) |
| `AGENT_MAX_IDENTICAL_TOOL_CALLS` | Stall guard: how many times the agent may call the **same tool with identical arguments** before the loop is blocked and the agent is told to change approach (default `3`, min `2`). |
| `AGENT_MAX_STALL_STRIKES` | Stall guard: how many blocked-loop strikes accumulate before the agent stops cleanly with a "I was repeating myself" message (default `4`). |
| `AGENT_MAX_WALL_SECONDS` | Stall guard / backstop: wall-clock cap on a whole agent response (all turns); on exceed the loop ends **gracefully** with a message instead of running until the request times out. This is the real safety net when an agent's turn count is **unlimited** (`0`). Default `600`; per-agent override via the agent's `max_wall_seconds` field; set `0` to disable. |
| `AGENT_RUN_HEARTBEAT_INTERVAL` | Self-healing: how often (seconds) the live loop advances `session_runs.heartbeat_at` to prove liveness for the watchdog (default `5`). |
| `AGENT_STREAM_OPEN_SECONDS` | Self-healing: max seconds to wait for the LLM to **return the response-stream object** (the initial connect/open) before the turn is treated as stalled (default `120`, `0` disables). Bounds the open call — which is otherwise protected only by the client's own request timeout (observed to silently *not* fire, freezing a turn indefinitely with no tokens/error/heartbeat). On timeout the turn aborts as a **resumable `crash`**, the same way the per-chunk stall guard handles a mid-stream stall. |
| `AGENT_STREAM_STALL_SECONDS` | Self-healing: max seconds to wait for the **next streaming token** once the stream is open before treating it as stalled and aborting to a resumable `crash` (default `45`, `0` disables). Complements `AGENT_STREAM_OPEN_SECONDS` (which covers the open) — together they bound both phases of every LLM call. |
| `WEBAGENT_SPAWN_CONCURRENCY` | Agent Orchestration ability — how many **forked** spawned-helper turns may execute their (heavy) agent loop **at once** (default `3`, min `1`). Agents may request/fork unlimited helpers (and helpers may spawn their own, recursively); excess sit `queued` in a run-queue and start as slots free up. This bounds concurrent loopback agent loops so a wide/deep fan-out can't exhaust memory and hard-kill the process. Blocking (`wait=true`) spawns run inline and bypass this. |
| `WEBAGENT_SPAWN_HEARTBEAT` | Agent Orchestration — how often (seconds) a live spawn runner refreshes its `agent_spawns.heartbeat_at` to prove liveness (default `20`, min `5`). The self-healing sweep treats a spawn with no heartbeat for ~180s as dead and **fails it (never auto-re-runs it)** — re-running would re-execute everything the spawn already did, *including re-spawning the sub-helpers it created*, snowballing one restart into an exploding fan-out; instead the orchestrator is re-woken to re-spawn deliberately if it still needs the work. Keep this well under that window. The heartbeat is what makes recovery **multi-instance safe** (a stale heartbeat = no live runner on *any* instance), alongside an atomic token-based claim so two instances can't fail the same spawn twice. |
| `WEBAGENT_BROWSER_CDP_PORT` | Remote-debugging port used for the **on-device browser** (the `browser_backend` switch + the launcher's App Window) — the port the engine launches the real Chrome on and attaches to over CDP (default `9222`). A session only attaches when its backend is `local`; headless sessions ignore it. |
| `WEBAGENT_BROWSER_AUTO_ATTACH` | When `1`/`true`, **every** session attaches to a debuggable Chrome on the CDP port if one is open (the old launcher App-Window behaviour). Default off — sessions stay headless unless explicitly switched to the on-device backend, so a stray debug Chrome never hijacks them. |
| `WEBAGENT_LOCAL_BROWSER_PATH` | Explicit path to the Chrome/Chromium/Edge executable the on-device backend launches. Default: auto-detected from the OS's usual install locations + PATH. |
| `OPENROUTER_REFERER` | Optional Referer header for OpenRouter |
| `OPENROUTER_TITLE` | Optional app title for OpenRouter |
| `SUPABASE_URL` | Supabase URL (**required in cloud mode**) |
| `SUPABASE_SERVICE_ROLE_KEY` | Service role key (**required in cloud mode**) |
| `ENVIRONMENT` | e.g. `development` |
| `LOG_LEVEL` | e.g. `INFO`. Overridable from `data/config/debug-config.json` (`logging.log_level`), the consolidated debug-knobs file, which wins over this env var. |
| `DIAGNOSTICS_CAPTURE_LEVEL` | Minimum log level the diagnostic flight-recorder mirrors from stdlib logging. Overridable from `data/config/debug-config.json` (`logging.diagnostics_capture_level`), which wins over this env var. (**default `INFO`** — full backend detail, affordable now that logs live in the isolated `logs.db`; the handler still applies a per-logger policy so noisy third-party libs are only kept at WARNING+). Other recorder knobs live in `app-settings.json`: `diagnostics_enabled` (default true), `diagnostics_persist_level` (records at this level+ persist to `logs.db`; default `info`), `diagnostics_ring_size` (RAM ring, default 4000), `diagnostics_retention_rows` (durable cap, default 200000), `diagnostics_retention_hours` (default 168 = 7 days), `diagnostics_file_logging` (per-source `logs/*.log` files, default true), `diagnostics_log_dir` (default `logs`), `diagnostics_log_max_bytes` (rotate size, default 5 MB), `diagnostics_log_backups` (default 3), `diagnostics_http_capture` (record 4xx/5xx HTTP errors, default true), `diagnostics_auto_investigate` (off), `diagnostics_auto_investigate_max_per_hour` (default 3). The flight-recorder writes to the **dedicated, always-local `data/db/logs.db`** + `recordings.db` (own WAL, separate from the main DB) — see `app/db/logs_store.py`. |
| `EMBED_MODEL` | Embedding model (default: `text-embedding-3-small`) |
| `EMBED_DIM` | Embedding vector dimension (default: `1536`) |
| `MAX_UPLOAD_SIZE_MB` | Max file upload size in MB (default: 25) |
| `UPLOAD_DIR` | Directory for uploaded files (default: `uploads`) |
| `GOOGLE_CLIENT_ID` | Google OAuth client ID (Integrations page — Gmail, Drive, Docs, Calendar) |
| `GOOGLE_CLIENT_SECRET` | Google OAuth client secret |
| `MICROSOFT_CLIENT_ID` | Microsoft Azure App registration client ID (Integrations page — Outlook, OneDrive, SharePoint) |
| `MICROSOFT_CLIENT_SECRET` | Microsoft client secret value |
| `YAHOO_CLIENT_ID` | Yahoo Developer app consumer key (Integrations page — Yahoo Mail) |
| `YAHOO_CLIENT_SECRET` | Yahoo consumer secret |
| `DROPBOX_APP_KEY` | Dropbox App Console app key (Integrations page — file storage) |
| `DROPBOX_APP_SECRET` | Dropbox app secret |
| `META_APP_ID` | Meta App ID (Integrations page — Facebook + Instagram via one Meta app) |
| `META_APP_SECRET` | Meta App Secret |
| `TWITTER_CLIENT_ID` | Twitter/X OAuth 2.0 Client ID (Integrations page — X/Twitter; uses PKCE) |
| `TWITTER_CLIENT_SECRET` | Twitter/X Client Secret |
| `LINKEDIN_CLIENT_ID` | LinkedIn App Client ID (Integrations page — LinkedIn) |
| `LINKEDIN_CLIENT_SECRET` | LinkedIn Client Secret |
| `TIKTOK_CLIENT_KEY` | TikTok for Developers Client Key (Integrations page — TikTok; uses PKCE) |
| `TIKTOK_CLIENT_SECRET` | TikTok Client Secret |
| `PINTEREST_APP_ID` | Pinterest Developer App ID (Integrations page — Pinterest) |
| `PINTEREST_APP_SECRET` | Pinterest App Secret |
| `REDDIT_CLIENT_ID` | Reddit App Client ID (Integrations page — Reddit) |
| `REDDIT_CLIENT_SECRET` | Reddit Client Secret |
| `SNAPCHAT_CLIENT_ID` | Snap Kit App Client ID (Integrations page — Snapchat) |
| `SNAPCHAT_CLIENT_SECRET` | Snapchat Client Secret |
| `TWITCH_CLIENT_ID` | Twitch Developer App Client ID (Integrations page — Twitch) |
| `TWITCH_CLIENT_SECRET` | Twitch Client Secret |
| `BOOTSTRAP_ADMIN_ID` | User ID to auto-promote to admin on server start (first admin bootstrapping). Once an admin exists in `user_profiles`, this var has no further effect. |
| `WEBHOOK_BASE_URL` | Public base URL of this server (e.g. `https://myservice.run.app`). Set this on Cloud Run or any hosted environment — the app uses it at startup to register webhooks instead of falling back to polling. Auto-detected from the incoming request when first connecting a bot via the UI if not set. |
| `TELEGRAM_BOT_TOKEN` | Telegram bot token (env-var fallback; UI-set value in `registry.json` takes priority). |
| `TWILIO_ACCOUNT_SID` | Twilio Account SID (env-var fallback for SMS and WhatsApp plugins). |
| `TWILIO_AUTH_TOKEN` | Twilio Auth Token (env-var fallback for SMS and WhatsApp plugins). |
| `TWILIO_FROM_NUMBER` | Twilio phone number for outbound SMS in E.164 format (e.g. `+15551234567`). |
| `TWILIO_WHATSAPP_FROM` | Twilio WhatsApp-enabled number (e.g. `whatsapp:+14155238886`; sandbox or approved). |
| `DISCORD_BOT_TOKEN` | Discord bot token (env-var fallback; from discord.com/developers/applications → Bot). |
| `DISCORD_APPLICATION_ID` | Discord Application ID (for interaction endpoint registration). |
| `DISCORD_PUBLIC_KEY` | Discord application public key for Ed25519 webhook signature verification. |
| `SLACK_BOT_TOKEN` | Slack bot token (env-var fallback; `xoxb-…` from api.slack.com/apps → OAuth & Permissions). |
| `SLACK_SIGNING_SECRET` | Slack signing secret for request verification (api.slack.com/apps → Basic Information). |
| `EVENTS_GMAIL_PUBSUB_TOPIC` | Full Cloud Pub/Sub topic name that Gmail `users.watch` should publish to (e.g. `projects/webagent-495517/topics/webagent-gmail-watch`). Enables the Gmail event source. See **[`docs/events-setup.md`](docs/events-setup.md)**. |
| `EVENTS_PUBSUB_AUDIENCE` | OIDC audience set on the Pub/Sub push subscription; webAgent validates inbound JWTs against this. Typically the public webhook URL (e.g. `https://your.host/api/v1/events/gmail`). Required when any Google push event source is enabled. |
| `EVENTS_PUBSUB_SA_EMAIL` | Optional. If set, inbound Pub/Sub JWTs must additionally carry this service-account email in the `email` claim. |
| `EVENTS_GRAPH_NOTIFICATION_BASE` | Public base URL Microsoft Graph posts subscription notifications to (e.g. `https://your.host`). Combined with `/api/v1/events/outlook_mail`, `/outlook_calendar`, `/onedrive` per source. Enables all MS Graph event sources. |
| `EVENTS_GCAL_NOTIFICATION_BASE` | Public base URL Google Calendar channels post to. Combined with `/api/v1/events/google_calendar`. Enables Google Calendar event source. |
| `EVENTS_GDRIVE_NOTIFICATION_BASE` | Public base URL Google Drive channels post to. Combined with `/api/v1/events/google_drive`. Enables Google Drive event source. |
| `EVENTS_DROPBOX_WEBHOOK_BASE` | Public base URL Dropbox posts webhook notifications to. Combined with `/api/v1/events/dropbox`. Enables Dropbox event source. |
| `EVENTS_SHOPIFY_WEBHOOK_BASE` | Public base URL Shopify posts webhook notifications to. Combined with `/api/v1/events/shopify`. Enables Shopify event source. |
| `WEBAGENT_CONFIG_SOURCE` | Set to `env` to lock all storage configuration to environment variables (no JSON file writes). Used on Cloud Run / containerized deploys where the local filesystem is ephemeral. UI shows the storage panel as read-only and the config endpoints return `403` on writes. |
| `WEBAGENT_DB_MODE` | When config is env-locked, selects `cloud` or `local` for the legacy backend toggle (replaces `db_mode.json`). |
| `WEBAGENT_DB_PROVIDER` | When env-locked, the active DB provider (`sqlite`, `supabase`, `postgres`, `gcp_cloud_sql`, `neon`, `mysql`). Companions: `WEBAGENT_DB_HOST`, `WEBAGENT_DB_PORT`, `WEBAGENT_DB_NAME`, `WEBAGENT_DB_USER`, `WEBAGENT_DB_SSLMODE` (`disable` for local docker, `require`/`verify-full` for remote), and the password via either `WEBAGENT_DB_PASSWORD` (direct) or `WEBAGENT_DB_PASSWORD_KEY` (key into the secrets vault). |
| `WEBAGENT_SECRETS_PROVIDER` | Selects the secrets vault: `inline_db`, `env`, `os_keyring`, `gcp_secret_manager`, `aws_secrets_manager`. In-code fallback is `inline_db`, but a **fresh, keyring-capable install auto-enables `os_keyring` + per-tenant `field` encryption on first boot** (see `app/encryption/defaults.py`; skipped when env-locked, when no secure OS keyring is present, or when secrets already exist). |
| `WEBAGENT_SECRET_*` | When the secrets provider is `env`, secret keys map to env vars with this prefix (e.g. key `db_password_postgres` → `WEBAGENT_SECRET_DB_PASSWORD_POSTGRES`). |
| `GCP_PROJECT` / `GOOGLE_CLOUD_PROJECT` | GCP project ID — required by the `gcp_secret_manager` secrets provider. |
| `AWS_REGION` | AWS region — used by the `aws_secrets_manager` secrets provider. |

In **local** mode, Supabase vars are not required for storage; you still need **`OPENROUTER_API_KEY`** (and usually **`OPENROUTER_MODEL`**) for LLM calls.

Provider, API key, and model can **also** be configured at runtime via the ⚙️ **Settings** modal in the UI (gear icon next to Cloud/Local toggle). Changes are saved to **`data/config/provider.json`** and applied on next server start. The API key is masked in the UI after saving.

**One default model, user-switchable per chat (no racing):** each agent runs **exactly one** model per turn — the **default**. Parallel model racing was removed. The default is chosen explicitly with a **"Default" radio** in the model table (left of the Text/In/Out columns); it's stored as the top-level `model` of the provider config (admin) or the agent's `metadata['llm_config']`. The saved-model **roster** (`multi_providers`) is still kept, but it is *not* raced — it only supplies the candidate list for the chat model-switcher and the vision / image-out worker picks.

**Per-agent model lists:** an individual agent can carry its **own** roster — stored in `metadata['llm_config']` as `{use_default, provider, base_url, api_key, model, multi_providers:[…]}`, where `model` is the chosen default. At run time `_agent_llm_override` / `_merge_agent_override` (in `app/admin/settings.py`) merge it over the app default (resolution order **app-default → agent → session**). The model-table UI for both the **admin default** (App Config → Agent Settings → Models) and the **per-agent** config (agent card → Config → LLM) is one shared, instantiable component, **`ui/shared/js/model-table.js`** (`mountModelTable`); the two mounts are sister panels (`SISTER-PANEL: MODEL-TABLE`). When the admin's **"Extend default LLM to agents"** is on the per-agent table shows the app-default models as read-only **"Inherited"** rows plus the agent's own editable rows; when off, only the agent's own models are shown.

**Model metadata catalog:** `app/model_catalog.py` is a self-contained library that merges two public, keyless sources — **models.dev** (`https://models.dev/api.json`, the backbone: context window, max output, input/output/cache cost, capability flags, modalities, family, release date across OpenAI / Anthropic / Google / Mistral / xAI / Meta and more) and **OpenRouter** (`https://openrouter.ai/api/v1/models`, written descriptions + extra coverage). The merged map is keyed by model id (matched on both full ids like `openai/gpt-5` and bare ids like `gpt-5`, with the picker's selected provider preferred for bare-id resolution) and cached to **`data/config/model_catalog.json`** with a fetch timestamp — served from cache instantly and re-fetched only when stale (> 24h) or forced. With this in place, `GET /admin/settings/models` enriches each model with `context`, `max_output`, `cost_input`, `cost_output`, and `description`, which the Settings model picker shows as context/cost badges per row and a metadata panel (chips + description) for the selected model. Endpoints: `GET /admin/settings/model-catalog` (cache status), `POST /admin/settings/model-catalog/refresh` (force re-fetch), `GET /admin/settings/model-info?model=<id>` (full merged metadata for one model), `GET /admin/settings/current-model-info?agent_id=<id>&session_id=<id>` (resolves the requesting user's *active* model exactly as a run does — app default then any per-agent override then any per-session override — and returns its context/max-output/cost in one call). The chat footer uses that last endpoint to show the active model's context window and max output (e.g. `ctx 1M · max 384K`) next to the in/out token counters; it refreshes when the model is changed in Settings (via `app.refreshModelContext`). **A dedicated "model" button below the chat pill** (`#chat-model-btn`, in `.chat-footer-right` next to the Ask/Plan/Auto pill) shows the active model's short name and opens an in-place model picker (`ui/shared/js/chat-activity.js`); the footer context chip is also still clickable. The picker is a floating panel anchored to the **left edge of the chat pill**, sitting **just above** the context-details line, themed with design-system variables (correct in dark + light). Its list is **curated, not the full provider catalog** — it shows **all of the agent's Text-capable models** (the union of the app default(s) + the agent's own roster, resolved server-side by `GET /admin/settings/agent-models?agent_id=<id>&session_id=<id>` exactly as the run resolves it). It falls back to the admin defaults (`GET /admin/settings/multi-providers`) when there's no agent/session. Each row shows the model's context size, the **total tokens that model has used in the current session**, and (in a detail footer for the focused row) its description + input/output cost per 1M tokens. **Picking a model is per-session by default:** in a chat it saves the choice on the **session** (`POST /api/v1/chat/session-model` → `sessions.metadata.llm_config`), so each conversation remembers its own model without touching the agent default or other sessions; with an agent but no session it overrides the agent's `llm_config.model`, and with neither it updates the global provider model. The switch takes effect on the **next turn** — the loop re-resolves the effective model every run as **app-default → agent → session** (`apply_provider_for_run` in `app/admin/settings.py`). Confirmation is an inline **spinner → green-check** (red cross on failure). The per-session per-model totals come from **`GET /admin/settings/session-model-usage?session_id=<id>`**, which sums input+output tokens **and `cost_usd`** per `model` for the session, scoped to the requesting user. The library lives outside `app/admin/` so model metadata keeps working if the admin package is removed.

**Accurate cost accounting (per call, per model).** Every LLM call records a row in **`usage_events`** carrying its `model`, `provider`, tokens, **and a locked-in `cost_usd`** — computed at save time from that model's published per-1M price (`app/model_catalog.cost_for`) × the call's tokens, so the figure never re-prices when a session later switches models. The table also gained a direct **`session_id`** and a **`source`** tag (`chat` vs `background:*`). **Background (non-chat) LLM calls are metered too** — git commit-message generation, the chat-pill suggestion text, the App-Settings page-assistant placeholder ideas (label `page-suggest`; **`POST /api/v1/app-prompts/page-suggestions`** → `app/agent/suggestions.py` `generate_page_suggestions`, which turns the hovered section's `suggest` hint in `app-prompts.json` into rotating example requests shown in the pill's placeholder — grounded in the app's CURRENT pages (`app.ui_pages.ui_catalog`, so it proposes new pages and never duplicates existing ones) and the default webAgent's MEMORY of the user (`db.memory_search` fast FTS path) so some ideas are personalised), context compaction, and embeddings each write a `source='background:*'` row via **`plugins/billing/usage.py` `record_background_usage`** (attributed to a `system` owner), so app-wide totals include them. Summing `cost_usd` then powers three surfaces: the chat footer's **session cost chip** (a running per-call sum — `_sessionCostUsd` in `chat-activity.js`, reconciled on load against **`GET /admin/settings/session-cost?session_id=<id>`**, so it stays correct across mid-session model switches); the **agent config tab's "Usage & Cost"** block (total + per-model breakdown from **`GET /admin/settings/agent-usage?agent_id=<id>`**, scoped to the **(viewing user, agent)** pair — what *this* user has spent with *this* agent, not the agent's all-users total; every row of that model table, inherited app-default models included, reports the same per-(user, agent) figure, and the table's "Reset Usage Counters" clears only the viewer's own rows); and the **admin Agent Settings model table**, whose usage cells call **`GET /admin/settings/model-usage?model=<id>&provider=<p>&scope=global`** — `scope=global` (admin-only) sums every agent, user, and background task app-wide and returns `total_cost_usd` (the canonical published-price figure) alongside the provider-billed `total_cost_cents`.

**Per-model reasoning effort (manual + agent-driven):** alongside *which* model runs a chat, each chat can set *how hard* the model thinks — the provider's reasoning-effort dial (`minimal → low → medium → high`, plus **default** = send no hint). In the footer model picker every reasoning-capable model row carries a small effort selector (Default · Min · Low · Med · High); the choice is **remembered per model** for the session. It saves via `POST /api/v1/chat/session-model-effort` into the same `sessions.metadata.llm_config` as the picked model, under a `model_effort` map (`{model id → level}`) — kept separate from the picked model so switching models preserves each model's level, and surviving a model swap. The level is resolved per run by `_resolve_session_effort` (in `app/admin/settings.py`) for whichever model is active and stashed in `LLM_REASONING_EFFORT`; the agent loop sends it as `extra_body.reasoning.effort` on the LLM call and, if the provider rejects the param or level, drops it and retries once (so an unsupported effort never breaks the turn). `GET /admin/settings/current-model-info` also returns the active `reasoning_effort` and the model's `reasoning` capability flag. **The Model Switcher ability** (`plugins/abilities/Core/model_switcher/`) lets the agent drive both dials itself: it rates a task's difficulty and either keeps the default model but calls `set_effort('<level>')`, or switches model *and* sets its effort, then calls `reset_to_default()` (clears both the picked model and all per-model effort) when the task is done. Raising effort or upgrading the model is confirm-gated (asks before spending more); lowering or reverting is free. Its **Model switching** posture knob (Conservative / Balanced / Eager) biases how proactively it right-sizes both.

## Installation

> Plain-English versions in the Wiki: [Installing & Running webAgent](http://localhost:8080/?tab=wiki&article=installing-running-webagent) and [Supported Devices & Platforms](http://localhost:8080/?tab=wiki&article=supported-devices-platforms). The exact commands and flags are below.

**Supported Python:** 3.11 or 3.12 (pinned via `pyproject.toml` `requires-python = ">=3.11,<3.13"`). Newer versions may drop stdlib modules or lack wheels for pinned deps.

1. Clone the repository and enter the directory.

2. **Windows quick path:** double-click **`webAgent.bat`**. It will:
   - Install **uv** (Astral's Python + venv manager) if missing — via the official PowerShell installer.
   - Run `uv sync`, which downloads a matching Python (3.11 or 3.12) into uv's cache if you don't have one, creates `.venv` in the project root, and installs all deps from `pyproject.toml` / `uv.lock`.
   - If the existing `.venv` was built with an unsupported Python, the script removes it so uv can rebuild cleanly.
   - Start the server with auto-restart on exit.

   On subsequent boots, `uv sync` is idempotent and near-instant when deps haven't changed.

   **Manual / non-Windows (uv path, recommended):**

```bash
# Install uv if not already present
curl -LsSf https://astral.sh/uv/install.sh | sh

# Sync deps + Python (idempotent)
uv sync

# Run the server
uv run python run.py
```

   **Manual / non-Windows (legacy pip path):** still works — `requirements.txt` is maintained alongside `pyproject.toml`.

```bash
python -m venv .venv
source .venv/bin/activate    # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

3. **Cloud (Supabase)** — If you use **`cloud`** mode, apply your team’s canonical schema (often the **Web Portal** monorepo migration, e.g. `Web Portal/supabase/migrations/20260130120000_webagent_complete_schema.sql`, when that sibling repo exists). The **`migrations/`** folder in *this* repo holds extra or historical SQL; read **`migrations/README.md`** for how it is being used.

  > **New (v0.3+):** `007_create_channel_identities.sql` and `008_create_linking_codes.sql` add tables for the communication plugin system (Telegram, WhatsApp, SMS). The **local** backend auto-creates these; on **Supabase**, apply via the SQL editor.

  > **New (v0.4+):** `009_multi_agent_system.sql` adds the `user_profiles` table (admin flag, default agent), extends `agent_templates` with `name`, `description`, `icon`, `trigger_description`, `can_be_default`, `is_system`, `is_pipeline`, `access_level` columns, extends `agents` with `template_id`, `owner_user_id`, `is_user_default`, `is_pipeline` columns, and seeds the default/optimizer/admin-agent templates. The **local** SQLite backend auto-applies these columns; on **Supabase**, run via SQL editor.

  > **New (v0.5+):** The **local** SQLite backend auto-applies two new columns to the `agents` table: `allowed_tools` (JSON array of disabled Tier-2 tool names — empty = all enabled) and `custom_tool_ids` (JSON array of opted-in DB tool IDs). These drive the interactive Agent Loop diagram editor in the UI. The `agents.py` API exposes `model`, `temperature`, and `max_tokens` as user-editable fields via `PUT /api/v1/agents/{id}`. Tool filtering is enforced at runtime in `app/tools/loader.py` so the LLM never sees disabled tools in its schema.

  > **New (v0.6+):** **Loop Logic Engine** — the `loop_logic` column (already present as `TEXT NOT NULL DEFAULT '[]'`) now drives runtime gating of five optional agent loop steps: `interrupt_chk`, `permission_chk`, `guardrails`, `delegation_chk`, and `skill_track`. Store a JSON object array (e.g. `[{"node": "skill_track", "enabled": false}]`) to selectively disable steps; a flat string array or empty array means all steps run (backward-compatible with existing agents). `LoopConfig` in `app/agent/loop_executor.py` parses this column and is called by `loop.py` before each gated step. The Agent Loop diagram in the UI lets users toggle these nodes per-agent; disabled nodes appear muted with a strikethrough label. No schema migration is required — the column already exists.

  > **New (v0.7+):** **Configurable Guardrails** — two new columns drive per-agent safety policies. `agents.safety_policy` (JSON) stores `destructive_tools` (tool names added on top of the hardcoded baseline), `auto_confirm` (skip the confirmation gate for automation agents), `blocked_imports` (extra modules forbidden in `create_tool`), and `max_concurrent_tools` (cap parallel tool execution). `tools.requires_confirmation` (boolean) marks individual user-created tools as destructive. At runtime `loop.py` merges all three sources into a single effective set — the hardcoded baseline (`DESTRUCTIVE_TOOLS`) ∪ `safety_policy.destructive_tools` ∪ any loaded tool with `requires_confirmation=True`. A **Safety & Guardrails** section in the agent Config tab lets users manage these settings with no code changes. The local SQLite backend auto-applies the schema via ALTER TABLE migrations; on **Supabase**, run `migrations/012_safety_policy.sql` via the SQL editor.

  > **New (v0.8+):** **Row ordering** — `agents.sort_order` and `sessions.sort_order` (both nullable `INTEGER`) persist the user's manual **drag-to-reorder** order for the Agents page and the chat-header session dropdown (each row has a grip handle). The agent order is applied on the Agents page (🤖); the session order applies in the chat-header session dropdown (which now spans all agents). Order is written by index via `POST /api/v1/agents/reorder` and `POST /api/v1/db/sessions/reorder` (account-level, so it follows the user across devices), and read back via the `(sort_order IS NULL), sort_order ASC, …` clauses in `list_agents_for_user` and the sessions list query (pinned rows still float to the top). The shared frontend helper (pin state, display sort, and the pointer-based drag that works on mouse + touch) lives in `ui/shared/js/ordering.js`, imported by both `ui/chat-side-panel/js/session-core.js` (the dropdowns) and `ui/main-panel/agents/js/agents.js` (the Agents page). The local SQLite backend auto-applies the columns via ALTER TABLE migrations; on **Supabase**, run `migrations/024_row_ordering.sql` via the SQL editor.

4. Run the server:

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8080
```

**Windows users:** `webAgent.bat` (described above) is the recommended path — it handles venv creation, dependency install/update, and auto-restart. Internally it invokes `run.py`, which **guarantees a single server instance**: before binding it kills any process already listening on port 8080 (take-over policy — a killed mid-turn run is recovered by the app's startup orphan-cleanup) and then binds the port **exclusively** (no `SO_REUSEADDR`). This matters because the live chat WebSocket and the running agent only share events through one process's memory; if two servers stacked on :8080 (which Windows `SO_REUSEADDR` used to allow), the OS split connections between them and live messages silently failed to stream while still landing in the shared DB.

**TUI launcher (optional, prettier than .bat):** the `launcher/` directory builds a portable `webagent.exe` (Textual-based TUI, Lucide bot icon) with buttons for Launch / Restart / Stop / Open Browser / Clear DB / Reset Python / Full Reset, plus an animated procedural ASCII background with cycleable color presets (phosphor, amber, cyan, vaporwave, neon tide, rainbow, fire, ice, …). Config (project path, theme) persists at `%APPDATA%\webAgent\launcher.json` so the .exe can sit anywhere. To build: `cd launcher && uv sync --extra build && uv run python scripts/build_exe.py` → `launcher/webagent.exe` (single file, no scratch folders left behind). To run from source: `cd launcher && uv sync && uv run python -m webagent_launcher`. Keyboard shortcuts and feature list in `launcher/README.md`.

**Unix:** `bash scripts/start_webAgent.sh` (background + logs).

**Android (Termux + proot-distro Ubuntu):** the `launcher_android/` directory is the Android counterpart to `launcher/` — a Textual TUI with Launch / Restart / Kill / Browser buttons plus a dependency **Doctor** that surfaces venv / `.env` / apt build-deps / per-package pip status and offers one-tap fixes. The known Android-ARM blocker (Playwright Chromium download fails on ARM) is downgraded from fatal to skip-OK by writing `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` into `.env`; other optional deps that fail to install are listed as non-critical rather than blocking startup. Run it from Termux with `bash launcher_android/start.sh` — the shim handles installing `proot-distro`, the Ubuntu distro, and `textual` inside the proot on first run. See `launcher_android/README.md` for full setup steps and the Browser-bridge mechanism that forwards URLs to Android's `termux-open-url`.

**Android (Termux, TUI manager only):** to install just the standalone **Server Manager TUI** (`TUI/`) on a phone — not the full server — paste one line into Termux:

```
cd ~ && pkg install -y git && git clone --depth 1 https://github.com/botboss3000/webagent ~/webagent && bash ~/webagent/TUI/install-termux.sh
```

…or, using the short public URL the server itself hosts:

```
pkg install -y curl && curl -fsSL https://webagent.live/termux | bash
```

The installer (`TUI/install-termux.sh`) installs Python + git, the TUI and its two deps (clean editable install on Python 3.11/3.12, deps-only run-from-source fallback on newer), then drops a `webagent` command on PATH plus a Termux:Widget home-screen shortcut. The app serves that script verbatim (LF-normalised) at **`/termux`** (and `/termux.sh`). Unlike `launcher_android/` (which runs the whole server under proot), this is just the lightweight manager. See `TUI/README.md`.

**Useful URLs**

| URL | Purpose |
|-----|---------|
| `http://localhost:8080/` | **Front door.** A new human visitor gets the server-rendered crawlable landing page when the welcome is on (App Settings → Startup & Boot, `splash_enabled`); returning visitors (the `wa_seen_splash` cookie) and humans when it's off get the app shell. **Search/social crawlers always get the crawlable landing — independent of `splash_enabled`** (`_is_crawler` by User-Agent), so the home page is never an empty JS shell to a search engine. Landing built by `_render_app_shell` / `_render_landing_page` from `ui/splash/splash-page/`, with Organisation + WebSite **JSON-LD** structured data. |
| `http://localhost:8080/app` | **The app's stable home** — always the app shell, bypassing the landing. The landing's "Enter app" button and the installed PWA (`manifest.json` `start_url`) point here. Serves the **same** content as `/`, so its `canonical` (and `og:url`) point at `/` — search engines treat the two as one page, not duplicate content (`_render_app_shell`). |
| `http://localhost:8080/docs` | Swagger |
| `http://localhost:8080/index.html` | Main UI (legacy alias; also bypasses the landing). Like `/app`, its `canonical` points at `/` (same content → not a duplicate). |
| `http://localhost:8080/test` | Minimal HTML chat (`ui/test_interface.html`) |
| `http://localhost:8080/web-terminal/` | Terminal tab page (self-contained web-terminal app; also embedded in the main UI) |
| `http://localhost:8080/privacy` | Privacy Policy page (`ui/privacy.html`, public, no auth). The static file ships only a bare `<title>`, so the handler injects the shared SEO head (description / canonical / Open Graph) server-side before `</head>` (`_render_static_seo_page`) so the page isn't thin to a crawler. |
| `http://localhost:8080/tos` | Terms of Service page (`ui/tos.html`, public, no auth). Same server-side SEO-head injection as `/privacy`. |
| `http://localhost:8080/robots.txt` | Crawler directives (public). Disallows API/auth/dev endpoints, allows the public surface + static assets, points to the sitemap. Built in `app/main.py` (`robots_txt`) with an absolute `Sitemap:` line from the configured base URL. |
| `http://localhost:8080/wiki` | **Public, crawlable Wiki index** (server-rendered) — lists every **published** article, grouped by category, each linking to its own page. Same data as the in-app Wiki SPA (`data/wiki.db`) but real HTML so search engines can read it. Built in `app/main.py` (`wiki_public_index`) via `app/wiki/public_pages.py`; carries a BreadcrumbList **JSON-LD**. Drafts never appear. |
| `http://localhost:8080/wiki/<slug>` | **Public, crawlable page for one published article** (server-rendered) — drawn live from the DB on each request (no build step; edit/publish in-app → next visit reflects it), with its own `<title>` / meta description / canonical / Open Graph (**`og:type=article`** + `article:published_time` / `article:modified_time`, so it previews as a dated article) + **Article & BreadcrumbList JSON-LD** (headline, publish/updated dates, publisher). The share image is the **article's own first image** when it has one (external urls trusted as-is; a site-relative one used only if it verifiably exists on disk), else the default 1200×630 card. 404s for a missing slug or a draft. Built in `app/main.py` (`wiki_public_article`). The in-app reader's "Copy link" button copies this URL. |
| `http://localhost:8080/sitemap.xml` | Sitemap of publicly indexable pages — home + `/privacy` + `/tos` + `/wiki` + **one entry per published wiki article** (with `<lastmod>`, read live from `data/wiki.db`). Public per-agent pages omitted pending indexing policy. Built in `app/main.py` (`sitemap_xml`). |
| `https://webagent.live/termux` (also `/termux.sh`) | One-line Termux installer for the standalone webAgent TUI — serves `TUI/install-termux.sh` (LF-normalised); `curl -fsSL https://webagent.live/termux \| bash` |
| `http://localhost:8080/user_data/<uid>/<room>/<file>` | Per-user data home (auth-gated): **uploads**, generated **visuals**, browser **screenshots**, and general agent outputs (**files**). Written by the attachment store, image-gen, browser tool, and the **User Files** ability (`save_file`) — see `app/user_workspace.py`. (Replaces the retired `/uploads` and `/visuals` mounts.) |
| `http://localhost:8080/api/v1/canvases/<uid>/<slug>/html` | Served Canvas body (bodies stored at `data/user_data/<uid>/canvas/<slug>/index.html`). **Gated:** every `/api/v1/canvases/*` route now resolves the caller from their token and enforces (1) the **Canvas page's visibility** (registration required by default; an admin sets it in App Settings → main-panel pages) and (2) **per-user ownership** (you can only read/write your own canvases; admins may act for anyone). So a hidden tab can't be bypassed by calling the API directly. |
| `http://localhost:8080/api/v1/canvases/<uid>/<slug>/logs` | Per-canvas **console log** — POST records the page's own browser console output (captured live), GET reads it back. Stored beside the canvas at `data/user_data/<uid>/canvas/<slug>/console.log` (JSON-lines, capped, auto-cleared on re-render); the agent reads it via the `get_canvas_logs` tool. Not the global `logs.db`. |
| `http://localhost:8080/api/v1/canvases?user_id=...` | Canvases REST API (list / create / delete / rename) |
| `http://localhost:8080/api/v1/canvases/vault/keys` | **Vault Capture** — agent-defined canvas credentials (Bearer-auth, caller's own vault). `GET` lists keys (metadata only), `GET/POST/DELETE /{key_id}` view / save / remove. The **save** is the only write path for the secret (browser→server straight from the secure card, never via the agent). Backed by `app/abilities/vault_store.py` (a `canvas_vault` row in the encrypted `auth_elements` store). |
| `http://localhost:8080/api/v1/canvases/vault/proxy` | **Vault Capture** credentialed request — `POST {key_id, path\|url, method, …}` makes an outbound call with the stored secret attached **server-side** per the key's binding (pinned to its `service_url`). Lets a canvas use a key by id (`api.callWithKey`) without ever holding the plaintext. The agent's `request_credential` / `list_vault_keys` tools (Visualizer ability) reserve + inspect these keys. |
| `http://localhost:8080/api/v1/wiki` | Company-wide Wiki REST API — list / `search?q=` (hybrid) / `{slug}` CRUD |
| `http://localhost:8080/api/v1/diagnostics` | Diagnostic flight-recorder — recent records (admin only; filters: `levels`, `categories`, `session_id`, `agent_id`, `search`, `since_minutes`, `limit`) |
| `http://localhost:8080/api/v1/recordings` | Client render recorder — recent browser records (admin only; filters: `kinds`, `levels`, `session_id`, `client_id`, `session_seq`, `since_minutes`, `search`, `limit`; html omitted — fetch per-row via `/api/v1/recordings/one?id=`). OFF until `render_recording_enabled` is set. |
| `http://localhost:8080/ui/diagnostics.html` | **Standalone Diagnostics page** — a single self-contained file that renders the same flight-recorder feed independently of the main app's boot. Survives a broken front-end (the API process just has to be alive); reuses the app's `auth_token` when same-origin; can also be opened from disk (`file://`) and pointed at a remote server (`?server=…&token=…` or the sidebar Connection panel). |
| `http://localhost:8080/go/<key>` | Remote Access — phone bookmark; redirects to the PC's current address |
| `http://localhost:8080/api/v1/remote-access/pointer` | Remote Access signpost — PC reports its current address (POST) |
| `http://localhost:8080/docs` | Swagger UI (includes upload endpoint docs) |

**Unix quick start (background + logs):** `bash scripts/start_webAgent.sh` (works from any cwd; script `cd`s to repo root).

## Context defaults (no `/seed-docs` route)

There is **no** `POST /api/v1/seed-docs` in this codebase. When a user has no context rows, the first chat path copies **`context_templates`** into that user’s store (**`public.context`** on Supabase, **`context_documents`** in local SQLite).

## Using the chat API

**`POST /api/v1/chat`**

Minimal JSON body:

```json
{
  "user_id": "auth-user-uuid",
  "session_id": "session-uuid",
  "message": "What can you help me with?"
}
```

**`session_id`** is auto-created if it doesn't exist (first message in a new session creates the row automatically).

Optional fields include **`documents`**, legacy **`history`**, and **`attachment_ids`** (list of UUIDs from prior uploads) — see **`ChatRequest`** in **`app/models/schemas.py`**. When `attachment_ids` are provided, the agent sees a `[USER ATTACHMENTS]` section in its system prompt and can use the **`read_attachment`** tool to inspect file contents. **Model context is rebuilt from `interactions` in the DB** for that `session_id`; you do not need to resend **`history`** after a refresh (it is ignored for the transcript).

Example:

```bash
curl -X POST "http://localhost:8080/api/v1/chat" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "user_123",
    "session_id": "session_456",
    "message": "What can you help me with?"
  }'
```

Response includes **`reply`**, **`response`** (duplicate for different clients), and **`session_id`**. Turns are stored in **`interactions`**.

## Adding custom context

- **Supabase:** insert into **`public.context`** with the desired **`context_type`**.
- **Local:** insert into **`context_documents`**.

Common types include `agent`, `user`, `skills`, `tools`, `tasks`, and optionally `memory`, `project`, `jobs` (see **`app/api/chat.py`**).

## Prompt slots and overrides

> For *what* prompts and context are (in plain English), see the Wiki: [Prompts & Context](http://localhost:8080/?tab=wiki&article=prompts-context). This section is the storage-layer reference.

Prompts that shape an agent flow through **three tables**, each with a single, clear job:

| Table | What it holds | Who writes it |
|-------|---------------|---------------|
| **`agent_templates`** | Per-template **config** (model, temperature, max_tokens, loop_logic, trigger). One row per template id (`default`, `admin-agent`, etc.). | JSON seeder |
| **`agent_prompt_templates`** | Canonical **slot content** for each template (system, agent, user, skills, tasks, misc, automation, bootstrap_tools). One row per `(template_id, slot_name)`. Versioned via `version` int + `source` enum (`'json'` \| `'admin'`). | JSON seeder + admin-edit-back UI (planned) |
| **`agent_prompts`** | Per-agent runtime slot rows: the **admin-base rows cloned from the template** when a new agent is created, plus any **per-user override rows**. Each clone stamps `template_version` so the agent can be told "your template moved to v4". | Agent creation + per-user override endpoints |

A new agent is born by:

1. Looking up the template row in `agent_templates` (config).
2. Cloning every row from `agent_prompt_templates WHERE template_id = <template_id>` into `agent_prompts` under the new agent's id (with `user_id IS NULL` and `template_version` stamped from the source row).
3. Any per-user override gets written as an extra row in `agent_prompts` with a non-null `user_id`.

**Admin base** rows in `agent_prompts` carry the slot's policy: `order_index` (where the slot sits in the assembled system message), `lock` (admin-only when true), and `merge_mode` (`replace` or `append`).

**User override** rows have a non-null `user_id`. Each user (including anonymous browser-scoped visitors) can have at most one override per slot. When the agent loop assembles the system prompt for a caller, locked slots ignore overrides; unlocked slots either `replace` the admin base with the user override or `append` it below, per the slot's `merge_mode`.

### Seeding from JSON (manifest-gated, version-aware)

`app/defaults/agents/*.json` (or a `data/agents/` override if present) is the recovery source of truth for fresh databases. See [`app/defaults/agents/README.md`](app/defaults/agents/README.md) for the **full field schema** — every agent-page setting a template can carry (limits, trigger, loop logic, per-tool `tool_modes`/`tool_permissions`), with override-or-default semantics (present overrides the built-in default; missing falls back and never breaks). Each file declares a top-level integer `"version"` that gates the per-slot upsert:

- New slot → insert with `source='json'`, `version=<JSON version>`.
- Existing slot with `source='json'` and JSON version > DB version → update + bump.
- Existing slot with `source='admin'` → **skip** (admin edits are sacred).
- Manifest hash of all JSON files is stored in `app_meta.last_agent_manifest_hash`; the seeder short-circuits when the hash matches, so it's cheap to call at every boot.

The seeder runs at **app startup** (manifest-gated, almost always a no-op). To re-seed on demand or force-overwrite admin edits, use **App Config → Database → Agent Prompt Templates** (`/admin/db/templates`, `POST /admin/db/templates/seed`, `POST /admin/db/templates/seed-force`).

### Endpoints

- **`GET /api/v1/agents/{agent_id}/slots?user_id=...`** — returns the admin-base slot list plus the caller's overrides per slot, and a `user_role` of `"admin"` or `"member"`.
- **`PUT /api/v1/agents/{agent_id}`** (admin only) — agent-row fields plus an optional `slots` array that fully reconciles the admin-base slot set. May include `reset_overrides_for` (list of slot_names) to wipe all per-user overrides for those slots at save time.
- **`PUT /api/v1/agents/{agent_id}/my-prompts`** — any caller writes their own override rows for unlocked slots; locked or unknown slot_names are rejected per item.
- **`DELETE /api/v1/agents/{agent_id}/my-prompts/{slot_name}?user_id=...`** — clears one user's override for a slot.

### Recycling bin (soft delete)

Agents, chat sessions, and automations are never hard-deleted from the normal UI — they go to a recycling bin first. Lifecycle is tracked by a marker column: agents are `active` ↔ `trashed`, sessions are `active` ↔ `recycled`, and the automation surfaces use a nullable **`deleted_at`** (NULL = active, set = binned). Child rows (agent_prompts, interactions) carry no flag of their own; they follow their parent and are erased only on a permanent delete. (`agents.status` already existed and was extended; `sessions.status` was added; `deleted_at` was added to `agent_automations`, `agent_event_subscriptions`, `webhook_registrations`, and the plugin-owned `agent_spawns` (clones reuse `status='clone_trashed'`) — see `app/db/schema/tables.py` and the `local.py` SCHEMA_SQL + ALTER-TABLE migration 030; Postgres picks it up via column reconcile.)

All three pages share one toolbar control — a button that shows a green **recycle** icon when nothing is selected (click it to open the bin) and a red **trash** icon when rows are selected (two-click confirm to move them to the bin, or — inside the bin — to permanently delete). Inside the bin a **Back** link and a **Restore** button appear. (HARMONY: grep `HARMONY-AGENTS-BIN` / `HARMONY-SESSIONS-BIN`.)

- **`GET /api/v1/agents?user_id=...&view=bin`** — lists only the caller's trashed agents (the Agents-page bin view). Default `view=active` lists live agents + visible templates.
- **`DELETE /api/v1/agents/{agent_id}?user_id=...`** — SOFT delete: moves the agent to the bin (`status='trashed'`). Add **`&permanent=true`** to erase it for good (agent + prompts + its sessions + their transcripts/summaries + connections/abilities) — used by the bin's own trash button. Permanent delete also **cascade-removes any clones** the agent's sessions orchestrated (`cascade_delete_clones`; `status='clone'` agents only).
- **`POST /api/v1/agents/{agent_id}/restore?user_id=...`** — restores a trashed agent back to the Agents page.
- **`DELETE /api/v1/db/sessions/{session_id}?db=local.db`** — SOFT delete: recycles the session (`status='recycled'`, hidden from the chat dropdown but kept). Add **`&permanent=true`** to erase it. A recycled session is truly removed only when its agent is permanently emptied from the bin.
- **Automations completed view + bin.** The Automations dashboard (`ui/main-panel/automations/server.py`) splits its rows into three sibling views via **`?view=`**: `active` (default — only in-process/future automations), **`completed`** (a non-destructive history of automations that have finished for good — one-off tasks that fired, expired/exhausted recurring tasks, and worker spawns in a terminal status; computed at read time by `_is_completed`, never a stored flag, so nothing migrates), and `bin` (the recycling bin). The completed view keeps the main list uncluttered without deleting anything; its toolbar button carries a `completed_count` badge and deleting from it soft-deletes into the bin like the active view. The dashboard recycles all five record types it aggregates. **`GET /api/v1/automations/dashboard?user_id=...&view=bin`** lists the binned rows (default `view=active`). The four `DELETE` routes (`/{id}` for scheduled+event, `/spawn/{id}`, `/clone/{id}`, `/webhook/{id}`) SOFT-delete by default and take **`&permanent=true`** to erase; each has a matching **`POST .../restore`**. Recycling a scheduled task or event sub also sets `enabled=0` (so the scheduler/event engine — which only act on `enabled=1` rows — never fire a binned row, no hot-path change) and mangles its slot `source_hash`; a webhook recycle sets `active=0`. Slot reconciliation (`delete_*_not_in`) skips `deleted_at IS NOT NULL` rows so the bin is never silently purged, and restored rows flip to `origin='dashboard'` so reconciliation leaves them alone.
- **Hidden sessions (declutter, not delete).** Sessions carry a `hidden` flag (`sessions.hidden` INTEGER, default 0 — added via the `local.py` ALTER-TABLE migration, paired with `pinned`). The chat-header dropdown has a **management footer row** (an eye toggle + a delete-all button) — see `ui/chat-side-panel/js/session-list.js` / `session-init.js`. The eye toggle enters "manage mode": it reveals hidden sessions and swaps each row's trash button for an eye that hides/un-hides that session. **`GET /api/v1/db/sessions`** excludes `hidden=1` rows by default; pass **`&include_hidden=1`** to include them (each session object carries a `hidden` boolean). **`PATCH /api/v1/db/sessions/{session_id}`** now accepts a `hidden` boolean (alongside `title` and `pinned`). The footer **delete-all** spares pinned and hidden sessions and reuses the per-session soft delete.

The Agents-page header (`ui/main-panel/agents/agents.html` + `ui/main-panel/agents/js/agents.js`) renders the bin controls — per-square checkboxes plus **Select all / Deselect all / Trash** (and **Back to Agents / Restore** inside the bin). The trash button moves checked agents to the bin, or — with nothing checked — opens the bin view (a view-swap inside the same Agents tab, not a new nav tab).

### Clones view

The Agents page has a **Clones** tile (to the right of the "New Agent" tile) that opens a read-only view listing ephemeral clone agents created by the Agent Orchestration ability. Clones have `status='clone'` and are hidden from the normal roster and recycling bin. The view groups clones by their orchestrator agent and shows each clone's name, `spawn_status`, and result summary. Use the "Back to Agents" header to return to the normal grid.

- **`GET /api/v1/agents?user_id=...&view=clones`** — lists the caller's `status='clone'` agents, newest first, enriched with spawn-ledger fields (`spawn_status`, `result_summary`, `orchestrator_session_id`, `orchestrator_agent_id`) from the `agent_spawns` table when available.
- The "Clones" tile uses a Lucide `copy` icon with the `color-teal` square style. The clones view shares the same "Back to Agents" toolbar pattern as the recycling bin (grep `RETURN-TO-AGENTS-HEADER` in `ui/`).

### In-chat self-modification

The agent's in-chat prompt-refinement flow uses the same admin/user split: admin callers may write to admin-base rows, members and anonymous visitors can only write to their own override rows. Locked slots are refused with a user-visible message.

### Optional: run on local Postgres (pgvector)

SQLite is the zero-config default and needs no setup. To run on Postgres locally
(for multi-writer concurrency and a path toward a remote/managed Postgres later):

1. **Start Postgres + pgvector** (Docker):

   ```bash
   docker compose -f docker-compose.postgres.yml up -d
   ```

   This runs Postgres 16 with pgvector on `localhost:5433` (db/user/pass = `webagent`).

2. **Copy your existing SQLite data over** (idempotent; safe to re-run):

   ```bash
   WEBAGENT_DB_HOST=localhost WEBAGENT_DB_PORT=5433 WEBAGENT_DB_NAME=webagent \
   WEBAGENT_DB_USER=webagent WEBAGENT_DB_PASSWORD=webagent WEBAGENT_DB_SSLMODE=disable \
   python -m app.db.migrate_sqlite_to_pg data/db/local.db
   ```

3. **Activate Postgres** — either:
   - In the app: **Config → Storage → Application Data → Postgres → Test → Save → Activate**, or
   - Env-lock it (see the `WEBAGENT_DB_*` block in `.env.example`) and restart.

   SQLite stays intact as a fallback. If the configured Postgres is unreachable at
   startup, the app automatically falls back to SQLite. To go back to SQLite, set the
   provider to `sqlite` in the Storage modal (or remove the env-lock) and restart.

For a **remote/managed Postgres** (Cloud SQL, Neon, etc.) later, the only change is the
connection config (host/port/SSL + password in the secrets vault) — no code changes.

## Quick test

1. Start **`uvicorn`** as above.
2. Open **`/index.html`** or **`/test`**.
3. Ensure a **session** row exists for your **`user_id`** / **`session_id`**.
4. Chat; defaults apply on first use when the DB has **`context_templates`**.

## Deployment

> Plain-English version in the Wiki: [Deploying to the Cloud](http://localhost:8080/?tab=wiki&article=deploying-to-the-cloud) and [Editions and the Production Path](http://localhost:8080/?tab=wiki&article=editions-and-the-production-path). The exact host steps and the Cloud Run checklist are below.

Use any Python-capable host (Railway, Render, Fly.io, Docker, etc.). Set the same env vars; use **cloud** + Supabase when you run multiple app instances.

### Google Cloud Run

webAgent is designed to run on Cloud Run. The `Dockerfile` is Cloud Run-ready (`PORT` env var, health check, CORS).

**Cloud Run compatibility checklist (maintain when changing code):**

| Requirement | Why |
|-------------|-----|
| All new Python deps added to `requirements.txt` | Cloud Run builds from `pip install -r requirements.txt` — missing deps crash at runtime |
| No dependency on persistent local filesystem | Container filesystem is **ephemeral** — use Supabase (cloud mode) for DB, Supabase Storage for uploads |
| WebSocket endpoints accept remote clients | Both `/api/v1/agent/ws` and `/api/v1/terminal/ws` accept connections from any origin (no loopback guard) |
| File writes backed by `git push` | Code changes on ephemeral disk are lost on container recycle — commit and push to persist |
| `WEBAGENT_CONFIG_SOURCE=env` set | Locks storage config to env vars so `db_mode.json` / `db_connection.json` / `secrets_mode.json` writes are suppressed (they would only land on the ephemeral container disk). Provider, host, db, user, and the secrets vault are sourced from env vars instead. |
| Secrets in a real vault | Use `WEBAGENT_SECRETS_PROVIDER=gcp_secret_manager` on GCP (set `GCP_PROJECT`) — never leave passwords in `inline_db` for production. |
| `asyncpg` available | Required when using any Postgres-family provider (Supabase, Postgres, Amazon RDS, Google Cloud SQL, Azure Database, Neon) for connection testing and schema bootstrap. Pinned in `requirements.txt`. |
| `aiomysql` + `sqlglot` available | Required for per-agent external data source connectors. `aiomysql` powers the `sql_mysql` connector; `sqlglot` is used by SQL connectors to parse statement type and referenced tables before sending to the customer DB. Both are pinned in `requirements.txt`. |
| Automation scheduler set to a remote backend on Cloud Run | The default `LocalScheduler` in **`app/scheduler/local.py`** is in-process — it does not survive container recycles and will not fire across multiple instances. For Cloud Run deployments switch the provider to `google_cloud` under **App Config → Agent Settings → Automation Engine** (pushes each job to Google Cloud Scheduler — see **`app/scheduler/providers/google_cloud.py`**). |
| Event-trigger poll + renewer loops are in-process | The loops in **`app/events/poller.py`** and **`app/events/renewer.py`** share the same Cloud Run limitation as the local scheduler — they only run while the container is warm. Push-based event sources (Gmail / Outlook / Calendar / Drive / Dropbox / Shopify / comms bridges) work fine on Cloud Run because providers POST directly to **`/api/v1/events/{source}`** and wake the container; poll-only sources (Twitter, Reddit) need a long-running host (the GCE VM) or an external scheduler that hits the poll endpoints. Subscription renewal also needs a warm instance — if you scale to zero, set `min_instances=1` or invoke a refresh from Cloud Scheduler. |
| Agent Orchestration spawns rely on background work | Forked spawned-helper runners and the orchestration recovery poller (**`plugins/abilities/agent_orchestration.py`**) are in-process background tasks. They need **CPU always allocated** (not request-scoped — otherwise a forked helper is frozen after its request returns) and **`min_instances=1`** (so a poller is always alive to recover spawns orphaned by a recycle; with scale-to-zero, orphans wait until the next request). Recovery **is** multi-instance safe (heartbeat liveness + atomic token claim, so two instances can't re-run the same spawn), but the loopback that runs a spawn always targets the same instance that fired it. Blocking (`wait=true`) spawn chains run inside one request — keep them under the Cloud Run request timeout; prefer forks for long work. Tune throughput with `WEBAGENT_SPAWN_CONCURRENCY`. |
| `doc_store` connector uses Supabase Storage in cloud mode | The local-filesystem variant relies on a writable container path that survives between requests. In Cloud Run that path is ephemeral, so the data-source admin UI hides the `local` backend option when `WEBAGENT_CONFIG_SOURCE=env`. Use `backend: supabase_storage` instead, and apply **`migrations/014_data_sources.sql`** to add the `data_sources`, `agent_data_sources`, and `doc_chunks` tables (plus pgvector + the `doc_chunks_hybrid_search` RPC) before attaching any sources. |

**Deploy (manual):**
```bash
gcloud run deploy webagent \
  --source . \
  --region europe-west1 \
  --allow-unauthenticated
```

### Google Compute Engine VM (persistent disk)

Unlike Cloud Run, a GCE VM has a real, persistent filesystem — so you can keep code under version control on the box itself and pull updates in place from the GitHub tab inside the app.

**One-time VM setup:**

1. **Clone the repo** to a stable path, e.g. `/opt/webagent`, and create the `.venv` + `.env` next to it (same layout as local dev).
2. **Create a dedicated user** for the service (e.g. `webagent`) and `chown -R webagent:webagent /opt/webagent`. The user needs read/write on the repo so the in-app **Pull & Restart** button can run `git pull` and rewrite files.
3. **Install the systemd unit** shipped at **`scripts/webagent.service`**:
   ```bash
   sudo cp /opt/webagent/scripts/webagent.service /etc/systemd/system/
   sudo systemctl daemon-reload
   sudo systemctl enable --now webagent
   ```
   Edit `User`, `WorkingDirectory`, `EnvironmentFile`, and the `ExecStart` venv path inside the unit before enabling it if your layout differs.

**Why systemd matters for the GitHub tab’s Pull & Restart button:**

- **`POST /api/v1/restart`** (`app/main.py`) terminates the uvicorn process with `os._exit(0)`.
- The unit file has `Restart=always` / `RestartSec=1`, so systemd immediately relaunches uvicorn from the just-updated source tree.
- Without a supervisor (or with a plain `nohup ./scripts/start_webAgent.sh &`) the server stays dead after restart and the app becomes unreachable.

**How updates flow (GCE):**

1. You push code from a dev machine to GitHub.
2. On the VM, in the running app, open the **GitHub** tab as admin — or switch the **File Editor** sidebar to its Source Control view (the GitHub icon next to Explorer).
3. Click **Pull & Restart** — the UI calls `/api/v1/github/pull`, then `/api/v1/restart`, then polls `/health` until the new process is up.
4. Static assets (`ui/`, `index.html`) refresh on the next page reload; Python backend code is now the freshly-pulled version because systemd relaunched uvicorn.

**File Editor sidebar — Source Control view.** The Files tab’s left sidebar has two view modes, toggled by icon buttons at the top:
- **Explorer** (default) — the directory tree.
- **Source Control** — a compact GitHub manager (status, changes, commit, push/pull, token) plus a VS Code-style commit graph that shows *all* branches/refs (not just `main`). Each row has a coloured lane dot; merge bubbles and side branches render as bent connector lines. The graph is fed by `GET /api/v1/github/log-graph`, which extends `/status` with `git log --all` plus pre-computed lane positions so the frontend just draws SVG segments. Both `/status` and `/log-graph` take an optional `fetch` flag (default `1` = run a live `git fetch` from origin first, so ahead/behind and `origin/*` tips are accurate; `fetch=0` = skip the network round-trip and serve last-known local refs instantly). **Opening the panel uses the fast local-only read (`fetch=0`) so it paints immediately, then refreshes from origin in the background** (a brief "syncing" hint shows on the header) and repaints in place. The **refresh** button (the circular-arrow icon next to **Merge**), and the post commit/push/pull/merge/checkout refreshes, go straight to a live fetch so they reflect origin; refresh repaints *only* this sidebar without touching the rest of the app and spins while loading.
  - **Multiple repos — the repo selector.** A dropdown at the top of the panel chooses **which repo the page acts on**. The built-in **webAgent (this app)** entry is always first and behaves exactly as before; **Add repository…** registers any other local git folder paired with its own GitHub address + key — the folder field has a **Browse…** picker that walks the host's folders via the existing `GET /api/v1/files/tree`. Picking a repo re-points the **whole page** (Changes list, commit graph, branches, commit/push/pull) at that repo using *its* key; the Production section and the shared-key row hide for non-default repos (each carries its own key, edited from its row). The switch is server-side: the active selection is resolved at the top of every Git endpoint — `_use_active_repo()` re-points the single `_run_git` working dir + token — so the agent's **Git Control** ability is deliberately **pinned** to this app's repo (`_pin_to_project_root()`) and never follows the page's selection. Engine: `app/git_repos.py`; endpoints `GET /api/v1/github/repos`, `GET …/repos/validate`, `POST …/repos`, `POST …/repos/select`, `POST …/repos/{id}`, `DELETE …/repos/{id}`; the list lives in the gitignored `data/config/git-repos.json` (added repos store their own folder/remote/key; the webAgent entry is synthesized live and never persisted).
  - **⭐ Instant commit + push.** Next to the commit-message box sits an amber **star** button (enabled when there are uncommitted changes *or* unpushed commits). Clicking it runs the **whole job immediately in a single request** — no chat agent, no waiting through turns. It calls `POST /api/v1/github/commit-and-push`, which stages everything, **auto-writes a commit note from the diff** via a lightweight LLM sub-call (or uses the note typed in the box, if any), runs a **secret-scan safety check** (refuses obvious tokens/keys in the diff), then commits and pushes — reporting the note, hash, and push result inline in the sidebar. **Live progress:** with `stream: true` the endpoint emits newline-delimited JSON phase events (`analyzing` → `scanning` → `message` → `committing` → `pushing`, then a final `done` with the result), so the button shows **granular live status** — a spinner over the button, a stepped status line, and a green check on success — and surfaces a note when the AI message-writer was unavailable and the deterministic file-list summary was used instead. Without the flag it runs to completion and returns a single result dict (the agent-tool path); the frontend also degrades to that automatically if the server doesn't stream. The auto-writer retries a couple of times on a fast failure (empty reply / connection blip) but never waits through a timeout, and its deterministic fallback **title degrades gracefully** (names files → summarises by area) rather than collapsing to a bare "update N files". The shared core (`app/api/github.py` → `_commit_and_push_events`, drained by `commit_and_push_repo`) also backs the **Git Control** ability's `commit_and_push` agent tool (`plugins/admin/source_tools.py`), so the button and the agent tool can't drift. Frontend wiring: `ui/shared/js/files-git.js` (`runCommitFlow`, shared by the ⭐ and plain **Commit** buttons; the NDJSON reader is `streamCommitPush`). All git calls route through the same `_run_git` helper the Push button uses, so auth is identical.
  - **Production (trimmed mirror) — "Release to production".** Below Sync, a **Production** section publishes a *trimmed copy* of this repo to a **separate** GitHub repo (its own folder + remote + history) — everything except the folders **and files** an admin marks **dev-only** in the **File Explorer**. The explorer's toolbar **More** menu (⋯) holds the production tools: *Show dev checkboxes* enters *production-preview* mode — a checkbox appears on every folder and file (ticked = ships, untick = dev-only); unticking a folder dims + locks everything beneath it, and a right-click *Exclude / Include from production* does the same for a single item. **Folder checkboxes are tri-state** — a partial (dash) tick means *some* items inside are dev-only; clicking a partial folder includes its whole subtree, clicking it again excludes the whole folder (all-on → all-off). Git can't push "everything except folder X", so production is regenerated on demand: list the project's real files (`git ls-files`, so `.gitignore` is honoured), drop the excluded paths, mirror the rest into the production repo (wiping all but its `.git` so deletions propagate), secret-scan, **auto-write the release note**, then commit + push — streamed as NDJSON like the ⭐ button. **The release note auto-writes exactly like the ⭐ commit&push:** a blank note (the File Explorer's Sync/Push always leave it blank; the Git page's *Release note* field can override it) triggers the *same* lightweight LLM writer (`_generate_commit_message`), fed the production repo's already-staged diff, so a release gets a real conventional-commit summary of what changed — falling back to the deterministic *"Release from dev … — N files"* line if the writer is down; the stream's `message` → `message_ready` / `message_fallback` phases mirror the ⭐ button's so both surfaces show the same "writing a release message…" step and the written title. The engine is **two composable halves** so the same work runs as one click or as two steps: **sync/copy** (build the trimmed tree in the sister folder + commit it there *locally*, no push) and **push** (send that commit to the production remote). The Git page's **Release** button runs copy-then-push back-to-back; the explorer's **More** menu exposes the halves separately as **Sync to production** and **Push to GitHub** (so an admin can stage and review before publishing). At the top of that menu are **three fields** — **Production repo** (the destination GitHub remote; accepts an `owner/repo` shorthand that's expanded to a full `https://github.com/owner/repo.git` on save, and shows the friendly short form, e.g. `botboss3000/webagent`), **GitHub key** (the personal-access / fine-grained token used to push to that repo) and **Production folder** (the local destination, defaulting to a sibling of the dev repo — `…-dev` → `…-prod`, e.g. `webagent-prod`) — plus a live **in-sync line** that, when the menu opens, compares the dev shipping set against the production folder and reports the result (*In sync — production matches dev* or *Differs: N to add, N to update, N to remove*; *Not synced yet* before the first sync). **The key is a secret, so it goes to the encrypted vault, not the local config** — it's stored in `auth_elements.secret_ref` (per-tenant Fernet when field encryption is on; the same store the deploy panel uses, keyed by service `production_mirror`), the field is **masked, never pre-filled, and never echoed back**, and a **blank value leaves the stored key unchanged** (the config GET returns only a `token_set` boolean). When no production key is stored the engine falls back to the shared Git-page token from `provider.json`, so existing setups keep working with no key entered; the repo + folder still save to the local config. **Sync to production** is a *one-way sync* of the shipping set from dev into the production folder — it copies new/changed files and drops files no longer shipped — then commits locally. The first sync clones the existing remote (one-time, ~20s — the status line says so); the secret-scan **blocks** the sync if a key/token is found in the shipping set (mark that file dev-only or remove the secret, then retry). First setup is non-destructive (it **clones** an existing remote so the mirror commits on top; only an empty remote gets a fresh `init`). Engine: `app/production_mirror.py` (`copy_events` / `push_events` / `release_events` / `diff_status` + `_normalize_remote_url` + `_auto_commit_message` (reuses the ⭐ button's LLM writer) + the vault helpers `save_vault_token` / `get_vault_token` / `_resolve_token`); endpoints `GET/POST /api/v1/github/production/{config,exclude}` (config POST takes `github_token` → vault; config GET returns `token_set`), `POST …/exclude-bulk`, `GET …/status`, `GET …/diff`, `POST …/{release,copy,push}`; non-secret settings in the gitignored `data/config/production-mirror.json`, the production **key** in the encrypted vault (and the shared `provider.json` token as the fallback).

**Troubleshooting:**
- `journalctl -u webagent -f` — live logs from the unit.
- If Pull & Restart hangs at "Waiting for relaunch…": systemd isn’t restarting the process. Check `systemctl status webagent` and verify `Restart=always` is set.
- If `git pull` fails with auth errors: set a GitHub Personal Access Token via the GitHub tab → Settings (stored in `provider.json` under the service user’s home / repo dir).

## Maintenance

### Resetting local state (`reset_webagent.bat`)

Use **`reset_webagent.bat`** (repo root, Windows) to wipe webAgent back to a clean state in one go. Intended for local dev — handy when prepping a clean handoff, reproducing a first-run bug, or escaping a corrupted local DB. Double-click or run from a terminal.

**What runs automatically (no prompt):**

1. **Stops the running app** — kills any process on port 8080 plus `python.exe` / `python3.exe`, the same pattern as `kill_webagent.bat`. This is required so the SQLite WAL isn't held open during deletion.
2. **Wipes the userbase** — the database files (`data\db\local.db` plus `-journal`, `-wal`, `-shm`, `.preprompt-bak`, and any stray root-level `local.db`) and the per-user generated pages directory (`visuals\users\`). Credentials (`data\db\vault.db`), the LLM key/config, and logins are preserved unless their opt-in group is selected; the diagnostics logs (`data\db\logs.db`, `recordings.db`) are kept unless explicitly cleared. The DB schema, the default `admin / admin` user, and the agent templates are all re-seeded automatically on the next `webAgent.bat` start (see `_init_db()` and `_seed_agent_templates_from_json_files()` in `app/db/local.py`, and `_ensure_default_admin()` in `app/auth/users.py`).

**Prompts (in order):**

| # | Prompt | Default | Targets when "yes" |
|---|--------|---------|---------------------|
| 1 | Back up files to `temp\reset-backup-<timestamp>\` before deleting? | **Yes** | Every targeted file/folder is `move`d into a timestamped folder under `temp\` mirroring its original path (reversible). On "No", everything is hard-deleted via `del /F /Q` or `rmdir /S /Q`. |
| 2 | Clear app secrets (LLM API keys, OAuth tokens, integration creds, scheduler config)? | No | `data/config/provider.json`, `data/config/app-settings.json`, `data/config/scheduler_config.json`, `app\db_mode.json`, `app\pages_mode.json`, `app\db_connection.json`, `app\secrets_mode.json`. App falls back to local defaults when these are missing. |
| 3 | Clear local user accounts (passwords, remember-me tokens)? | No | `app\auth\users.json`, `app\auth\users.json.bak`. `admin / admin` is re-created on next start. |
| 4 | Delete `.env`? | **No** | `.env` at repo root. Default No — most installs (Supabase, OAuth client IDs) need this file to start. |
| 5 | Delete agent template JSON files in `app\context\agents\`? | No | All `*.json` under `app\context\agents\`. **Warning:** there is no fallback — the next start will boot with zero agents until you restore templates. |
| — | Final **Proceed? [y/N]** summary lists every path that will be processed, tagged `[BACKUP]` or `[DELETE]`. Defaults to No (abort). | No | — |

After execution the script prints `OK` / `FAIL` / `SKIPPED` counts and (if backups were enabled) the backup path. Run `webAgent.bat` afterwards to relaunch from a clean install.

**Related scripts:** `webAgent.bat` (start + auto-restart), `kill_webagent.bat` (stop-only), `export_agent_templates.bat` (reverse-seed: DB → JSON templates).