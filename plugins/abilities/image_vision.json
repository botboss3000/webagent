{
  "_comment": "Companion config for the image_vision ability, read by app/agent/attachment_router.py. 'handles' lists the mime-types this ability routes; 'worker_system' is the one-shot describe worker's system prompt ({context} = recent conversation, {request} = the user's message); 'guidance' templates are folded into the user turn so the agent knows what happened and what its options are.",
  "handles": ["image/png", "image/jpeg", "image/gif", "image/webp"],
  "worker_system": "You are a vision assistant. A user attached an image to a conversation and the main assistant cannot see images, so your detailed description IS what it will rely on. Look at the image and describe it thoroughly and factually, tailored to what the conversation needs. Include any visible text verbatim and concrete, measurable detail: objects, layout, colours, positions, counts, spatial relationships, and (where relevant) dimensions, lighting, and materials. If the conversation implies a purpose, describe with that purpose in mind. Do not speculate beyond what is visible.\n\nConversation context:\n{context}\n\nWhat the user asked:\n{request}",
  "worker_instruction": "Describe this image in detail, tailored to the conversation above.",
  "guidance": {
    "switch_available": "NOTE: your current model cannot see images, so a vision model's description of the attached image has been added below. Answer from it if it's enough. If the user's request needs you to inspect the image directly (fine visual detail, iterating on a design), you may call switch_model('{switch_target}') to run on a vision-capable model, then revert with switch_model('default') when done. If a detail is missing, call process_image with a precise question.",
    "describe_only": "NOTE: your current model cannot see images, so a vision model's description of the attached image has been added below. Answer from it. If you need a sharper detail, call process_image with a precise, narrow question (you can call it again with an even tighter question).",
    "not_enabled": "An image was attached ({names}) but your Image Vision ability is not enabled, so you cannot see images. Tell the user plainly that your Image Vision ability is turned off and that an admin can enable it for this agent (Agent Settings → Abilities → Image Vision). Do NOT say it is a limitation of your model, and do NOT guess, invent, or describe the image's contents.",
    "no_vision_model": "An image was attached ({names}) but no vision model is configured, so your Image Vision ability has nothing to look at the image with. Tell the user plainly that image viewing isn't set up and that an admin can add a vision model in App Config → Models (tick In on a vision-capable model). Do NOT guess, invent, or describe the image's contents."
  }
}
